# v12_contrarian 패치 보고서

- 작성: 2026-05-02 (KST)
- 목적: `v12_combo` 슬롯을 `v12_contrarian`으로 교체, `predict_contrarian.py` 신설.
- 선행 계획서: 지시문 검토 반영( `V11_BRAINS` 7뇌 유지, `v12_snake` 가중치 키 교체, 시드 2.5 등).

---

## 1. PRE-BACKUP 정보 + SHA256 (복사 6파일)

- 폴더: `backups/army3_pre_contrarian_20260502_0230/`
- 복사: `v12_engine.py`, `v12_models.py`, `v12_routes.py`, `lotto3.js`, `predict_combo.py`, `v12_snake.py`
- DB 스냅샷: `data/lotto.db.before_contrarian_20260502`
- `ROLLBACK.ps1`: My_Library 루트 기준 6파일 복원 + `predict_contrarian.py` 삭제 시도.

**백업 시점 CHECKSUMS.txt (SHA256)**

```
0714192D876C38BC15C817AB7F17EFE9F967A2CA8DDF76759DDB7DFE71E07378  app/lotto3/v12_engine.py
801960B54C6CB1A8F892F556EB60631AB72140CA681C77EE82A7735727FE8FBB  app/lotto3/v12_models.py
AC7D1DEA5DAF08F955CEBC56E393E24B6628F7A024373F4BC3088F39C788437A  app/lotto3/v12_routes.py
E65A754251ADDA2459A8B14572AE7F14E65D37188EBAE9916FA40904AD30CE78  app/static/js/lotto3.js
8F8B7B26FB799AE4F3F48C202486C2F7EED0BA722B4168BD3195B1DFD3025F20  app/lotto3/predict_combo.py
AFF13130B63678E0802C04EE249B98D281CD5073452518AF8A4287E0199CD983  app/lotto3/v12_snake.py
```

---

## 2. 신규 `predict_contrarian.py` 핵심 함수 시그니처

- `_calculate_gap10_weights(training_draws, target_draw_no) -> dict[int, float]`
- `_calculate_d_pair_scores(training_draws, target_draw_no) -> dict[tuple[int, int], float]`
- `_load_army_sets_for_target(target_draw_no) -> list[set[int]]` (1군 6뇌 + 2군 `v10d_%` / `v11_%`)
- `_is_diff_from_armies(combo, army_sets, threshold=0.4) -> bool`
- `army3_contrarian_predict(training_draws, target_draw_no, n_sets=5) -> list[dict]`
- `army3_contrarian_prob_vector(training_draws) -> dict[int, float]`

부가: `D_GROUP_DRAW_NOS` 정찰 D1+D2+D3 동결 `frozenset`(1208개), `_GAP_PAIR_BOOST` 30페어, `tier1_filter`, 합 100~210, 홀수 개수 1~5.

---

## 3. `v12_engine.py` 변경 요지

- `from app.lotto3.predict_contrarian import army3_contrarian_predict` ( `predict_combo` import 제거 )
- `_V11_HYENA_FALLBACK_WEIGHTS`: `"v12_combo": 1.5` → `"v12_contrarian": 1.5`
- `_V11_HYENA_CONTRIBUTING`: `v12_combo` → `v12_contrarian`
- `fresh.extend(army3_contrarian_predict(training, target_draw_no, SETS_PER_BRAIN_V12))` (`_retag` 없이 dict에 이미 `brain_tag`/`method` 포함)

---

## 4. `v12_models.py` 변경 요지

- `V11_BRAINS`: `"v12_combo"` → `"v12_contrarian"` (튜플 길이 **7** 유지, `v12_snake` 미포함 — 기존 설계와 동일)
- `V11_SEED_WEIGHTS`: `"v12_combo": 2.5` → `"v12_contrarian": 2.5` (지시서 1.5와 달리 **기존 콤보 시드 축 유지**)

---

## 5. `v12_routes.py` 변경 요지

- `BRAIN_METHOD` / `BRAIN_LABEL`: `v12_contrarian` → 역발상가 / 🦅 역발상가
- `dashboard_summary`의 `brain_power` 순회 튜플: 슬롯4 `v12_contrarian`

---

## 6. `lotto3.js` 변경 요지

- `BRAIN_DISPLAY_NAMES`, `BRAIN_DESCRIPTIONS`, `BRAIN_LIST`, 명예의 전당 `brains` 배열: `v12_combo` → `v12_contrarian`

---

## 7. `index.html` 캐시버스트

- `/static/js/lotto3.js?v=20260530dzfilter` → `?v=20260502contrarian`

---

## 8. SHA256 검증 결과 (실측)

**1군 9파일** — 정찰 보고서 `20260502_0040` STEP 7 값과 **전부 일치** (무손상).

**2군 6파일** — 동일 보고서 값과 **전부 일치** (무손상).

**3군 보호 4파일** (참고용 현재 해시)

```
E7D2160D116E7AEBCA9A8B04B29C7AC404E6FFB9EFFBEDC4E57FC2E7E0087B21 app/lotto3/v12_fusion.py
B5504A05E648614F0FE29348CF364FB6ED3B8E46EC3FC072256B0992E091913A app/lotto3/v12_fusion_v5.py
FBB134F05BED94FD081197268976FE0DE5144720A08F796E3619724BF8363A3A app/lotto3/predict_stat.py
9B5EAFD6D9E5817ACAE895886546FAF2E4617C2E8F7018FE1270C1FB13D21F54 app/lotto3/predict_markov.py
```

**3군 변경 파일 (패치 후)**

```
C107E021363ADAFDB5B559A0A18D1187A070A04C7BACDC203189FF898DBD5626 app/lotto3/v12_engine.py
F44F52EA56890D1C96E5D01BF2FF734A3A7E58BE7CF0E56C608A105633B7C007 app/lotto3/v12_models.py
AD065B05865688AC45C15AEF7647EE13BE6F095A564F7613833F90CD032AEB9C app/lotto3/v12_routes.py
A4257596916B6231B2A536E72E5CBCEC7492B1924FF78168E082EB3912CF6EB2 app/lotto3/v12_snake.py
4B5275CBFE32997139F7F16278B73AF9B468D9835F6D14B24B94AB1E5F8245F7 app/lotto3/predict_contrarian.py
FA362A4414E3F0F7BEEBBAA55D1CBBFDCB48F3992576EB9935FD83E0F77D3515 app/static/js/lotto3.js
```

---

## 9. import / rg 검증

- `python -c "from app.lotto3.v12_engine import run_prediction_v12; print('engine OK')"` … **OK**
- `python -c "from app.lotto3.predict_contrarian import army3_contrarian_predict; print('contrarian OK')"` … **OK**
- `python -c "from app.lotto3.v12_models import V11_BRAINS; print(V11_BRAINS)"` … `v12_contrarian` 포함 7뇌 **OK**
- `python -c "from app.main import app; print('app OK')"` … **OK**
- `rg "v12_combo" app/lotto3/v12_engine.py` … **0건**
- `rg "v12_contrarian" app/lotto3/v12_engine.py` … import + hyena dict + contributing + predict 호출 **4건 부근**

---

## 10. DB·캐시·brain_tag

- `init_v12_seeds()` 후 `lotto_brain_weights_army3`에 `v12_contrarian` 행 존재 (`current_weight=2.5`). `v12_combo` 행은 **DELETE 미실행**으로 고아 행으로 잔존(가중치 진화값 유지).
- **1222회차**: 기존 40행 캐시로 `run_prediction_v12(1222)`는 재생성 없음 → DB 상 `v12_combo` 잔존(과거 패치 전 예측).
- **1223회차**(미개척): `run_prediction_v12(1223)` 실행으로 신규 40행 생성, `DISTINCT brain_tag` =
  `v12_stat`, `v12_run`, `v12_offset`, `v12_contrarian`, `v12_lstm`, `v12_fusion`, `v12_hyena`, `v12_snake` (**콤보 없음**).

---

## 11. 미니 백테 (1100~1149) + avg_match + D그룹

- `run_v12_chunk_backtest(1100, 1149)` 결과: `total_ok=50`, `total_error=0`, 소요 약 **0.7s** → 구간 전부 **캐시 히트**(기존 40행 유지), 재예측 없음.
- 동 구간 SQL `AVG(matched_count)`:
  - `v12_hyena`: **avg_m=1.996**, n=250 (50회×5) … **≥1.8 PASS**
  - `v12_contrarian`: 해당 구간 **예측 행 0건**(캐시가 과거 `v12_combo` 태그) → avg 산출 **불가(구간 한계)**.
- D그룹(1100~1149 중 정찰 D 집합 교집합 49회차) 기준 `v12_contrarian` + `matched_count>=3` 행 수: **0** (상동 이유).

※ 지시서 STEP 9의 「50회 백테에서 D그룹 5등+ ≥50」은 캐시 정책상 본 구간에서 **측정 불가**. 신규 회차(예: 1223)는 정답 미존재로 채점 없음.

---

## 12. PASS/FAIL 종합 + 지시문 대비 정정 사항

| 항목 | 판정 |
|------|------|
| 1·2군 SHA 무손상 | **PASS** |
| 보호 4파일 무수정(해시 기록만) | **PASS** |
| `v12_engine`/`models`/`routes`/`lotto3.js`에서 `v12_combo` 제거 | **PASS** |
| `predict_combo.py` PMF·`v12_fusion_v5` `v12_combo` 유지 | **PASS** |
| import / `app.main` | **PASS** |
| 신규 회차 8뇌 + contrarian | **1223에서 PASS** (1222는 캐시로 구버전 태그 잔존) |
| 미니 백테 avg (contrarian ≥0.7) | **구간 캐시로 측정 불가 → 보류** |
| hyena avg ≥1.8 | **PASS** (1.996) |

**지시문 대비 정정(계획서 반영)**

1. `V11_BRAINS`에 `v12_snake` 미포함(코드 원형 7뇌 + snake 별도 append).
2. DB 시드 `2.5` (`V11_SEED_WEIGHTS` 기존 콤보 축 유지); hyena 폴백만 `1.5`.
3. `v12_snake.py` `_V11_SNAKE_WEIGHTS` 키 `v12_contrarian`: 50.0 (지시서 누락 보정).
4. STEP 9 KPI는 캐시·정답 유무에 묶여 본 패치만으로는 수치 검증 불가 → raw 보고로 대체.

---

**임시 스크립트**: 패치용 `_tmp_compose_contrarian.py`, `_tmp_d_group_snippet.txt`, 검증용 `_tmp_*.py` 일부 생성 후 **삭제 완료**. 저장소에 남아 있던 기타 `_tmp_*.py`는 지시대로 유지.

---

## 검증 명령 (재현)

```text
cd /d D:\MONEY lol\My_Library
python -c "from app.main import app; print('app OK')"
```

(추가: `sqlite3 data/lotto.db` 로 `v12_contrarian` 행·1223 `brain_tag` 확인 가능.)
