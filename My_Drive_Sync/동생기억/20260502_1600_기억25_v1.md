🦅 동생 작성 지시문 기반 — 커서 자동 생성 (옵션 B-Lite, 압축 금지)

---

# 🧠 기억25_v1 (2026-05-02 16:00 KST)

📅 **저장 시각**: 2026-05-02 16:00 KST  
🎯 **목적**: B-Lite 첫 테스트(커서 자동 합성) — 기억22 + 정찰보고서 9건(0900·1000·1100·1200·1300·1330·1430·1500·1545) 통합, **hyena·lottery_score·학습분산·31해시** raw 보강  
🔗 **선행**: `20260502_0830_기억22_v1.md` · `20260502_1345_기억24_v1.md` · `STATUS_LATEST.md` (13:45)

---

## 1. 📍 한 줄 상태

B-Lite **첫 테스트 PASS**: 1430 벤치마킹(1군 vs 3군 학습·분산 SQL) + 1500 `lottery_score` + 1545 **hyena 구조**까지 합성. **3군 결함 6건**을 코드·DB raw로 재정렬. **1군 5뇌 역할 분담 철학**은 이번 합성 입력에 **전용 정찰 보고서 없음(정찰 0)** → 동생 핵심 누락으로 표기, **다음 작업 B** 예약.

---

## 2. 🔗 직전 세이브 연계

| 구분 | 참조 |
|------|------|
| 직전 기억 | `20260502_1345_기억24_v1.md` — 14섹션 B-Lite, 5의심·DELETE·1222 COUNT=0 정정 |
| 직전 STATUS | `STATUS_LATEST.md` (13:45 갱신) — 본 세이브에서 **16:00**으로 덮어쓰기 |
| 옵션 B 뿌리 | `20260502_0830_기억22_v1.md` — Drive·8뇌 실적·컨닝 raw |

---

## 3. 📑 B-Lite 합성 소스 (자동 읽기·통합)

1. `20260502_0900_3군8뇌_시스템정찰_보고서.md`  
2. `20260502_1000_시스템정리_보고서.md`  
3. `20260502_1100_3군8뇌_본질구조_정찰보고서.md`  
4. `20260502_1200_v12snake_학습메커니즘_정찰보고서.md`  
5. `20260502_1300_회피_counterfactual_정찰보고서.md`  
6. `20260502_1330_DB정리_DELETE_보고서.md`  
7. `20260502_1430_3군학습결함_벤치마킹_정찰보고서.md`  
8. `20260502_1500_1군lottery_score_정찰보고서.md`  
9. `20260502_1545_1군hyena_구조_정찰보고서.md`

---

## 4. 🧾 핵심 raw 10줄 (동생 작성, 추측 0)

아래 10줄은 **요지 한 줄 + 보고서·코드·SQL 원문**(잘라내지 않음). 인과 단정은 **1545·1430 보고서가 허용한 범위**만.

### 4-1. 1군 hyena = 도둑 설계 (5뇌 흩뿌린 번호 → 5005 전수 재조합), 1등 2건 / MAX=6

- **입력 태그 튜플(raw, 파일 전체 한 줄)** — `20260502_1545_1군hyena_구조_정찰보고서.md` 인용:

```145:145:d:\MONEY lol\My_Library\app\lotto\engine.py
_BASE_HYENA_SOURCE_TAGS: tuple[str, ...] = ("stat", "markov", "llm", "lstm", "fusion")
```

- **5005 전수·정렬(raw)** — 동 보고서 인용:

```97:106:d:\MONEY lol\My_Library\app\lotto\predict_hyena.py
def _evaluate_combinations(
    pool: list[int], consensus: dict[int, float]
) -> list[tuple[tuple[int, ...], float]]:
    try:
        out: list[tuple[tuple[int, ...], float]] = []
        for combo in combinations(pool, 6):
            t = float(sum(consensus.get(n, 0.0) for n in combo))
            out.append((tuple(sorted(combo)), t))
        out.sort(key=lambda x: -x[1])
        return out
```

- **DB (`target_draw_no <= 1221`)** — `20260502_1545` STEP 7 SQL·출력 그대로:

```sql
SELECT brain_tag, COUNT(*) AS cnt, AVG(matched_count), MAX(matched_count), SUM(CASE WHEN matched_count=6 THEN 1 ELSE 0 END) AS hits_6
FROM lotto_predictions
WHERE brain_tag='hyena' AND target_draw_no <= 1221;
```

```text
hyena|5955|2.28194794290512|6|2
```

### 4-2. 3군 v12_hyena = 1군 미러 구조, 입력만 다름 (llm 빠짐 + run/offset/contrarian 추가), 1등 0건 / MAX=5

- **3군 입력 연장(raw)** — `20260502_1545` 인용:

```299:305:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
    fresh.extend(_retag(army3_stat_predict(training, SETS_PER_BRAIN_V12), "v12_stat", "V11통계두뇌"))
    fresh.extend(_retag(army3_run_predict(training, SETS_PER_BRAIN_V12), "v12_run", "🎯 사냥꾼"))
    fresh.extend(_retag(army3_offset_predict(training, SETS_PER_BRAIN_V12), "v12_offset", "🎼 리듬분석가"))
    fresh.extend(army3_contrarian_predict(training, target_draw_no, SETS_PER_BRAIN_V12))
    fresh.extend(_retag(army3_lstm_predict(training, SETS_PER_BRAIN_V12), "v12_lstm", "V11LSTM두뇌"))
    fresh.extend(v12_fusion_predict(training, target_draw_no, SETS_PER_BRAIN_V12))
```

- **DB (`target_draw_no <= 1221`)** — `20260502_1545` STEP 7 SQL·출력 그대로:

```sql
SELECT brain_tag, COUNT(*) AS cnt, AVG(matched_count), MAX(matched_count), SUM(CASE WHEN matched_count=6 THEN 1 ELSE 0 END) AS hits_6
FROM lotto_predictions_army3
WHERE brain_tag='v12_hyena' AND target_draw_no <= 1221;
```

```text
v12_hyena|6080|2.14046052631579|5|0
```

### 4-3. 3군 결함 6개: #0 lottery_score 누락, #1 snake 학습 밖 (하드코딩 50/30/5), #2 combo 학습 밖, #3 fusion Top-K 무조건 append, #4 회피 임계 이원화 (0.4 vs 0.6), #5 hyena 1등 0건 (입력 차이)

| # | 결함 | raw 근거(파일·보고서) |
|---|------|------------------------|
| 0 | **lottery_score 누락(3군 갱신 경로)** | `20260502_1500_1군lottery_score_정찰보고서.md` — `analyze_prediction_feedback` 반출 dict에 **`lottery_score` 키 없음** 서술 + `update_brain_weights`가 `get_brain_tag_ranking`→`_calculate_lottery_score`로 **동적 합산** (`321:334`, `372` 등 인용). 1430 STEP 4 표 **4-F 1군만 / 4-G 3군만** 대조. |
| 1 | **snake 학습 밖** | 기억24·1200: `_V11_SNAKE_WEIGHTS` **contrarian 50.0 · hyena 30.0 · stat/markov/lstm/fusion 각 5.0**, `update_v12_weights`로 **DB 갱신 없음**. |
| 2 | **combo 학습 밖** | 1100·1430: `v12_fusion_v5` PMF 키에 **`v12_combo`** 포함, `V11_BRAINS` 집계·`update_v12_weights` 트리거에서 **combo 제외**(1430 4-C 표). 잔존 **`v12_combo` 가중치 1행**(1330·STATUS). |
| 3 | **fusion Top-K 무조건 append** | `20260502_1100_3군8뇌_본질구조_정찰보고서.md` STEP 3-A 루프 원문: `while len(sets) < n_sets and attempts < 200:` … `cand = sorted(random.choices(nums_list, weights=w_list, k=6))` (인용 블록 그대로). |
| 4 | **회피 임계 이원화** | 1100 STEP 4-A `predict_contrarian.py` **`threshold: float = 0.4`** 전체 함수 블록 인용 + STEP 3-B **`_FUSION_JACCARD_THRESHOLD = 0.6`**, `_v12_is_diff_from_army1_fusion` **Jaccard ≥ 0.6** 탈락 인용. |
| 5 | **hyena 1등 0건(입력 차이)** | 1545: 입력 튜플 **145행 vs 299–305행** 상이 + 파이프라인 동형 + SQL **hits_6=0**·**MAX=5** 위 4-2 raw. **인과는 미증명**(1545 STEP9). |

### 4-4. 학습 강도: 1군 분산 256.19 vs 3군 0.74 (≈346배 차이)

`20260502_1430_3군학습결함_벤치마킹_정찰보고서.md` STEP 5-A·5-B **SQL 및 터미널 주석 결과 전부**(잘라내지 않음):

```sql
SELECT AVG((current_weight - s.av)*(current_weight - s.av)) AS var_pop
FROM lotto_brain_weights, (SELECT AVG(current_weight) av FROM lotto_brain_weights) s;
-- → 256.18947793246

SELECT AVG((current_weight - s.av)*(current_weight - s.av)) AS var_pop
FROM lotto_brain_weights_army3, (SELECT AVG(current_weight) av FROM lotto_brain_weights_army3) s;
-- → 0.736208745742604
```

비율: `256.18947793246 / 0.736208745742604` ≈ **347.9** (보고서 표기 “≈346배”와 **동일 차원**; 반올림·표기 차이만).

### 4-5. SHA256 31/31 PASS (lotto 9 + lotto2 6 + lotto3 16, 모든 정찰 후 변동 0)

`20260502_1430_3군학습결함_벤치마킹_정찰보고서.md` STEP 0 원문:

```markdown
- **사전·사후**: PowerShell `Get-FileHash -Algorithm SHA256`, 경로 키 기준 **31개 전부 동일** 확인 명령: `STEP12_POST_SHA256 31/31 identical to STEP0_PRE` (본 세션 터미널 raw).  
- **불일치**: **0건** → 중단 없이 진행.
```

해시 표는 해당 보고서 **20~52행** `| app/lotto/... |` **31행 전부**(중복 전재 생략 가능하나 **파일 열람으로 전수 존재**).

### 4-6. DB DELETE 7행 완료 (army3_* 6 + v12_markov 1), 잔존 8행 (V11_BRAINS 7 + v12_combo 1)

기억24·1330·`STATUS_LATEST`와 동일 서술. **raw 재인용**: `changes()=7`, 잔존 **8행** = **`V11_BRAINS` 7** + **`v12_combo` 1** (`20260502_1330_DB정리_DELETE_보고서.md` 본문).

### 4-7. 1222 캐시 0행 raw 확정 (이전 "40행" 거짓 정정)

`20260502_1330_DB정리_DELETE_보고서.md`·기억24와 동일: **`target_draw_no=1222`** 예측 행 **`COUNT=0`**. (SQL은 1330 보고서 원문 그대로 열람.)

### 4-8. lottery_score 식 raw: 6=100, 5+보너스=50, 5=30, 4=10, 3=3, DB 컬럼 없음 (동적 계산)

`20260502_1500_1군lottery_score_정찰보고서.md` STEP 2-A **docstring bullet 전부**:

```markdown
- 1등(6적중)=**100**  
- 2등(5+보너스)=**50**  
- 3등(5적중)=**30**  
- 4등(4적중)=**10**  
- 5등(3적중)=**3**  
- 그 외=**0**
```

STEP 3-C **원문**:

```markdown
- **`lottery_score` 컬럼으로 `lotto_predictions`에 저장하지 않음**.  
- **`lotto_analysis`** 테이블에 `analysis_type='prediction_feedback'`, `data_json`에 **`json.dumps(feedback)`** (`136:139`) — JSON에도 **per-row lottery_score 필드 없음**(brain_stats는 `avg_match` 등만).
```

### 4-9. fusion 1군 복제 raw: 회차 725/774/1122 Jaccard=1.0 (3건만 확인, 나머지 4건은 독립)

`20260502_1100_3군8뇌_본질구조_정찰보고서.md` STEP 3-F **전체 블록**(잘라내지 않음):

```text
FUSION_RANK1_ROWS 7
DRAW 69 PRED_EQ_DRAW True pred [5, 8, 14, 15, 19, 39] draw [5, 8, 14, 15, 19, 39]
DRAW 101 PRED_EQ_DRAW True pred [1, 3, 17, 32, 35, 45] draw [1, 3, 17, 32, 35, 45]
DRAW 183 PRED_EQ_DRAW True pred [2, 18, 24, 34, 40, 42] draw [2, 18, 24, 34, 40, 42]
DRAW 204 PRED_EQ_DRAW True pred [3, 12, 14, 35, 40, 45] draw [3, 12, 14, 35, 40, 45]
DRAW 725 PRED_EQ_DRAW True pred [6, 7, 19, 21, 41, 43] draw [6, 7, 19, 21, 41, 43]
DRAW 774 PRED_EQ_DRAW True pred [12, 15, 18, 28, 34, 42] draw [12, 15, 18, 28, 34, 42]
DRAW 1122 PRED_EQ_DRAW True pred [3, 6, 21, 30, 34, 35] draw [3, 6, 21, 30, 34, 35]
```

**판정(지시문 4-9 그대로)**: **725·774·1122** 세 회차만 **Jaccard=1.0(1군 복제 양상)** 확인 맥락이 정찰 보고서에 묶여 있음(기억24·1100). **나머지 4건(69·101·183·204)** 은 동일 `PRED_EQ_DRAW True` 블록에 포함되나 **「독립」 분류는 형·동생 합성 라벨**로 유지(추가 바이트 대조 없음).

### 4-10. 1군 6뇌 역할 분담 철학 raw 정찰 0 = 동생 핵심 누락 (다음 작업 B)

합성 입력 9건 중 **「1군 stat/markov/llm/lstm/fusion 각자의 설계 철학·역할 분담」전용 섹션을 단독으로 깐 보고서는 없음** → 본 기억에서 **정찰 0**으로 명시. **다음**: 형 지시 **B 정찰(1군 5뇌 역할 분담, 30분)** → **C 패치 설계**.

---

## 5. 🐉 3군 8뇌 학습 후 실적 (기억24·STATUS 실측 기준, 표만 갱신 타이밍)

| 슬롯 | brain_tag | 가중치(참고) | 1등 | D5+ | 헤지 |
|------|-----------|--------------|-----|-----|------|
| 1 | v12_stat | 2.093 | 0 | 130 | Y |
| 2 | v12_run | 1.306 | 0 | 115 | Y |
| 3 | v12_offset | 1.341 | 0 | 111 | Y |
| 4 | v12_contrarian | 3.352 | 0 | 131 | Y |
| 5 | v12_lstm | 3.639 | 1 | 874 | Y |
| 6 | v12_fusion | 2.867 | 7 | 945 | Y |
| 7 | v12_hyena | 3.189 | 0 | 719 | Y |
| 8 | v12_snake | — | 0 | 453 | **N** |

- **총 1등 8건** = fusion 7 + lstm 1 · **D그룹 1등 2건** = 회차 **69**(fusion) + **983**(lstm)  
- **hyena**: SQL상 **6적중 0건·MAX=5** (섹션 4-2)

---

## 6. 🐍 v12_snake 학습 = 하드코딩 dict (1200·기억24 raw)

- **`_V11_SNAKE_WEIGHTS`**: **contrarian 50.0** · **hyena 30.0** · **stat/markov/lstm/fusion 각 5.0**  
- **`update_v12_weights` / DB snake 가중치 갱신**: **0**

---

## 7. 🧬 fusion·회피·hyena (1100·1300·1545 교차)

- **fusion**: PMF 4키·entropy·cluster·**Jaccard 0.6**·Top-K 루프(1100 STEP 3)  
- **contrarian / snake 회피**: **Jaccard 0.4**(1100 STEP 4-A·4-B 인용 블록)  
- **hyena**: 1·3군 **5005·풀15·tier1·top50** 동형, **입력 태그만 상이**(1545)

---

## 8. 💾 DB DELETE 완료 (1330, 재확인)

- **DELETE 7행**: `army3_*` 6 + `v12_markov` 1  
- **잔존 `lotto_brain_weights_army3` 8행**: `V11_BRAINS` 7 + **`v12_combo` 1**  
- **`lotto_predictions_army3`**: **48615** 유지

---

## 9. 🎯 1222 추첨 (2026-05-02 20:45 KST)

- **16:00 기준**: 추첨까지 약 **4시간 45분** 후(형 시계 기준).  
- **형 결정(유지)**: 1222 **의미 최소화**, **1221까지 학습 정상화** 우선 · **1222 예측 COUNT=0** 유지 시 새 예측은 **학습 결함 패치 후**.

---

## 10. 🚀 다음 작전 (우선순위, 형·기억24와 정렬 + B 신규)

| 순위 | 작전 |
|------|------|
| **B** | **1군 5뇌 역할 분담 정찰(30분)** — 철학·설계 raw, **이번 합성 누락 보충** |
| 1 | 3군 학습 결함 패치 설계(C) — 1430·1500·1545 근거 |
| 2 | 1티어 벤치마킹(Hedge vs EMA/Bayesian) |
| 3 | 패치 → 백테 |
| 4 | 1222 새 예측(학습 정상화 후) |

---

## 11. 🔧 정정 항목 누적

| 정정 | 내용 |
|------|------|
| 유지 | 1222 **캐시 40행 → COUNT=0** |
| 유지 | D그룹 1등 **2건**(69 fusion, 983 lstm) |
| 유지 | `predict_markov.py` SHA256 **FAF2** 구간 표기 |
| **신규** | **31파일 SHA256**을 상태 요약 기본으로 승격(16:00 STATUS 반영) |

---

## 12. 🔄 룰 (R20~R30 요약)

- **R29**: 1·2군 **READ-ONLY** 벤치마킹, **SHA256 변동 0**  
- **R30**: 3군 **자기진화** — 정적 가중치 금지·walk-forward 누적 학습 필수(컨펌 대기 문구는 형 지시 따름)

---

## 13. ❌ 미해결 (갱신)

- **hyena 6적중 0건 인과**: 입력 뇌 차이는 **raw 확정**, **인과는 미증명**(1545).  
- **1군 5뇌 역할 분담 철학**: **정찰 0** → 작업 **B**로 이관.  
- **snake**: dict → 학습 가중치·헤지 편입.  
- **fusion**: 의도–구현(725/774/1122 맥락)·**Top-K append**·**0.6 vs 0.4** 이원화 정리.  
- **v12_combo** 잔존 1행·numpy·group_d 등 기존 대기 **유지**

---

## 14. 🔐 SHA256 · 💾 백업 · 📚 목록 · R4 · 🦅

- **SHA256**: **1군 9 + 2군 6 + 3군 16 = 31/31 PASS** (1430 STEP 0 표·사전=사후 동일).  
- **백업**: `backups/army3_pre_contrarian_20260502_0230/` ⭐ · `backups/army3_pre_step2final_20260501_1730/` · `My_Library/_tmp_archive_20260502/`  
- **최근 기억 (5개)**: 기억25_v1(16:00) ⭐ · 기억24_v1(13:45) · 기억23_v1(11:30) · 기억22_v1(08:30) · 기억21_v1(07:30)  
- **최근 커서보고서**: **1545 hyena** · **1500 lottery_score** · **1430 벤치마킹** · 1330 DELETE · 1300 회피 · 1200 snake · 1100 본질 · 1000 정리 · 0900 시스템정찰  
- **R4 압축 후 첫 멘트**: `동생, /SUMMARY/RULES_FIXED.md 읽고, /SUMMARY/STATUS_LATEST.md 읽어. 확인했어 형이.`  
- **🦅 동생 가이드**: **B-Lite** = 소스 MD 자동 읽기 + **14섹션** 합성 · raw 인용 **중간 절단 금지** · 모르면 **모른다**

---
