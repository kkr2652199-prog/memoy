# 4군 부가 페이지 완성 보고서 (2026-05-09 KST)

## 목적

`localhost:6124`에서 **명예의전당·통계분석·당첨이력·데이터수집·특수뇌니**를 플레이스홀더 없이 구현하고, **두뇌예측**에 가중치 순위 뱃지·나노 전적을 연동.  
**My_Library 미접근** · **`d:\3kweon`만 수정** · `My_Library` 문자열 **0건** 유지.

---

## 신규·변경 파일 (줄 수 대략)

| 파일 | 줄 수(대략) | 비고 |
|------|-------------|------|
| `app/lotto4/v13_routes.py` | ~886 | API 다수 추가 |
| `app/lotto4/army4_draw_sync.py` | ~38 | 신규: 당첨 확정 시 army4 적중 갱신 |
| `app/static/js/lotto4.js` | ~969 | 부가 페이지·필터·차트·수집·탭 뱃지 |
| `app/static/index.html` | ~257 | 섹션 HTML 교체 |
| `app/static/css/lotto4.css` | ~686 | 등수 뱃지·차트·필터·보너스볼 |

---

## API 요약

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/lotto4/v13/hall-of-fame` | 등수·뇌 필터, `records` + `brain_summary` (적중번호·당첨번호·등수 뱃지용 필드) |
| GET | `/api/lotto4/v13/stats/comprehensive-full` | `get_comprehensive_stats()` + 최근 10/30/50회 `trend_*` (`lotto4.db`의 `lotto_draws`) |
| GET | `/api/lotto4/v13/draws` | `page` & `per_page` 페이지네이션 또는 `draw_no` 단건 필터 (기존 `limit` 모드 유지) |
| GET | `/api/lotto4/v13/collect-status` | `get_collection_hint()` — 최신 회차·다음 수집 후보 |
| POST | `/api/lotto4/v13/collect-draws` | `fetch_single_draw` + `save_draw` + `refresh_army4_predictions_for_draw` |
| GET | `/api/lotto4/v13/brain/analysis` | 뇌별 성능·가중치·Jaccard 쌍·다양성(합 평균/표준편차) |
| GET | `/api/lotto4/v13/brain/weight-history` | `lotto_weight_log_army4` |
| GET | `/api/lotto4/v13/brain/nano-summary` | 뇌별 1~5등 건수 (나노 전적) |
| GET | `/api/lotto4/v13/brain/ranks` | 가중치 내림차순 순위 `#1…` |

기존 `/api/lotto4/v13/brain/hall-of-fame` 등은 **그대로 유지**.

---

## 데이터·통계 범위

- **`get_comprehensive_stats`**: `app.lotto.models.get_lotto_db()` → **`d:\3kweon\data\lotto4.db`** (벤더 `lotto` 패키지 기준 경로).
- **수집**: `app.lotto.data_service.fetch_single_draw` / `save_draw` 동일 DB 사용.  
  이후 **`refresh_army4_predictions_for_draw`**로 `lotto_predictions_army4`의 `matched_count` / `bonus_matched` 갱신.
- **수집 실패**: 미추첨 회차·네트워크 시 API 200 + 본문 `ok: false` 가능(정상).

---

## UI 요약

1. **명예의전당**: 등수/뇌 필터 · 뇌별 집계 테이블 · 상세 테이블(등수 색 뱃지·컬러볼·적중번호·당첨번호).
2. **통계분석**: 요약 타일 · 홀짝·번호대·연속·합계 · 전체 번호 막대 차트 · 최근 10/30/50 트렌드 차트 · 원시 JSON(축약).
3. **당첨이력**: 50건 페이지네이션 · 회차 검색(`draw_no`) · 당첨 컬러볼 · 보너스 별도 스타일.
4. **데이터수집**: 상태 표시 · 최신 1회 수집 · 로그.
5. **특수뇌니**: 뇌별 카드(성능·가중치·다양성) · 가중치 로그 테이블 · Jaccard 테이블.
6. **두뇌예측**: 탭에 **`#순위` 뱃지** (`/brain/ranks`) · 제목 옆 **나노 전적** (`/brain/nano-summary`).

---

## 검증 (TestClient)

다음 요청 **200** 확인:  
`hall-of-fame`, `stats/comprehensive-full`, `collect-status`, `draws?page=1&per_page=50`,  
`brain/nano-summary`, `brain/ranks`, `brain/analysis`, `brain/weight-history`, `brain/hall-of-fame`,  
`POST collect-draws` (미추첨 시 본문 `ok: false` 가능).

`grep -r "My_Library" d:\3kweon\` → **0건**.

---

## 제한·메모

- **3군 UI 픽셀 동일**은 My_Library 미참조로 불가 — **기능·메뉴·데이터 항목** 동등 목표.
- **Jaccard·다양성**은 최근 예측 샘플 기반 근사.
- `collect-draws`는 **1회차씩** 확장(전체 백필은 별도 `fetch_all_draws` 미연동).

---

**보고서 경로**: `D:\MONEY lol\My_Drive_Sync\커서보고서\20260509_4군부가페이지완성_보고서.md`
