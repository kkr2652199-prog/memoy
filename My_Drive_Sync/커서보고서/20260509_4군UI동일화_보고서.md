# 4군 UI 동일화 보고서 (2026-05-09 KST)

## 목적

`localhost:6124` 4군 전용 페이지를 **3군급** 다크 테마·사이드바·뇌 탭·컬러볼·신뢰도·적중 표시·카톡·대시보드·부가 화면으로 확장. **My_Library 파일은 읽기·쓰기 없음** — `d:\3kweon` 내부만 수정.

## 변경 파일 요약

| 파일 | 변경 성격 | 대략 줄 수 |
|------|-----------|------------|
| `app/static/css/lotto4.css` | 전면 재작성 (사이드바·탭·볼·모달·테이블) | ~525 |
| `app/static/index.html` | 전면 재작성 (7뷰 + 모달) | ~177 |
| `app/static/js/lotto4.js` | 전면 재작성 (탭·API·대시보드·모달 등) | ~575 |
| `app/lotto4/v13_routes.py` | `v13_snake` method 보강 + API 별칭 3개 | +약 20줄 |
| `run_v13.py` | docstring 문구 (`My_Library` 문자열 제거) | 1줄 |

## STEP 4 — API

기존 + 다음 **별칭** 추가 (지시서 경로 호환):

- `GET /api/lotto4/v13/dashboard` → `dashboard-summary`와 동일
- `GET /api/lotto4/v13/predictions/{draw_no}` → `predictions/draw/{draw_no}`와 동일
- `GET /api/lotto4/v13/tier-wins/{draw_no}` → `predictions/draw/{draw_no}/tier-wins`와 동일

그 외 지시 항목은 이미 존재:

- `GET /brain/status`
- `POST /predict/{target_draw_no}`
- `GET /dashboard-summary`

`api_v13_brain_status`의 `BRAIN_METHOD`에 **`v13_snake`: `"에이전시"`** 추가.

**TestClient 검증** (`app.main_v13:app`):

- `GET /` 200  
- `GET /api/lotto4/v13/brain/status` 200  
- `GET /api/lotto4/v13/dashboard` 200  
- `GET /api/lotto4/v13/dashboard-summary` 200  
- `GET /api/lotto4/v13/tier-wins/1222` 200  
- `GET /api/lotto4/v13/predictions/1222` 200  

## STEP 5 — grep

- `grep -r "My_Library" d:\3kweon\` → **0건** (문서/주석에서 해당 문자열 제거)
- `grep -r "localhost:8123" d:\3kweon\app\` → **0건**

## UI 구현 요약

- **레이아웃**: 좌측 사이드바 + 메인 헤더 + 콘텐츠.
- **메뉴**: 대시보드 · 두뇌예측 · 명예의전당 · 통계분석 · 당첨이력 · 데이터수집(플레이스홀더) · 특수뇌니(플레이스홀더) · 두뇌현재.
- **두뇌예측**: 회차 이전/다음 + 셀렉트, 예측 실행·불러오기, **엘리트 뇌만 표시**(`/brain/elite-tags`), 1~5등 모달(`tier-wins`), 카톡 복사.
- **탭**: 상위 4뇌 / 하위 4뇌; `window.lotto4SwitchBrainTab(tag)`.
- **볼 색**: 1–10 노랑, 11–20 파랑, 21–30 빨강, 31–40 회색, 41–45 초록; 당첨 번호 있으면 **글로우**.
- **카톡**: `window.lotto4CopyKakaoText()` — 지시한 한글 뇌 이름 매핑 + 신뢰도 표기.
- **버그 수정**: 예측 불러오기 시 `data.predictions` 배열 사용 (기존 배열 직통 가정 오류 수정).

## STEP 6 — 한계·메모

- **3군과 픽셀 단위 동일** 재현은 My_Library 정적 파일을 열람하지 않는 조건상 불가; 동급 **패턴·구조**를 `d:\3kweon`에서 재구현.
- **통계분석**: `/stats/comprehensive`는 `app.lotto.data_service`를 호출 — 당첨 raw는 공용 로직이나 **표시 데이터 범위**는 형 확인 권장.
- **데이터수집·특수뇌니**: 문구만 안내하는 플레이스홀더(향후 4군 파이프 연동).

## 보고서 경로

`D:\MONEY lol\My_Drive_Sync\커서보고서\20260509_4군UI동일화_보고서.md`
