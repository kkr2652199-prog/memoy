# 4군 4단계 보고서: 구조예측뇌 (Structure Prediction) — `v13_trend` → `v13_struct`

## 1. SHA256 전·후 비교

| 파일 | 사전 SHA256 (STEP 0) | 사후 SHA256 (STEP 7) | 비고 |
|------|----------------------|----------------------|------|
| `app/lotto4/brains/trend.py` | `82c9ede1c3d07545f1291990d5f75fdae329382c00afdfda00deb6f373957115` | 동일 | 롤백용 보존, 미변경 |
| `app/lotto4/brains/_utils.py` | `930f436f4d56e3e1e1a0db358fbcb2fd0c5c0eeb5488b04f6352763f14d4ac69` | 동일 | 미변경 |
| `app/lotto4/brains/__init__.py` | `d021dfce5f8caac84d1ffdcf4f2f418a2c3ba02b9dc70ff43ea7ec892aee6431` | 동일 | 미변경 |
| `app/lotto4/v13_engine_v2.py` | `74159426e93de832e969ac0d4b8e3ff76b131990775e2d4ce46fb6437e396fef` | `edf43dd8232f513bc95fbf584f388fe474c2a7e844a1bdc9b19372b11b60ed4f` | `v13_struct` 등록 |
| `app/lotto4/brains/struct_brain.py` | (없음) | `6a07a0a33dc1de4a1f7f66abcdaef72db883627a854cf269ca29c989637ebafc` | 신규 |

모델 파일 7개: `d:\3kweon\models\struct_brain_*.json` (XGBoost native JSON)

---

## 2. 구조 변수 7개 · XGBoost 파라미터

| 변수 | 의미 |
|------|------|
| `total_sum` | 6개 번호 합 |
| `odd_count` | 홀수 개수 |
| `high_count` | 23~45 고번호 개수 |
| `ac_value` | AC값 (차이 집합 크기 − 5) |
| `consec_count` | 연번 쌍 개수 |
| `decade_max` | 동일 십의 자리 최대 개수 |
| `tail_variety` | 끝수(일의 자리) 서로 다른 종류 수 |

**XGBoost (회귀, 변수별 독립 모델)**  
`n_estimators=200`, `max_depth=4`, `learning_rate=0.05`, `subsample=0.8`, `colsample_bytree=0.8`, `objective=reg:squarederror`

입력 피처 36차원: 직전 구조 7, MA5·MA20·표준편차(5)·Δ7·회차 정규화 1.  
학습 구간: `draw_no ≥ 101`, `draw_no < target_draw`. 예측 시 **1223회 이후 미공개 당첨은 사용하지 않음** (`load_draws_before` 상한 `min(draw_no, 1223)`).

---

## 3. 초기 학습 결과 (5-fold CV RMSE, `target_draw=1223`)

| 변수 | CV RMSE | 지시 목표 |
|------|---------|-----------|
| total_sum | 32.1400 | < 25 |
| odd_count | 1.2043 | < 1.0 |
| high_count | 1.2305 | < 1.0 |
| ac_value | 1.5715 | < 1.5 |
| consec_count | 0.7812 | < 0.8 (통과) |
| decade_max | 0.6574 | < 0.7 (통과) |
| tail_variety | 0.8258 | < 0.8 |

지정 하이퍼파라미터 고정 상태에서 일부 목표는 미달. 트리 앙상블의 한계·합·AC 분산이 반영됨.

---

## 4. 단위 테스트 (`tools/test_struct_brain_step5.py`)

- **예측 구조 (1224)**: sum=130, odd=3, high=3, ac=8, consec=1, decade=2, tail=5  
- **5세트** (오름차순, 5/7 이상 만족, 세트 간 Jaccard < 0.5):

  1. [5, 12, 21, 24, 33, 34]  
  2. [5, 12, 20, 25, 26, 41]  
  3. [10, 11, 18, 23, 31, 34]  
  4. [2, 7, 20, 25, 36, 37]  
  5. [3, 14, 15, 26, 31, 44]  

- 출력: `[OK] 구조예측뇌 단위 테스트 전부 통과`

---

## 5. 미니 백테스트 (1200–1222, `python tools/mini_backtest_v13.py`)

`v13_struct`는 회차마다 `update_models` 포함. 115세트 기준 평균 적중 수:

| 뇌 | 이전 avg (지시서 참고) | 교체 후 avg (본 실행) | 변화 |
|----|------------------------|----------------------|------|
| v13_struct | 0.730 (trend) | **0.861** | +0.131 |
| v13_ensemble | 0.791 | **0.817** | +0.026 |
| v13_seq | 0.922 | **0.852** | −0.070 |
| v13_cdm | 0.730 | **0.730** | 0.000 |
| v13_cond_prob | 0.739 | **0.739** | 0.000 |
| v13_anti_popular | 0.861 | **0.861** | 0.000 |
| v13_rl | 0.800 | **0.800** | 0.000 |
| v13_contrarian_v2 | 0.774 | **0.774** | 0.000 |
| v13_gen | 0.757 | **0.757** | 0.000 |

**참고**: `v13_seq`는 지시서 표(0.922)와 본 실행(0.852)이 다릅니다. `seq_brain` 단독으로 동일 DB·동일 구간만 돌렸을 때 평균 **0.870**이었고, `mini_backtest_v13.py`에서는 회차당 `ensemble`이 하위 뇌를 먼저 호출한 뒤 같은 프로세스에서 `seq`가 다시 호출되는 구조라 **Torch/CUDA 비결정성·호출 순서**에 따라 스크립트 일체 실행 결과와 단독 실행 결과가 어긋날 수 있습니다. `v13_seq` 등 소스는 이번 패치에서 변경하지 않았습니다.

---

## 6. trend 대비 변화 분석

- **태그·모듈**: `v13_trend` / `trend.py` 예측 → `v13_struct` / `struct_brain.py` (트렌드 파일은 유지).
- **백테스트**: 본 실행에서 `v13_struct` 평균 적중은 trend 대비 상승(0.730 → 0.861). 다만 표본·노이즈에 민감하므로 장기적으로 재검증 필요.
- **앙상블**: 하위 뇌 중 하나가 구조예측으로 바뀌었으므로 `v13_ensemble` 분포 변화는 정상.

---

## 7. 1~3군 간섭 여부

- `app/lotto/`, `app/lotto2/`, `app/lotto3/` **미수정** (0건).

---

## 8. 다음 단계 예고

**5단계**: 서브 4뇌 일괄 구현 (지시서 예고).

---

## 부록: 변경 파일 요약

- 신규: `app/lotto4/brains/struct_brain.py`, `tools/test_struct_brain_step5.py`
- 수정: `v13_engine_v2.py`, `v13_weights_v2.py`, `ensemble.py`, `app/static/js/lotto4.js`, `app/static/index.html`, `tools/mini_backtest_v13.py`, `tools/update_weights_v13.py`, `tools/backtest_to_db_v13.py`, `tools/full_backtest_v13.py`, `tools/simulate_hedge_ensemble_v13.py`, `requirements.txt` (`xgboost`)
- DB: `lotto_brain_weights_army4.brain_tag` `v13_trend` → `v13_struct` (및 `INSERT OR IGNORE`)

Google Drive 공유 링크는 웹에서 직접 생성해야 합니다. 동기화 복사본 경로: `D:\MONEY lol\My_Drive_Sync\커서보고서\20260516_4군_구조예측뇌_4단계_보고서.md`.
