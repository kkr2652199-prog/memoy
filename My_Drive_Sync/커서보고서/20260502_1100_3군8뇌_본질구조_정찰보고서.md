# 3군 8뇌 본질 구조 정밀 정찰 보고서 (READ-ONLY)

- 작성: 2026-05-02 11:00 KST
- 선행: `20260502_0900_3군8뇌_시스템정찰_보고서.md`, `20260502_1000_시스템정리_보고서.md`
- 제약: `app/lotto/`, `app/lotto2/` **미접근**, 코드·DB **쓰기 0**, `target_draw_no = 1222` **미접근**(SQL은 `target_draw_no <= 1221` 등가 사용)

---

## STEP 0 — SHA256 사전 (`app/lotto3` 16파일)

- 정리보고서 STEP 0-B baseline과 **전 파일 일치** → **16/16 OK** (중단 없음).
- 사후: STEP 9에서 **동일 16/16 재확인**.

---

## STEP 1 — 의심1: `V11_BRAINS`에 `v12_snake` 누락?

### 1-A. `V11_BRAINS` 정의

```19:27:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
V11_BRAINS = (
    "v12_stat",
    "v12_run",
    "v12_offset",
    "v12_contrarian",
    "v12_lstm",
    "v12_fusion",
    "v12_hyena",
)
```

### 1-B. `update_v12_weights` 전체 (요약: `brain_tag IN (V11_BRAINS)` 집계 후 UPDATE)

```146:193:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
def update_v12_weights(target_draw_no: int, last_n: int = 50) -> None:
    """V11 가중치 진화 (Hedge η=2.0, V9의 η=1.5보다 강함).

    new_weight = base_weight × exp(η × avg_match / 6)
    """
    import math

    conn = _connect()
    try:
        placeholders = ",".join("?" * len(V11_BRAINS))
        rows = conn.execute(
            f"""
            SELECT brain_tag, AVG(matched_count) AS avg_m,
                   COUNT(1) AS n, SUM(matched_count) AS sum_m
            FROM lotto_predictions_army3
            WHERE target_draw_no <= ? AND target_draw_no > ?
              AND matched_count >= 0
              AND brain_tag IN ({placeholders})
            GROUP BY brain_tag
            """,
            (target_draw_no, target_draw_no - last_n, *V11_BRAINS),
        ).fetchall()

        for r in rows:
            tag = str(r["brain_tag"])
            base = float(V11_SEED_WEIGHTS.get(tag, 1.0))
            avg_m = float(r["avg_m"] or 0.0)
            new_w = base * math.exp(V11_HEDGE_ETA * avg_m / 6.0)
            conn.execute(
                """
                UPDATE lotto_brain_weights_army3
                SET current_weight = ?, recent_avg_match = ?,
                    total_predictions = ?, total_matches = ?,
                    last_updated_draw = ?, updated_at = datetime('now','localtime')
                WHERE brain_tag = ?
                """,
                (
                    new_w,
                    avg_m,
                    int(r["n"] or 0),
                    int(r["sum_m"] or 0),
                    int(target_draw_no),
                    tag,
                ),
            )
        conn.commit()
    finally:
        conn.close()
```

### 1-C. `V11_SEED_WEIGHTS`

```30:38:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
V11_SEED_WEIGHTS: dict[str, float] = {
    "v12_stat": 1.5,
    "v12_run": 1.0,
    "v12_offset": 1.0,
    "v12_contrarian": 2.5,
    "v12_lstm": 2.0,
    "v12_fusion": 2.0,
    "v12_hyena": 2.0,
}
```

(`v12_snake` 키 **없음**.)

### 1-D. `v12_engine.py`에서 `update_v12_weights` 호출

```17:21:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
from app.lotto3.v12_models import (
    get_v12_training_draws,
    init_v12_seeds,
    update_v12_weights,
    get_v12_brain_weights,
)
```

```360:360:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
        update_v12_weights(target_draw_no)
```

### 1-E. `v12_snake.py` — `update_v12_weights` / `update_brain`

- `rg "update_v12_weights|update_brain" app/lotto3/v12_snake.py` → **매칭 없음**.

### Raw 질문 답

| 질문 | 답 |
|------|-----|
| `v12_snake`가 `V11_BRAINS`에 있는가? | **N** — 튜플에 없음 (`v12_models.py` 19–27행). |
| 없다면 누가 갱신? | 위 `update_v12_weights`는 **`V11_BRAINS`만** 갱신하므로, **이 함수로는 `v12_snake` 미갱신**. |
| `v12_snake.py`에 별도 학습? | **코드상 없음**(1-E). |
| DB `v12_snake` 행? | `SELECT COUNT(*) FROM lotto_brain_weights_army3 WHERE brain_tag='v12_snake';` → **`0`**. |

### 결론 (의심1)

- **`update_v12_weights` 기준 “헤지 학습”은 `v12_snake`에 적용되지 않음** — raw: `V11_BRAINS`·`V11_SEED_WEIGHTS`·`update_v12_weights` SQL·DB COUNT.

---

## STEP 2 — 의심2: `v12_markov` / `v12_combo` 숨은 뇌?

### 2-A. `_v12_load_brain_pmfs` (PMF 4키)

```121:149:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py
def _v12_load_brain_pmfs(training_draws: list[dict]) -> dict[str, dict[int, float]]:
    """4뇌 PMF 로드 (combo가 llm 자리)."""
    pmfs: dict[str, dict[int, float]] = {}

    try:
        from app.lotto3.predict_stat import army3_stat_prob_vector
        pmfs["v12_stat"] = army3_stat_prob_vector(training_draws)
    except Exception:  # noqa: BLE001
        pmfs["v12_stat"] = {n: 1.0 / 45.0 for n in range(1, 46)}

    try:
        from app.lotto3.predict_markov import army3_markov_prob_vector
        pmfs["v12_markov"] = army3_markov_prob_vector(training_draws)
    except Exception:  # noqa: BLE001
        pmfs["v12_markov"] = {n: 1.0 / 45.0 for n in range(1, 46)}

    try:
        from app.lotto3.predict_combo import army3_combo_prob_vector
        pmfs["v12_combo"] = army3_combo_prob_vector(training_draws)
    except Exception:  # noqa: BLE001
        pmfs["v12_combo"] = {n: 1.0 / 45.0 for n in range(1, 46)}

    try:
        from app.lotto3.predict_lstm import army3_lstm_prob_vector
        pmfs["v12_lstm"] = army3_lstm_prob_vector(training_draws)
    except Exception:  # noqa: BLE001
        pmfs["v12_lstm"] = {n: 1.0 / 45.0 for n in range(1, 46)}

    return pmfs
```

상단 튜플:

```24:29:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py
_V11_FUSION_BRAINS: tuple[str, ...] = (
    "v12_stat",
    "v12_markov",
    "v12_combo",
    "v12_lstm",
)
```

### 2-B. `predict_combo.py`

- **총 86줄**(파일 읽기 기준).
- 핵심: `army3_combo_predict` 39–71행, `army3_combo_prob_vector` 74–85행.

### 2-C. `predict_markov.py`

- **총 19줄**.
- 핵심: `army3_markov_predict` 5–12행, `army3_markov_prob_vector` 15–18행(1군 `get_markov_prob_vector` 호출 — **1군 모듈 본문은 미접근**).

### 2-D. `v12_engine.py` — `rg "combo|markov"`

- **매칭 없음** — 엔진 본문에서 `combo`/`markov` 문자열 **미사용**.

### 2-E. `predict_combo.py` fusion 외 사용처 (`app/lotto3`)

- `rg "predict_combo|army3_combo_prob"` → **`v12_fusion_v5.py`**, **`v12_fusion.py`** 만.

### Raw 질문 답

| 질문 | 답 |
|------|-----|
| combo/markov가 fusion PMF 4입력 중 몇 개? | **2/4** — `_v12_load_brain_pmfs`에 `v12_markov`, `v12_combo` 키 존재(위 인용). |
| combo/markov 없으면 fusion 동작? | **모름**(해당 함수를 “제거한 빌드”는 본 세션에서 실행·빌드 안 함). 현 코드는 **항상 4키 로드 시도**; 예외 시 균일 PMF 대입(`v12_fusion_v5.py` 128–147행). |
| 8뇌 슬롯 + 숨은 2뇌 = 10뇌? | **부분 Y**: 저장 태그 8종(`v12_stat`…`v12_snake`)은 엔진 파이프라인 raw와 정찰 DB 일치 전제; fusion 내부에 **PMF 전용 2키(`v12_markov`,`v12_combo`)** 존재 — “10뇌”는 **명칭 정의에 따라 Y/N**이 갈리므로 **구조 설명만 확정**, 단일 숫자 라벨은 **모름**. |

### 결론 (의심2)

- **markov/combo는 엔진 직접 예측 세트가 아니라 fusion PMF 입력** — `v12_engine`에 호출 없음, `_v12_load_brain_pmfs`에만 존재.

---

## STEP 3 — 의심3: fusion이 1군 복제본?

### 3-A. `v12_fusion.py` 핵심 (`v12_fusion_predict`)

```31:83:d:\MONEY lol\My_Library\app\lotto3\v12_fusion.py
def v12_fusion_predict(training_draws: list[dict], n_sets: int = 5) -> list[dict]:
    """V11 fusion: 4뇌 PMF → 가중 합성 → entropy clip → Top-K Greedy 샘플링."""
    if not training_draws:
        return []

    weights = get_v12_brain_weights()
    pmfs = {
        "v12_stat": army3_stat_prob_vector(training_draws),
        "v12_markov": army3_markov_prob_vector(training_draws),
        "v12_combo": army3_combo_prob_vector(training_draws),
        "v12_lstm": army3_lstm_prob_vector(training_draws),
    }
    ...
    fused = _entropy_clip(fused, target_entropy=3.5)
    ...
    while len(sets) < n_sets and attempts < 200:
        ...
        cand = sorted(random.choices(nums_list, weights=w_list, k=6))
```

### 3-B. `v12_fusion_v5.py` V12-D (요지)

- `_FUSION_JACCARD_THRESHOLD = 0.6` (`v12_fusion_v5.py` 33행).
- `_v12_is_diff_from_army1_fusion` — Jaccard ≥ 0.6이면 탈락 (`57:72:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py`).
- `v12_fusion_v5_predict`: PMF 결합 → entropy×cluster → Top-K 1세트 + 가중 random 4 + bypass (`235:339` 인용 생략 가능하나 보고서 본문에 단계 명시됨).

### 3-C / 3-D — 1군 `fusion.py` 시그니처·PMF 동일성

- **`app/lotto/` 미접근 → 비교 불가 → 모름.**

### 3-E. V12-D 차별화 raw (이미 3-B)

- **Jaccard 임계 0.6**, Top-K Greedy 1 + weighted random 4, bypass `bypass_attempts > 30` 시 회피 생략 가능(`329:330`).

### 3-F / 3-G — DB (`target_draw_no <= 1221`)

**`v12_fusion`, `matched_count = 6` 행 7건** — 각 행의 6수와 `lotto_draws` 동일 회차 당첨 6수 비교(READ-ONLY 스크립트 출력 raw):

```text
FUSION_RANK1_ROWS 7
DRAW 69 PRED_EQ_DRAW True pred [5, 8, 14, 15, 19, 39] draw [5, 8, 14, 15, 19, 39]
DRAW 101 PRED_EQ_DRAW True pred [1, 3, 17, 32, 35, 45] draw [1, 3, 17, 32, 35, 45]
DRAW 183 PRED_EQ_DRAW True pred [2, 18, 24, 34, 40, 42] draw [2, 18, 24, 34, 40, 42]
DRAW 204 PRED_EQ_DRAW True pred [3, 12, 14, 35, 40, 45] draw [3, 12, 14, 35, 40, 45]
DRAW 725 PRED_EQ_DRAW True pred [6, 7, 19, 21, 41, 43] draw [6, 7, 19, 21, 41, 43]
DRAW 774 PRED_EQ_DRAW True pred [12, 15, 18, 28, 34, 42] draw [12, 15, 18, 28, 34, 42]
DRAW 1122 PRED_EQ_DRAW True pred [3, 6, 21, 30, 34, 35] draw [3, 6, 21, 30, 34, 35]
```

### 결론 (의심3)

- **1군 바이트 단위 “복제” 여부: 모름**(1군 코드 미읽기).
- **3군 현행 경로(`v12_engine` → `v12_fusion_v5`)는 V12-D·entropy·cluster·회피 단계가 존재** — “동일 함수 복붙만”으로 단정할 수 없음(raw: `v12_fusion_v5.py`).
- **DB 7건은 예측 6수 = `lotto_draws` 당첨 6수** — 위 raw.

---

## STEP 4 — 의심4: 회피 로직 = 1등 0건 설계 결함?

### 4-A. `predict_contrarian.py` Jaccard 회피

```182:194:d:\MONEY lol\My_Library\app\lotto3\predict_contrarian.py
def _is_diff_from_armies(combo: list[int], army_sets: list[set[int]], threshold: float = 0.4) -> bool:
    if not army_sets:
        return True
    s = set(combo)
    for a in army_sets:
        inter = len(s & a)
        uni = len(s | a)
        jac = inter / uni if uni else 0.0
        if jac >= threshold:
            return False
    return True
```

샘플 루프에서 `if not _is_diff_from_armies(cand, army_sets, 0.4): continue` (`264:267` 근처 — 본 파일 266행).

### 4-B. `v12_snake.py` `_is_diff_from_army1`

```37:55:d:\MONEY lol\My_Library\app\lotto3\v12_snake.py
def _is_diff_from_army1(
    combo: list[int],
    army1_sets: list[set[int]],
    threshold: float = 0.4,
) -> bool:
    """1군 셋트 중 단 하나라도 Jaccard >= threshold 면 탈락.
    ...
    """
    if not army1_sets:
        return True
    s = set(combo)
    for a1 in army1_sets:
        inter = len(s & a1)
        uni = len(s | a1)
        jac = inter / uni if uni else 0.0
        if jac >= threshold:
            return False
    return True
```

### 4-C. `v12_engine.py` `_v12_evaluate_combinations`

```97:109:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
def _v12_evaluate_combinations(
    pool_nums: list[int],
    consensus: dict[int, float],
) -> list[tuple[tuple[int, ...], float]]:
    """15C6=5005 전수, tier1 없음 (1군 _evaluate_combinations)."""
    from itertools import combinations

    out: list[tuple[tuple[int, ...], float]] = []
    for combo in combinations(pool_nums, 6):
        t = float(sum(consensus.get(n, 0.0) for n in combo))
        out.append((tuple(sorted(combo)), t))
    out.sort(key=lambda x: -x[1])
    return out
```

(상위 50 가중 샘플은 `_v12_build_sets_from_ranked` 쪽 — 정찰 시 인용됨.)

### 4-D. `predict_run.py` / `predict_offset.py` 점수식

- **run**: `scores[i] = _ALPHA_RUN * rfn + _BETA_OFFSET * ofn` (`104:108`), `_ALPHA_RUN=0.6`, `_BETA_OFFSET=0.4` (`9:11`).
- **offset**: `scores[i] = _GAMMA_A * reg_a + _GAMMA_B * stab_b + _GAMMA_C * tight_c + _DELTA_D * ac_comp + _DELTA_SKEW * sk_n` (`129:145`).

### 4-E. `predict_stat.py`

```16:19:d:\MONEY lol\My_Library\app\lotto3\predict_stat.py
def army3_stat_prob_vector(miss_draws: list[dict]) -> dict[int, float]:
    if not miss_draws:
        return {n: 1.0 / 45 for n in range(1, 46)}
    return get_statistical_prob_vector(miss_draws)
```

- **3군 파일만 보면** `get_statistical_prob_vector`는 **1군 모듈** 호출 — **내부 구현·가중치 차별은 `app/lotto/` 미접근으로 모름.**

### 4-F / 4-G — SQL·시뮬

- `SELECT COUNT(*) ... brain_tag='v12_contrarian' AND matched_count=6 AND target_draw_no<=1221` → **`0`**.
- “회피 안 했으면 1등이었을 회차” **counterfactual 미실행 → 모름.**

### 결론 (의심4)

- **회피는 코드상 특정 조합을 제거할 수 있음** — raw: `_is_diff_from_armies` / `_is_diff_from_army1`.
- **그것이 1등 0건의 “직접 원인”인지: 모름**(4-G 미수행, 인과 단정 금지).

---

## STEP 5 — 의심5: D그룹 1208회 1등 0건?

### 5-A. A/B/C/D 분류 raw

- `predict_contrarian.py` 모듈 독스트링: `D draws: frozen D1+D2+D3 set from recon report (n=1208).` (`1:5`행).
- **`D_GROUP_DRAW_NOS`**: `frozenset({ ... })` (`56:88`행).
- **파싱 결과(숫자 집합 크기)**: `D_GROUP_COUNT 1208`, `D_MIN 1`, `D_MAX 1221`, `NON_D_COUNT 13`(즉 `1..1221` 중 D에 **속하지 않은** 회차 수 13).
- **A=13, B=7, C=4 라벨의 정의**: `app/lotto3/` 내 `rg`로 **해당 문자열 정의 미발견 → 모름.**

### 5-B. D그룹 내 `matched_count=6` (`target_draw_no <= 1221`)

```text
('v12_fusion', 1)
('v12_lstm', 1)
```

(행 단위 COUNT; `v12_fusion` 1건·`v12_lstm` 1건.)

### 5-C. D그룹 `matched_count>=3` distinct `target_draw_no` 수

```text
v12_stat 129
v12_run 115
v12_offset 110
v12_contrarian 130
v12_lstm 861
v12_fusion 932
v12_hyena 707
v12_snake 444
```

### 5-D. 회차 69, 983 (`target_draw_no <= 1221`, `matched_count=6`)

```text
DRAW 69 RANK1 [('v12_fusion', 6)]
DRAW 983 RANK1 [('v12_lstm', 6)]
```

- `69 in D_GROUP` → **True**; `983 in D_GROUP` → **True**(스크립트 raw).

### 결론 (의심5)

- **“D그룹 1208회 중 1등 0건”은 본 SQL raw와 모순** — D 내 `matched_count=6`이 **최소 2행**(fusion 1, lstm 1) 존재.
- **D그룹 정의 크기 1208**은 `D_GROUP_DRAW_NOS` 파싱과 일치.

---

## STEP 6 — 종합 raw 표

| 의심 | raw 근거 | 판정 |
|------|----------|------|
| 1) snake 헤지 밖 | `v12_models.py` 19–27행 `V11_BRAINS`; `update_v12_weights` 156–166행 `IN (V11_BRAINS)`; `v12_snake.py` rg 무일치; SQL `v12_snake` weight **0행** | **Y** (snake는 해당 헤지 집계 밖) |
| 2) markov/combo 숨은 뇌 | `v12_fusion_v5.py` 121–149행 PMF 4키; `v12_engine.py` combo/markov **무매칭** | **Y** (fusion PMF 입력으로 존재, 엔진 직접 뇌 아님) |
| 3) fusion 1군 복제 | 1군 파일 **미접근**; `v12_fusion_v5.py` V12-D·cluster·entropy; DB 7건 `PRED_EQ_DRAW True` | **1군 대비: 모름** / **완전 동일 복제: N**(차별 단계 raw 존재) |
| 4) 회피=1등 0 원인 | `predict_contrarian.py` 182–194, 266행; contrarian rank1 **COUNT=0**; counterfactual **미실행** | **회피 존재: Y** / **직접 원인: 모름** |
| 5) D그룹 사냥 미달 | D `1208`개; D内 rank1 SQL 위 **2행**; 69·983 raw | **“D에서 1등 완전 0”: N** |

---

## STEP 7 — 결론 (의심별)

| 의심 | raw 1줄 | 판정 | 1222 영향 | 패치 |
|------|---------|------|-----------|------|
| 1 | `V11_BRAINS`에 snake 없음·DB weight 0행 | 확정 | 없음(1222 미접근) | **Y**(헤지 대상 설계 검토) |
| 2 | `_v12_load_brain_pmfs`에 markov/combo 키 | 확정 | 없음 | **N**(정보 구조 설명) |
| 3 | 1군 미읽기·V5 차별·DB 7건 일치 | 부정+모름 혼합 | 없음 | **모름**(1군 diff 필요) |
| 4 | Jacc≠0 필터 존재·인과 미증명 | 부정(직접원인 단정) | 없음 | **모름**(시뮬 후) |
| 5 | D内 rank1 2 brain 존재 | 확정 | 없음 | **N**(지표 정의 재정렬) |

---

## STEP 8 — 보고서 저장

- `D:/MONEY lol/My_Drive_Sync/커서보고서/20260502_1100_3군8뇌_본질구조_정찰보고서.md`

---

## STEP 9 — SHA256 사후

- **`app/lotto3` 16/16** 정리보고서 baseline과 **불변**(코드 수정 0).

---

## 부록 — 추가 SQL / rg

### `rg "combo|markov" app/lotto3/v12_engine.py`

- (출력 없음)

### `rg "predict_combo|army3_combo_prob" app/lotto3/`

- `v12_fusion_v5.py:138-139`
- `v12_fusion.py:15-16,40-41,94-96`

### `rg "update_v12_weights|update_brain" app/lotto3/v12_snake.py`

- (출력 없음)

---

## 완료 멘트 (5줄)

1. 3군 8뇌 본질 구조 정찰 완료, 보고서 저장됨.  
2. SHA256 사전·사후 **16/16 PASS**.  
3. 5개 의심 판정: **확정 3** / **부정 2**(단, 의심3·4는 **“모름” 항목 병기**) / **모름 2**(1군 diff, 회피 인과).  
4. 가장 뚜렷한 raw: **`v12_snake`는 `V11_BRAINS`·`V11_SEED_WEIGHTS`·`lotto_brain_weights_army3`에 없어 `update_v12_weights` 헤지 루프 밖**이다.  
5. **1222**: 본 세션 **미접근**으로 영향 판정 **없음**; 즉시 패치는 **snake 헤지 정책만 “검토 권고”**(코드 변경 없음).
