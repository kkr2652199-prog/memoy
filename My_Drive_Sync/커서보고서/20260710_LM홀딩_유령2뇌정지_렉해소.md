# LM Studio 홀딩 + 유령 2뇌 정지 — 렉 해소

- **일자**: 2026-07-11 KST  
- **목적**: PC 렉 3대 원인(로컬 LLM·miss_analysis·snake) 정지, llm 포지션 통계 대체  
- **원칙**: 파일 삭제 없음 · 플래그로만 비활성(되돌리기 가능)

---

## STEP A — LM Studio 홀딩

| 항목 | 내용 |
|------|------|
| 플래그 | `predict_llm_client.py` **`LOTTO_LLM_HOLD = True`** |
| 가용성 | `is_lotto_llm_available_sync()` — 홀딩 시 **즉시 False** (3초 probe만, 120초 LLM timeout 미진입) |
| 대체 | `_llm_predict` 앞단에서 통계 대체 → `source=stat_hold_replacement` |
| brain_tag | **`llm` 유지** (engine.py — fallback이어도 `llm` 태그) |

**되돌리기**: `LOTTO_LLM_HOLD = False` 로 변경.

---

## STEP B — 유령 2뇌 정지

| 항목 | 내용 |
|------|------|
| 플래그 | `engine.py` **`ENABLE_SPECIAL_BRAINS = False`** |
| 영향 | `miss_should_run` / `snake_should_run` → **False** (신규 INSERT 중단) |
| DB | 기존 miss/snake 행 **미변경** (READ 유지) |

**되돌리기**: `ENABLE_SPECIAL_BRAINS = True`.

---

## STEP C — 검증 (READ-ONLY + in-memory 1회차)

**명령:**
```powershell
cd "d:\MONEY lol\My_Library"
python -c "from app.main import app; print('app OK')"
python tools/_temp_verify_lag_fix.py
```

### raw 출력 (2026-07-11)

```
=== AFTER PATCH ===
LOTTO_LLM_HOLD=True
is_lotto_llm_available_sync=False
ENABLE_SPECIAL_BRAINS=False
TARGET=1232
elapsed_sec=1.18
lstm_get_lstm_prob_vector_calls=0
llm_sources=['stat_hold_replacement', ... x5]
hyena_sets=5
lead1_before_db=[(21,24,27,32,34,45), (1,6,10,17,29,33), ...]
lead1_recompute=[(21,24,27,32,34,45), (1,6,10,17,29,33), ...]
lead1_unchanged=True
POOL_BRAINS=('stat', 'markov', 'llm', 'lstm', 'fusion') (miss/snake 미포함)
```

**교차 검증 (engine 패치 카운터):**
```
LOTTO_LLM_HOLD True
ENABLE_SPECIAL_BRAINS False
elapsed_sec 1.32
lstm_calls 1
```

### before / after 비교표

| 지표 | BEFORE (코드·추정) | AFTER (실측) |
|------|-------------------|-------------|
| LLM 호출 | LM Studio 120s timeout 대기 가능 | **0회** (즉시 stat 대체) |
| lstm `get_lstm_prob_vector` | **≈5+회/회차** (lstm1 + fusion1 + miss≈3 + snake경로) | **1회** |
| miss/snake 실행 | **매 회차 True** | **False** |
| 1회차 in-memory 소요 | 수십~120초+ (LLM+다중 LSTM) | **1.18~1.32초** |
| lead1 @1232 5세트 | — | **변경 없음 (True)** |

### lead1 동일성 — 코드 근거

`predict_brain7.py` L25-26:
```python
POOL_BRAINS = ("stat", "markov", "llm", "lstm", "fusion")
```
→ **miss_analysis·snake는 lead1 풀 미포함** → 정지해도 lead1 입력 불변 → DB lead1 @1232 재계산 **5세트 일치 확인**.

---

## 변경 파일

| 파일 | 행 | 변경 |
|------|-----|------|
| `app/lotto/predict_llm_client.py` | L9-11, L50-78 | `LOTTO_LLM_HOLD`, `is_lotto_llm_available_sync()` |
| `app/lotto/predict_llm.py` | L16-35, L162-167 | 앞단 가용성 체크 + `_stat_replacement()` |
| `app/lotto/engine.py` | L37-38, L317-325, L382-401 | `ENABLE_SPECIAL_BRAINS`, llm tag 유지 |

---

## 검증

```
python -c "from app.main import app; print('app OK')"
→ app OK
```

---

## 되돌리기 요약

1. `LOTTO_LLM_HOLD = False` → LM Studio 재활성  
2. `ENABLE_SPECIAL_BRAINS = True` → miss/snake 재활성
