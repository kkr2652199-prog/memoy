# 4군 백테스트 기록 DB 저장 보고서

**날짜**: 2026-05-13  
**작업 루트**: `d:\3kweon`  
**DB**: `d:\3kweon\data\lotto4.db`  
**제약**: `My_Library` — `d:\3kweon\app\` 기준 **0건**

---

## 파트 A: 세이브

- `D:/MONEY lol/My_Drive_Sync/동생기억/20260513_기억55_v1.md` 작성 (8뇌·백테스트·가중치·1224·남은 작업).
- `D:/MONEY lol/My_Drive_Sync/SUMMARY/STATUS_LATEST.md` 덮어쓰기 (기억55 한 줄).

---

## 파트 B: TASK 1 — `tools/backtest_to_db_v13.py`

### 지시서 대비 구현 보정

- **스키마**: `lotto_predictions_army4`는 `init_lotto4_db` 정의와 동일하게  
  `target_draw_no`, `method`, `num1~num6`, `confidence`, `reasoning`, `matched_count`, `bonus_matched`, `brain_tag` 를 사용 (JSON `numbers` 컬럼 없음).
- **당첨 조회**: `lotto_draws`의 **`num1~num6`, `bonus`** — 지시서의 `n1` 등 오탈자 수정.
- **삭제 범위**: `target_draw_no BETWEEN 100 AND 1222` 이고 **`brain_tag LIKE 'v13_%'`** 만 삭제 → **1224 등 실전 구간 v13 행 보존**.
- **보너스**: 5개 적중 시 `bonus` 번호 포함 여부로 `bonus_matched` 0/1.
- **method / reasoning**: `V13_BRAIN_METHOD` 및 `백테스트 {tag} 세트{n}`.

### 실행 결과 (원문)

```
백테스트 DB 저장: 100~1222 (1123회차)
삭제: target_draw 100~1222, v13_% → 80행
  ... 100/1123 회차 (56s)
  ... 200/1123 회차 (118s)
  ... 300/1123 회차 (176s)
  ... 400/1123 회차 (234s)
  ... 500/1123 회차 (294s)
  ... 600/1123 회차 (355s)
  ... 700/1123 회차 (418s)
  ... 800/1123 회차 (493s)
  ... 900/1123 회차 (565s)
  ... 1000/1123 회차 (635s)
  ... 1100/1123 회차 (704s)

완료: INSERT 44920행 (벽시계 721.2s / 12.0분)
검증(구간): 44920행, 1123회차, 8뇌
1224 실전 보존: v13 행 40개

적중 분포 (matched_count):
  5개: 1행
  4개: 52행
  3개: 991행
  2개: 6930행
  1개: 19080행
  0개: 17866행
```

---

## TASK 2: 검증 결과

| 항목 | 기대 | 결과 |
|------|------|------|
| 총 행 수 | ~44,920 | **44,920** (1123×8×5) |
| 회차 수 | 1123 | **1123** |
| 뇌 수 | 8 | **8** |
| `matched_count` 분포 | 0~5 | **0~5**, 5개 **1행**, 4개 **52행** |
| 1224 실전 보존 | 40행 존재 | **40개** |
| `grep My_Library` `app\` | 0건 | **0건** |

### 대시보드 / 명예의 전당

- `matched_count`가 **0 이상**으로 채워졌으므로, `WHERE matched_count >= 0` / `>= 3` 조건을 쓰는 API·페이지에서 통계·명예 데이터가 나올 수 있음.
- 실제 UI는 브라우저에서 `http://localhost:6124` 로 확인 (이번 작업에서는 서버 미기동).

---

## 소요 시간

| 단계 | 시간 |
|------|------|
| 백테스트 DB 적재 (1123×8뇌×5세트) | **약 721s (~12.0분)** |

---

## 후속 제안

1. **증분 적재**: 구간을 나누어 재실행하거나, 이미 있는 `target_draw_no`+`brain_tag`+동일 번호는 스킵하는 upsert 전략.
2. **디스크**: 4만여 행 INSERT 후 정기 `VACUUM` 여부는 DB 정책에 따름.
3. **실전 예측**: 1225 이후는 `run_prediction_v13` 경로로 적재 시 `matched_count=-1` 유지 → 추첨 후 `_score_predictions`로 갱신.
