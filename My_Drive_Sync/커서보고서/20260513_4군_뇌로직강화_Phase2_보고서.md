# 4군 뇌 로직 강화 Phase 2 보고서

**날짜**: 2026-05-13  
**대상**: `d:\3kweon`  
**DB**: `d:\3kweon\data\lotto4.db`  
**제한**: `My_Library` 미사용 — `d:\3kweon\app` grep **0건**

---

## PART A — 세이브

| 파일 | 상태 |
|------|------|
| `동생기억/20260513_기억58_v1.md` | 작성 (Phase2 결과 반영) |
| `SUMMARY/STATUS_LATEST.md` | 덮어쓰기 |
| `SUMMARY/RULES_FIXED.md` | **R26** 1줄 추가 (R25와 동일 취지, 워크플로 헤더 R1~R26) |

---

## PART B — 코드 변경 요약 (최종 저장소 기준)

### 유지됨
- **`app/lotto4/brains/_utils.py`**: `interarrival_gap_mean_std`, `z_scores_current_gap`, `predict_sum_range_adaptive` 추가 (재시도·타 뇌용).
- **`trend.py`**: `predict_sum_range_adaptive(...)`로 `generate_sets_with_filters`의 `sum_range` 동적화 (최근 50회 합·MA10·1σ).
- **`generator.py`**: 동일 합계 구간 + `NOISE_SCALE`을 구간 폭 대비 `ref_w=75`로 스케일(0.35~1.25 클램프), `_validate`에 동적 `sum_lo/hi` 반영.

### 시도 후 롤백 (미니백 악화: −0.01 초과)
- **`graph.py`**: 후보 풀 대비 `adj` 기반 조건부 블렌드(0.7/0.3) → avg **0.817→0.783**. **롤백**. (초기 누락으로 `CLUSTER_ATTEMPT_MULT` 상수도 잠시 빠졌다가 복구.)
- **`bayesian.py`**: `lotto_cooccur_4` 기반 상위20 쌍 빈도 보정 → **0.930→0.870**. **롤백**.
- **`contrarian_v2.py`**: z-score overdue 보너스 → **0.817→0.800**. **롤백**.
- **`rl_agent.py`**: z 상태 접미·overdue 보상 → **0.800→0.783**. **롤백**.

---

## PART C — 미니 백테스트 (1200~1222)

| 뇌 | Phase1 avg | Phase2 **최종** avg | 변화 | 판정 |
|----|-----------|---------------------|------|------|
| v13_graph | 0.817 | 0.817 | 0.000 | 롤백 후 기준선 |
| v13_bayesian | 0.930 | 0.930 | 0.000 | 롤백 후 기준선 |
| v13_trend | 0.704 | **0.713** | +0.009 | **유지** (±0.01 미만 확정) |
| v13_gen | 0.757 | 0.757 | 0.000 | **유지** |
| v13_contrarian_v2 | 0.817 | 0.817 | 0.000 | 롤백 후 기준선 |
| v13_rl | 0.800 | 0.800 | 0.000 | 롤백 후 기준선 |
| v13_transformer | 0.904 | 0.904 | 0.000 | 미변경 |
| v13_ensemble | 0.861 | **0.800** | −0.061 | trend 출력 변화에 따른 **부수적 열화** (별도 롤백 대상 아님) |

---

## PART D — 검증

| 항목 | 결과 |
|------|------|
| `predict(1224)` trend | 5세트, 6개 번호 |
| `predict(1224)` gen | 5세트, 6개 번호 |
| 미니 백테스트 | 에러 없음 |
| `GET /brain/status` | **200**, 프로필 8개 |
| My_Library | 0건 |

### 1224 샘플 (확정 로직 기준)

- **trend** 세트1: `[10, 12, 18, 23, 24, 28]`
- **gen** 세트1: `[5, 14, 15, 21, 26, 34]`

---

## 후속 제안

1. **graph·bayesian**: 조건부/4동반 보정을 더 약한 가중치(예: 0.9/0.1) 또는 소수 후보만으로 재실험.  
2. **ensemble**: trend 변경으로 미니 구간에서 ensemble이 내려감 → `full_backtest_v13`·더 긴 구간에서 재평가 후 Hedge 재학습 여부 결정.  
3. **Gap 로직**: contrarian·rl은 `_utils`의 z-score 함수가 이미 있으므로, 임계·보너스만 더 약하게 튜닝하는 Phase2-B 가능.

---

*끝.*
