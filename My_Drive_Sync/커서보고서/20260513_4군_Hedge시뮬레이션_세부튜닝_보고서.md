# 4군 Hedge 시뮬레이션 + 세부 튜닝 Phase 1 보고서

**일시**: 2026-05-13  
**대상**: `d:\3kweon`  
**DB**: `d:\3kweon\data\lotto4.db`  
**My_Library**: 읽기·쓰기 없음

---

## PHASE 1 — Hedge 시뮬레이션 (TASK 1)

### 실행 명령

기존 `tools/simulate_hedge_ensemble_v13.py` 확장 후 실행 (대체 `run_hedge_sim.py` **불필요**, 에러 없음):

```text
cd d:\3kweon
python tools/simulate_hedge_ensemble_v13.py --start 200 --end 1222 --init-weight 3.0 --snapshot-every 100 --write-db
```

### 코드 변경 (시뮬 레이어)

- `--init-weight`: 8뇌 동일 초기값 (본 실행 **3.0**)
- `--snapshot-every 100`: 100회차마다 `spread = max(w)-min(w)` 출력
- `--write-db`: 종료 시 `lotto_brain_weights_army4`에 `UPDATE` 반영
- 로직: 프로덕과 동일 **`apply_hedge_step_to_weights`** (세트 **평균** 적중 기반), `ensemble.predict(..., weights_override=...)`

### 시뮬 결과 (가장 중요)

| 항목 | 값 |
|------|-----|
| 유효 회차 | **200 ~ 1222** (1023회) |
| ensemble **best_avg** | **1.5582** |
| **최종 spread (max−min)** | **0.0200** (목표 >0.3 **미달**) |
| 벽시계 소요 | **약 722 s (~12.0 분)** |

### 최종 가중치 순위 (시뮬 → DB 반영)

| 순위 | brain_tag | weight |
|:----:|-----------|--------|
| 1 | v13_bayesian | **0.52** |
| 2~8 | 나머지 7뇌 | **0.50** |

→ 장기간 누적 후 대부분 **클립 하한(0.5)** 에 수렴, 베이지만 소폭 상단.

### 수렴 스냅샷 (100회차 간격)

| draw (대략) | spread |
|-------------|--------|
| 299 | 0.6200 |
| 399 | 0.0350 |
| 499 | 0.0000 |
| 599 | 0.0075 |
| 699 | 0.0050 |
| 799 | 0.0075 |
| 899 | 0.0375 |
| 999 | 0.0225 |
| 1099 | 0.0250 |
| 1199 | 0.0000 |

초반에는 분화가 있으나, **기준선(ref 1.25) + 학습률 0.05** 조합으로 후반에는 다시 **바닥 근처 동률**에 가깝게 붙는 패턴.

### DB 반영 확인

`lotto_brain_weights_army4` (실측):

- v13_bayesian: **0.52**
- 그 외 v13_*: **0.5**  
→ **“전부 3.0” 상태는 해제됨** (분화 폭은 매우 좁음).

---

## PHASE 2 — 약점 뇌 진단 (TASK 2)

### 가중치 하위

동률 **0.50** 다수 — 정렬 기준으로 **v13_contrarian_v2**, **v13_trend** 등을 하위로 볼 수 있으나 **수치 차이 없음**.

### 미니 구간(1200~1222) 직접 백테스트 (115세트/뇌)

시뮬 **직후**·**DB 가중치 0.5대** 상태에서 `mini_backtest_v13.py` 결과:

- **v13_trend** 평균 적중 **0.696** (8뇌 중 최저)
- **v13_ensemble** **0.748** (메타뇌 중 낮음)

### 저장 예측 기반 구간별 `best_avg` (DB `lotto_predictions_army4`)

#### 1200~1222 약세 구간이 뚜렷한 뇌 예시: `v13_trend`

| 구간 | best_avg | 비고 |
|------|----------|------|
| 200~399 | 1.6550 | |
| … | … | 중간 구간 1.73~1.78대 |
| **1200~1222** | **1.4783** | **상대적 저조** |

`v13_contrarian_v2`는 동일 1200~1222에서 **1.6522**로 trend보다 양호.

**하위 2뇌 선정 (Phase 3용):**  
- **성능 기준**: **v13_trend**, **v13_ensemble** (미니 평균 적중 최저·차저)

---

## PHASE 3 — 하이퍼파라미터 미세 튜닝 (TASK 3)

### 적용한 변경 (1뇌·1파라미터)

| 파일 | 변경 |
|------|------|
| `app/lotto4/brains/trend.py` | `WINDOWS` 첫 항목 `(10, 0.35)` → **`(10, 0.40)`** (최근 10회차 비중 소폭 상향) |

`v13_ensemble`은 메타 뇌라 동시에 여러 상수를 건드리지 않고, **trend 개선이 ensemble 투표에도 유리**한지 먼저 확인.

### 미니 백테스트 (1200~1222) 전후 비교

| 뇌 | 지표 | 조정 **전** | 조정 **후** | Δ |
|----|------|------------|------------|---|
| **v13_trend** | 평균 적중 | 0.696 | **0.704** | **+0.008** |
| **v13_ensemble** | 평균 적중 | 0.748 | **0.783** | **+0.035** |
| 기타 뇌 | (요약) | 변화 미기재 | 동일 실행에서 transformer 등 유지 | — |

→ **소폭 개선**으로 판단, **trend 패치 유지**.

---

## TASK 3 검증 체크리스트

| 항목 | 결과 |
|------|------|
| Hedge 시뮬 완료 | ✅ (1023회, ~722s) |
| 8뇌 가중치 분화 spread > 0.3 | ❌ (**0.02**) |
| DB 반영, 전부 3.0 아님 | ✅ (0.52 / 0.50×7) |
| 하위 뇌 식별 + 구간 분석 | ✅ (trend 1200~구간 등) |
| 하이퍼 1건 + 미니 검증 | ✅ (trend 윈도, 미니 개선) |
| `grep My_Library` `d:\3kweon\app` | **0건** |

---

## 총 소요 시간 (대략)

- Hedge 시뮬 + DB 반영: **~722 s**
- 구간 분석·grep·보고서 준비: **수 분**
- 미니 백테스트 ×2: **~20 s + ~19 s**

---

## 후속 제안

1. **Hedge 스케일**: `HEDGE_REFERENCE_MATCH` 추가 하향(예: 1.05~1.15) 또는 `HEDGE_LEARNING_RATE` 감소 후 **동일 시뮬 재실행**해 spread 목표 재평가.
2. **하한 0.5 고착**: `WEIGHT_CLIP_MIN`을 **0.55~0.6** 등으로 올려 과도한 동률 완화 검토.
3. **로그**: `lotto_weight_log_army4`에 시뮬/운영 헤지 스텝 기록 연동 여부 결정.
4. **v13_ensemble**: `CANDIDATE_POOL_SIZE` 등은 trend 검증 후 **단독 스윕** 권장 (한 번에 한 변수).
5. **전체 검증**: `full_backtest_v13.py`로 긴 구간 재측정(시간 허용 시).

---

## 부록: 실행 로그 원문 요약 (시뮬)

- `시뮬 회차 수: 1023`  
- `ensemble ... best_avg: 1.5582`  
- `최종 가중치 spread: 0.0200`  
- 스냅샷 및 최종 8뇌 순위표: 본 문서 PHASE 1 표와 동일  

---

*보고서 끝.*
