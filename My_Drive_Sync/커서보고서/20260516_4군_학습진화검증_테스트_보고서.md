# 4군 학습·진화 검증 테스트 보고서

- 실행일: 2026-05-16  
- 범위: `d:\3kweon`，`app/lotto`·`lotto2`·`lotto3` **수정 0건**  
- 풀 백테스트: **실행 안 함** (지시 준수)

---

## 0. 백테스트 프로세스 중단

- `full_backtest_v13.py` 매칭 `python.exe` PID **324280**, **328508** 종료 확인.

---

## 1. 룰즈 파일 저장 상태 (첫 3줄)

**STATUS_LATEST.md**

```text
# STATUS_LATEST (2026-05-16 기억71)

## 3군: 패치 A~J+M 완료, avg 1.502, SHA 고정, 간섭 0
```

**CURSOR_RULES.md**

```text
# CURSOR_RULES.md — 커서 행동 강제 규칙
# 최종 갱신: 2026-05-16
# 이 파일은 READ-ONLY. 형(사용자)만 수정 가능.
```

**RULES_FIXED.md** (R28~R30 말미 존재)

```text
\# 🛡️ RULES\_FIXED.md (불변 룰)


```

- 말미 확인: `## 추가 규칙 (2026-05-16)` 하에 **R28~R30** 기재됨 (`STATUS_LATEST` 동시 갱신: 학습·진화 검증 요약 추가).

---

## 2. struct_brain `.ubj` — save / load 경로

- **파일 경로·확장자:** `_model_path` 가 `struct_brain_{name}.**ubj**` 반환.
- **저장:** `_atomic_save_xgb` → `model.save_model(str(tmp))` (경로 확장자 `.ubj.tmp` → `.ubj`).
- **로드:** `_load_models` → `XGBRegressor.load_model(str(p))`, `p`는 동일 `.ubj`.

```149:161:app/lotto4/brains/struct_brain.py
def _model_path(name: str) -> Path:
    # XGBoost 3.x sklearn save_model 기본값은 UBJSON. .json 경로에도 UBJSON을 쓰면 load 시 JSON 파서 오류.
    return _models_dir() / f"struct_brain_{name}.ubj"


def _atomic_save_xgb(model: Any, path: Path) -> None:
    """동시 로드(XGBoost) 시 빈 파일을 읽는 일을 줄이기 위해 tmp 저장 후 replace."""
    _ensure_xgb()
    tmp = path.with_name(path.name + ".tmp")
    try:
        model.save_model(str(tmp))
        os.replace(str(tmp), str(path))
```

```222:231:app/lotto4/brains/struct_brain.py
def _load_models() -> list[Any]:
    _ensure_xgb()
    out: list[Any] = []
    for name in VAR_NAMES:
        p = _model_path(name)
        if not p.is_file():
            raise FileNotFoundError(str(p))
        m = XGBRegressor(**XGB_KW)
        m.load_model(str(p))
        out.append(m)
```

- **SHA256 (`struct_brain.py`, 작성 시점):** `E9868AF1774DBF00EE9FCA86822521F11EB24874601ED57EED7AD2B0C9879DB3`

---

## 3. DB 현재 상태 (조회만)

| 쿼리 | 결과 |
|------|------|
| `SELECT COUNT(*) FROM lotto_predictions_army4` | **2095** |
| `SELECT COUNT(*) FROM lotto_fullbacktest_army4` | **2095** |
| `SELECT COUNT(*) FROM lotto_evolution_trust_army4` | **118** |
| `SELECT COUNT(*) FROM lotto_draws WHERE draw_no BETWEEN 5 AND 1223` | **1219** |

---

## 4. 학습·진화 미니 테스트 결과

### 4-1 seq_brain

- `predict(100, db)` → `update_model(100, db)` → `predict(101, db)`
- **출력 요약**
  - draw 100: `[[3, 4, 13, 19, 38, 42], ...]` (5세트)
  - draw 101: `[[1, 13, 14, 19, 27, 38], ...]` (5세트)
- **학습 효과:** `sets_100 != sets_101` → **True**

### 4-2 struct_brain

- 지시서 코드: `initial_train(db_path=db, target_draw=100, ...)`
- **결과:** **실패** — `ValueError: 학습 표본 부족: 0`
- **원인(코드 사실만):** `_training_matrix` 내부에서 `draw_no >= MIN_DRAW_TRAIN`(101) 인 행만 학습에 포함. `target_draw=100`이면 사용 가능한 과거 회차만으로는 조건을 만족하는 표본이 없어 행 수가 0.

### 4-3 evolution_brain

- `w1 = get_dynamic_weights(100, db)` → `{'v13_seq': 0.5, 'v13_struct': 0.5}`
- **assert:** `v13_cdm`, `v13_cond_prob` **키 없음** (통과)
- `update_trust(100)`, `update_trust(101)`, `update_trust(102)`
- `w2 = get_dynamic_weights(103, db)` → 동일 `{'v13_seq': 0.5, 'v13_struct': 0.5}`
- **가중치 변화:** **False**

### 4-4 ensemble

- `predict(200, db)` → 5세트, 각 6번호, 1~45 범위 **통과**
- **콘솔:** XGBoost `Saving model in the UBJSON format as default` **UserWarning** 다수 (경로는 `.ubj` 사용 중)

---

## 5. 에러·경고 전문

**struct (예외 전문)**

```text
Traceback (most recent call last):
  File ".../struct_brain.py", line 205, in initial_train
    raise ValueError(f"학습 표본 부족: {0 if Xm is None else len(Xm)}")
ValueError: 학습 표본 부족: 0
```

---

## 📋 저장 체크리스트 (CURSOR_RULES 3-5)

- [✅] 보고서 저장 → `d:\3kweon\reports\20260516_4군_학습진화검증_테스트_보고서.md`
- [✅] STATUS_LATEST 갱신 → 학습·진화 미니 검증·풀백 중단 반영
- [✅] Drive 복사 → `D:\MONEY lol\My_Drive_Sync\커서보고서\` (동일 파일명)
- [✅] 1~3군 간섭: **0건**
