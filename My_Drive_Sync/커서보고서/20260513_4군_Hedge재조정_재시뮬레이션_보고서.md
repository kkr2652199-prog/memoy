# 4군 Hedge 파라미터 재조정 및 재시뮬레이션 보고서

**작성일**: 2026-05-13  
**대상 코드베이스**: `d:\3kweon`  
**DB**: `d:\3kweon\data\lotto4.db`  
**제약**: `My_Library` 경로 읽기·쓰기 미사용 (검증: `d:\3kweon\app` 내 `My_Library` 문자열 0건)

---

## 1. 파라미터 변경 전·후 (`app/lotto4/v13_weights_v2.py`)

| 상수 | 변경 전 | 변경 후 |
|------|---------|---------|
| `HEDGE_REFERENCE_MATCH` | 1.25 | **0.85** |
| `HEDGE_LEARNING_RATE` | 0.05 | **0.02** |
| `WEIGHT_CLIP_MIN` | 0.5 | **0.3** |
| `WEIGHT_CLIP_MAX` | 3.0 | **3.0** (유지) |

**검증 명령**

```text
python -c "from app.lotto4.v13_weights_v2 import HEDGE_REFERENCE_MATCH, HEDGE_LEARNING_RATE, WEIGHT_CLIP_MIN; print(HEDGE_REFERENCE_MATCH, HEDGE_LEARNING_RATE, WEIGHT_CLIP_MIN)"
```

**기대 출력**: `0.85 0.02 0.3` — 확인됨.

---

## 2. DB 초기화 및 시뮬 재실행

- 시뮬 전 `lotto_brain_weights_army4`의 8뇌 `current_weight`를 **1.5**로 맞춘 뒤 진행 (대화 세션 내 처리).
- **TASK 4-B** (ref 0.75 / lr 0.03 / clip min 0.2): 최종 spread가 이미 목표를 충족하여 **추가 조정은 실행하지 않음**.

**명령**

```text
cd d:\3kweon
python tools/simulate_hedge_ensemble_v13.py --start 200 --end 1222 --init-weight 1.5 --snapshot-every 100 --write-db
```

(스냅샷 수치 재현용으로 동일 인자에서 `--write-db` 없이 한 번 더 실행해 로그를 수집함. 궤적·지표는 동일.)

---

## 3. 시뮬 결과 요약

| 항목 | 1차 (이전 튜닝) | 2차 (본 재조정) | 비고 |
|------|----------------|-----------------|------|
| 총 유효 회차 | 1023 | 1023 | 동일 구간 |
| ensemble `best_avg` | 1.5582 | **1.5562** | ≥ 1.55 유지 |
| 최종 spread (max−min) | 0.0200 | **1.1690** | 목표 ≥ 0.5 **달성** |
| 총 소요 시간 | ~722 s (세션 기록) | **~735.9 s** | 재현 실행 기준 |
| 최고 가중치 뇌 | (전 뇌 ~0.50–0.52) | **v13_graph (1.469)** | 지시 예시(≥2.0) 미달, 분화는 충분 |
| 최저 가중치 뇌 | (동일 바닥) | **v13_rl (0.300)** | `WEIGHT_CLIP_MIN` 부근 |

---

## 4. 수렴 스냅샷 (100회차 단위)

시뮬 출력 기준 (`spread`, 최고 뇌, 최저 뇌).

| draw | spread | 최고뇌(값) | 최저뇌(값) |
|------|--------|------------|------------|
| 299 | 0.2560 | v13_graph(1.5480) | v13_bayesian(1.2920) |
| 399 | 0.4040 | v13_gen(1.5000) | v13_bayesian(1.0960) |
| 499 | 0.4640 | v13_graph(1.4280) | v13_transformer(0.9640) |
| 599 | 0.7480 | v13_graph(1.5240) | v13_transformer(0.7760) |
| 699 | 1.0160 | v13_graph(1.6360) | v13_transformer(0.6200) |
| 799 | 0.9880 | v13_graph(1.5920) | v13_transformer(0.6040) |
| 899 | 1.1600 | v13_graph(1.6680) | v13_transformer(0.5080) |
| 999 | 1.0320 | v13_graph(1.4640) | v13_transformer(0.4320) |
| 1099 | 1.1000 | v13_graph(1.4240) | v13_rl(0.3240) |
| 1199 | 1.1840 | v13_graph(1.4840) | v13_rl(0.3000) |

---

## 5. 최종 가중치 (DB 확정) 및 normalized

`SELECT` + 윈도우 정규화 (최대 가중치 기준 100).

| brain_tag | current_weight | normalized (%) |
|-----------|----------------|----------------|
| v13_graph | 1.469 | 100.0 |
| v13_ensemble | 1.001 | 68.1 |
| v13_trend | 0.565 | 38.5 |
| v13_gen | 0.509 | 34.6 |
| v13_bayesian | 0.421 | 28.7 |
| v13_transformer | 0.334 | 22.7 |
| v13_contrarian_v2 | 0.314 | 21.4 |
| v13_rl | 0.300 | 20.4 |

**가중치 분화**: max − min = 1.469 − 0.300 = **1.169** (≥ 0.5).

---

## 6. TASK 5 — 1224 재예측

- **스크립트**: `python tools/run_predict_1224.py` — Status `ok`, 40세트 + ensemble 5세트 출력 확인.
- **API**: `TestClient`로 `POST /api/lotto4/v13/predict/1224` → **HTTP 200**. 응답의 `all_predictions`는 **리스트** 형태이며, 행마다 `brain_tag`·번호가 포함됨 (지시서 예시의 dict 딥 접근과 다름).

### v13_ensemble 5세트 (갱신 가중치 반영 후)

1. [7, 14, 17, 18, 21, 34]  
2. [17, 18, 20, 26, 27, 38]  
3. [7, 18, 20, 21, 26, 40]  
4. [1, 7, 20, 27, 31, 38]  
5. [14, 16, 20, 26, 27, 40]

### 이전(균등·저 spread 시대) 대비

1차 시뮬 후에는 가중치가 사실상 동일해 ensemble이 **균등 투표에 가까웠음**. 본 재조정 후 DB 가중치가 **graph 상향 / rl·transformer 등 하향**으로 갈라졌으므로, 동일 회차·동일 하위 뇌 출력이라도 **가중 샘플링·MoE 경로가 달라져 ensemble 5세트가 전면적으로 바뀌는 것이 정상**이다. (이전 세트 하나 예: [13, 14, 20, 21, 24, 31] 등과 비교 시 번호 구성 전부 상이.)

---

## 7. 검증 체크리스트

| 항목 | 확인 | 결과 |
|------|------|------|
| 파라미터 | `python -c` import 출력 | **0.85 0.02 0.3** |
| 시뮬 완료 | 콘솔 | 에러 없음, 1023회 |
| spread ≥ 0.5 | 최종 spread | **1.1690** |
| DB 가중치 분화 | max − min | **≥ 0.5** |
| 1224 재예측 | `run_predict_1224.py` | **40 + 5 세트** |
| API POST | TestClient | **200** |
| `My_Library` | `grep` under `d:\3kweon\app` | **0건** |

---

## 8. 후속 제안

1. **운영 모니터링**: 실채점 후 `spread`가 다시 0.3 이하로 붕괴하면 `HEDGE_REFERENCE_MATCH`만 소폭 조정(예: 0.80)하는 쪽이 lr·min보다 안전할 수 있음.  
2. **지표 보강**: 스냅샷을 DB나 CSV에 자동 적재하면 보고서 작성 시 재실행(~12분)이 불필요함.  
3. **문서 정합**: 지시서의 “최고 뇌 bayesian/transformer ≥ 2.0”은 이번 궤적에서는 **graph**가 상단이었고 절대값 2.0 미달 — 목표는 spread·분화 달성으로 충분히 만족.

---

*끝.*
