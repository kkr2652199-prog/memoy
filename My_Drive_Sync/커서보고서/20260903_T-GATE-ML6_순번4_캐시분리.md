# 20260903 — 순번4 패치 · 캐시/명예의전당 실전 분리

**작업일:** 2026-09-03  
**범위:** 1군 · 형 지시「다음 진행」(순번4)  
**문헌:** backtest/paper 성적을 live 대시보드와 섞지 않음 (label + default live). DB는 삭제하지 않음.

---

## 0. 명분 공식 (유지)

**T-GATE-ML6** — 당첨 보장 아님. 난수가 아니라 시점·다중라벨·정직축·실전 Hedge.

순번4는 그 명분이 **화면에서 뒤집히지 않게** 하는 단계다. 04월 백테 1등이 명예의전당·대시보드에 실전처럼 보이면 공식은 문서에만 남는다.

---

## 1. 문제

- `run_prediction` 캐시: 기존 행이 있으면 「1회 실행 원칙」으로 그대로 반환. 추첨 **후** 생성된 1등도 그대로.
- 캐시 top5는 `ORDER BY confidence` **전체**(F-J). fresh는 miss/snake 제외.
- `get_hall_of_fame` / dashboard-summary / elite-tags / brain status: 181건 1~3등(전부 AFTER)을 실전 실력처럼 집계.

삭제하지 않음. 표시·집계 기본값만 **추첨일 20:45 이전 created_at**.

---

## 2. 패치

| 표면 | 기본 | 백필 조회 |
|------|------|-----------|
| 명예의전당 | `?scope=live` | `after` / `all` |
| 대시보드 1~3등·파워·점수 | live | `after_draw_counts` 메타 |
| elite-tags | live | (빈 목록이 정직) |
| 두뇌 등급 | live 최고적중 | `best_record_all` 보존 |
| 캐시 반환 | 행 유지 + `cache_kind` 라벨 | 상태문에 「실전 성적 아님」 |

캐시 top5 = fresh와 동일하게 miss/snake 제외 (`filter_top5_rows`).

---

## 3. 검증 (파일·DB·HTTP)

```
python -m app.lotto._test_tgate_ml6  → T-GATE-ML6 unit OK
live hall n=4  r1/r2/r3=(0,0,0)
after hall r1/r2/r3=(10,3,168)
run_prediction(1232) cache_kind=live
run_prediction(198)  cache_kind=after_draw  (fusion 1등, created 2026-04-25 > draw 2006-09-16)
get_brain_status grade=천재 best_mc=4  (구버전은 신/6)
HTTP 재시작 후:
  GET /brain/hall-of-fame          n=4 scope=live r1=0
  GET ...?scope=after              r1=10
  dashboard rank1=0  after_draw_counts={10,3,168}
  elite-tags=[]
```

---

## 4. 변경 파일

- `My_Library/app/lotto/generation_timing.py` (신규) — classify / top5 / hall 쿼리
- `engine.py` 276~320 캐시 라벨+F-J · 663~758 status/hall
- `routes.py` hall scope · elite live JOIN · dashboard live
- `honesty_flags.py` 60 `HALL_DEFAULT_SCOPE="live"`
- `lotto.js` 명예의전당 스코프 탭 · 대시보드 실전 문구
- `lotto.css` `.fame-scope-bar` 등
- `index.html` 캐시버스트 `v=20260903tgate4`
- `_test_tgate_ml6.py` classify·top5 단위테스트
- FINDINGS F-J PATCHED · F-H 캐시 항목 PATCHED (잔여 pool_size/tier1)

---

## 5. 미해결

- 순번5: F-D llm HOLD 태그 · F-I hyena readiness
- 순번6: F-C top-k · F-A confidence · F-B entropy
- F-H 잔여: pool_size / tier1 이중계산
- 과거 AFTER 행은 DB에 남음. 재생성은 형이 지시할 때만.

운은 남는다. 이번 패치는 **백테 1등을 실전 실력으로 안 보이게** 한 것이다.
