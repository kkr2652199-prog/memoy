# 패치 N 롤백 보고서 (M-4 코드 상태 복원)

- **일자**: 2026-05-07 KST  
- **목적**: **패치 N**(세트 간 다양성 강제) **완전 롤백** → **M-4** 시점과 동일한 `v12_models.py` · `v12_engine.py` 바이트 복원  
- **선행**: 기억45_v1 · `20260507_패치N_백테스트_보고서.md`  
- **제한**: `app/lotto/` · `app/lotto2/` **수정 없음** · **1222 미사용** (검증은 **100회차**)

---

## STEP 0 — SHA256 사전검증 (지시 기준)

### 롤백 대상 (패치 N 적용 시 로컬 기대값)

| 파일 | 해시 (전체, 대문자) |
|------|---------------------|
| `v12_models.py` | `26EEC603722E5F06A931B94CF2E4925A2613C41324AE21BAE69F7EBA9795B70B` |
| `v12_engine.py` | `1DA25D09AFBFE2F66DB01D467FE55DE10E8916AFA1D4696B048AE109A18FB3BF` |

### 롤백 목표 (M-4)

| 파일 | 해시 (전체, 대문자) |
|------|---------------------|
| `v12_models.py` | `82B1FBD76D0DE52C545AFA0BDACC1BBD86A21A9E0397493BEA12BBB26432896C` |
| `v12_engine.py` | `A23248FC9D94A96BAD254DC41B9C81D44F415FFDA61AC5B2699BDBEEF41C7684` |

### 미변경 (본 세션 롤백 후 재해시, STEP 0 지시와 일치)

- `v12_fusion_v5.py` · `v12_snake.py` · `predict_sumrange.py` · `predict_cooccur.py` · `predict_cdm.py` · `predict_constraint.py` · `predict_lstm.py` · `predict_stat.py` · `predict_run.py` · `predict_offset.py` · `predict_contrarian.py` — **전부 True** (전체 해시는 STEP 3-B 참고).

---

## STEP 1 — `v12_models.py` 롤백

**삭제·복원 내용**

- `from collections.abc import Sequence` import 제거  
- `v12_perturb_combo_multi_swap` · `v12_perturb_combo_fresh_random` 제거  
- `MAX_INTRA_PAIR_JACCARD_N` · `MAX_DIVERSITY_RETRY` 제거  
- `_jaccard_similarity` · `_nums_pass_intra_jaccard` · `_try_place_diverse_perturbation` · `v12_enforce_diversity` 제거  
- `v12_perturb_combo_one_swap` 직후 **`_calc_lottery_score`** 로 이어지도록 복원 (현재 **207~218**행 → **220**행부터 `_calc_lottery_score`)

**롤백 후 SHA256**: `82B1FBD76D0DE52C545AFA0BDACC1BBD86A21A9E0397493BEA12BBB26432896C` ✓

---

## STEP 2 — `v12_engine.py` 롤백

**삭제·복원 내용**

- `v12_models` import 목록에서 **`v12_enforce_diversity`** 제거 (**17~25**행)  
- `run_prediction_v12` 내 **8뇌** `stat_sets` / `v12_enforce_diversity(...)` 패턴 제거 → **`fresh.extend(_retag(...))` 직접 연쇄** 및 `hyena_sets` / `snake_sets` **직접 `extend`** 복원 (**322~354**행)

**롤백 후 SHA256**: `A23248FC9D94A96BAD254DC41B9C81D44F415FFDA61AC5B2699BDBEEF41C7684` ✓

---

## STEP 3 — 검증

### 3-A

```text
python -c "from app.main import app; print('app OK')"
→ app OK
```

### 3-B — SHA256 사후검증

| 파일 | 결과 |
|------|------|
| `v12_models.py` | **82B1FBD7…432896C** (목표와 일치) |
| `v12_engine.py` | **A23248FC…41C7684** (목표와 일치) |
| `v12_fusion_v5.py` | `30EBA7B9…8F10255` |
| `v12_snake.py` | `B76F8579…11DEB20` |
| `predict_*.py` (지시 9종) | M-4 보고서·지시와 **동일** |

### 3-C — `run_prediction_v12(100)` (기존 `v12_%` **100회차** 행 `DELETE` 후)

| 항목 | 결과 |
|------|------|
| `status` | **ok** |
| `v12_sets` / `all_predictions` 길이 | **40** / **40** |
| **v12_hyena** 5세트 | 점수·합계 기반 reasoning (`V12-A 5005, 합계…` / `점수…`) — **다양성 강제 없음** |

**hyena 5세트 (검증 실행 시점)**

1. `[1, 3, 7, 11, 24, 35]` — `V12-A 5005, 합계 2224.03…`  
2. `[1, 3, 7, 11, 24, 37]` — `점수 2065.85…`  
3. `[1, 3, 7, 24, 29, 35]` — `점수 2140.17…`  
4. `[1, 3, 7, 11, 35, 40]` — `점수 2065.85…`  
5. `[1, 3, 7, 24, 35, 40]` — `점수 2130.62…`  

---

## STEP 4 — diff 요약 (패치 N 대비 **삭제**된 내용, 라인번호는 **롤백 전 N 버전** 기준)

### `app/lotto3/v12_models.py`

```diff
--- v12_models.py (패치 N)
+++ v12_models.py (M-4 롤백 후)
@@
-from collections.abc import Sequence
@@ (약 221~329행 삭제)
-def v12_perturb_combo_multi_swap(...)
-    ...
-def v12_perturb_combo_fresh_random(...)
-    ...
-MAX_INTRA_PAIR_JACCARD_N = 0.5
-MAX_DIVERSITY_RETRY = 500
-def _jaccard_similarity(a: Sequence[int], b: Sequence[int]) -> float:
-    ...
-def _nums_pass_intra_jaccard(...) -> bool:
-    ...
-def _try_place_diverse_perturbation(...) -> bool:
-    ...
-def v12_enforce_diversity(...) -> list[dict]:
-    ...
```

*(실제 삭제 블록 약 **109행** — `v12_perturb_combo_one_swap` 종료 직후 ~ `v12_enforce_diversity` `return accepted` 까지.)*

### `app/lotto3/v12_engine.py`

```diff
--- v12_engine.py (패치 N)
+++ v12_engine.py (M-4 롤백 후)
@@ import 블록 (약 25행)
-    v12_enforce_diversity,
@@ run_prediction_v12 (약 323~363행 → 직접 extend 패턴으로 복원)
-    stat_sets = _retag(...)
-    fresh.extend(v12_enforce_diversity(stat_sets, ...))
+    fresh.extend(_retag(army3_cdm_predict(...), "v12_stat", ...))
     ...  # run/offset/contrarian/lstm/fusion 동일 패턴
-    hyena_sets = _v12_hyena_predict(...)
-    fresh.extend(v12_enforce_diversity(hyena_sets, ...))
+    hyena_sets = _v12_hyena_predict(...)
+    fresh.extend(hyena_sets)
-        fresh.extend(v12_enforce_diversity(snake_sets, ...))
+        fresh.extend(snake_sets)
```

*(N 버전 대비 **import 1줄 + 본문 약 40줄** 축소.)*

---

## 메모

- DB(`lotto_predictions_army3` 등)는 **롤백하지 않음** — 코드만 M-4로 복귀. 필요 시 별도 **초기화 + 백테**로 데이터 재생성.  
- 검증용 `_tmp_verify_draw100.py`는 실행 후 **삭제**함.

---

*검증 환경: `My_Library` · `data/lotto.db`.*
