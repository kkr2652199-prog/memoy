# 패치 K — 주기적 가중치 정규화 (NORMALIZE_PERIOD=50, TARGET_SUM=12.0)

- **일자**: 2026-05-05 KST  
- **목적**: `W_MAX=100` 고착 완화 — **50회차마다** 7뇌 가중치를 **비율 유지**하며 합계 **12.0**으로 리스케일 후 `W_MAX` 재클리핑  
- **선행**: 기억37_v1 · `20260505_클리핑고착_정규화_정찰보고서.md`  
- **제한 준수**: `app/lotto/`, `app/lotto2/` 미수정 · **1222 미사용** · `lotto_brain_weights_army3` / `lotto_weight_log_army3` **스키마 변경 없음** (`eta`에 정규화 표시)

---

## STEP 0 — SHA256 사전검증 (31파일)

- 패치 적용 **전**: `app/lotto3/v12_models.py` = **CA001F7B9CE8DB9700FE24B62A66299A1E34D2101939DF9E86A53D576DEF11E8** (패치 J) · 나머지 30파일 = 패치 J 보고서 기대값과 **일치** 확인.

---

## STEP 1 — 상수 추가 (`v12_models.py`)

| 항목 | 내용 |
|------|------|
| 위치 | `W_MAX` 직후 **46~49행** |
| 상수 | `NORMALIZE_PERIOD = 50`, `TARGET_SUM = 12.0`, `ETA_LOG_NORMALIZE = -1.0` |
| 주석 | 패치 K — 주기적 가중치 정규화. 50회차마다 비율 유지 리스케일. 고착 해소용. |

*로그에서 일반 업데이트 `eta=2.0`과 구분하기 위해 **`ETA_LOG_NORMALIZE=-1.0`** 상수 사용(지시의 `eta=-1.0`과 동일 의미).*

---

## STEP 2 — `update_v12_weights` 정규화 (`v12_models.py`)

| 항목 | 내용 |
|------|------|
| 위치 | **356~370행**: `wa_hedge` 산출 → `normalize_effective` 판정 → `wa_final` 리스케일·`W_MAX` 클리핑 |
| 조건 | `target_draw_no > 0` 이고 `target_draw_no % NORMALIZE_PERIOD == 0` 이고 `sum(wa_hedge) > 0` |
| 수식 | `wa_final[i] = min(wa_hedge[i] * (TARGET_SUM / sum_hedge), W_MAX)` |
| DB UPDATE | `new_by_tag`에 있는 뇌: 기존과 동일 full UPDATE · **정규화만 해당** 뇌: `current_weight`·`last_updated_draw`·`updated_at` 부분 UPDATE |
| 로그 | 정규화 적용 시: `weight_before` = **정규화 전** `wa_hedge`, `weight_after` = `wa_final`, `eta` = **-1.0** · `matched_count`는 기존처럼 해당 회차 MAX(특수값 미사용) |
| 비정규화 회차 | 기존과 동일: `weight_before` = DB `wb`, `eta` = `V11_HEDGE_ETA` (2.0) |

---

## STEP 3 — 검증 결과

| 단계 | 결과 |
|------|------|
| 3-A | `python -c "from app.main import app; print('app OK')"` → **`app OK`** |
| 3-B | `lotto_weight_log_army3`에서 `draw_no=50` 기존 7행 삭제 후 `update_v12_weights(50)` | **`eta=-1.0` 행 7개** · `SUM(weight_after)` = **12.0** (부동소수 **~1e-15** 오차) |
| 3-C | `update_v12_weights(51)` 후 `draw_no=51` | **`eta=2.0`인 행 7개** (일반 업데이트) |
| 3-D | hyena/fusion 비율 | 정규화 **전** `weight_before` 비율과 **후** `weight_after` 비율 **동일** (스케일만 적용, 검증 스크립트 `abs(r_before-r_after)<1e-9`) |

---

## STEP 4 — SHA256 사후검증 (31파일)

### 변경 (1)

| 경로 | SHA256 (전체) |
|------|----------------|
| `app/lotto3/v12_models.py` | **9F7C60DA258550E0EBFAE672F22717FAEC4B800759145AF75387675CE64AF50C** |

### 미변경 (30)

패치 J 기준 30파일 — 본 세션 `python` 해시 재계산 **전부 일치**.

---

## STEP 5 — diff 요약 (`v12_models.py`)

| 라인대 | 변경 요지 |
|--------|------------|
| **46~49** | 패치 K 상수 3개 + 주석 |
| **296~302** | `update_v12_weights` 독스트링에 정규화 설명 |
| **356~432** | `wa_hedge` / `sum_hedge` / `normalize_effective` / `wa_final` · `UPDATE` 분기(`elif normalize_effective`) · 로그 `log_wb`/`log_eta` 분기 |

---

## 미해결 / 참고

- Sonar **인지 복잡도** 경고(`update_v12_weights`) — 본 패치 범위 밖 리팩터 미실시.  
- 정규화 직후 `W_MAX`로 잘리면 **합계가 12 미만** 가능(본 시나리오에서는 미발생).

---

## 검증 명령 (재현)

```powershell
Set-Location "d:\MONEY lol\My_Library"
python -c "from app.main import app; print('app OK')"
```

*(draw 50/51 로그 검증은 임시 스크립트로 수행 후 삭제.)*
