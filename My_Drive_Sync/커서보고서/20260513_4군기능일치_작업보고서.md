# 4군 기능 일치 작업 보고서 (3군 동기화)

- **일자**: 2026-05-13  
- **작업 트리**: `d:\3kweon` (4군, localhost:6124)  
- **참고**: `D:\MONEY lol\My_Library` 는 참조만, 미수정  
- **제약**: `app/lotto*`(3군 공용 패키지) 파일은 변경하지 않음  

---

## 지시문 점검 (간략)

| 항목 | 내용 |
|------|------|
| `cooccur-3` 명칭 | API는 **3개 조합**이나, 지시문에서는 "쌍"으로 표기됨 → 프론트 차트 제목에 **3-조합(cooccur-3)** 으로 명시 |
| `GET /draws` 의 `total` | **DB 전체 행 COUNT** 로 통일 (`total_pages * per_page` 와 혼동 방지) |

문제로 인한 차단 사항 없음, 위 전제로 구현·검증 진행.

---

## TASK별 요약

### TASK 1 — 데이터수집 일괄 (`mode`)

| 구분 | 내용 |
|------|------|
| **백엔드** | `POST /collect-draws` JSON: `mode`: `single` \| `all` \| `latest`, `single` 시 선택 `draw_no`. 응답: `ok`, `collected`, `count`, `errors` + `hint_before`/`hint_after`/`mode` |
| **프론트** | 전체·최신·단일(회차 입력) 버튼, `fetchJsonHandled`로 요청, 로딩 `#collectBusy`, 요약 `#collectResultSummary`, 완료 후 `loadCollectStatus`·`loadDashboard`·`loadBrainMeta` |
| **HTML** | 데이터수집 카드에 버튼·입력·요약 영역 추가 |

### TASK 2 — 통계 다중 차트

- `renderOddEvenChart`, `renderRangeChart`, `renderSumChart`, `renderPairChart` 추가  
- `loadStats`: `comprehensive-full` 의 `odd_even`, `range_distribution`, `sum_range` 사용 + 기존 `cooccur-3` 응답으로 동반출현 차트  
- `index.html` 통계 섹션에 차트 4개 컨테이너, `lotto4.css` 에 `.chart-*` 스타일  

### TASK 3 — 대시보드 카운트다운

- 매주 **토요일 20:45 KST** 기준 다음 추첨까지 남은 시간, `DD일 HH시간 MM분 SS초`, 1초 갱신  
- `loadDashboard` 에서 `renderCountdown(next_draw_no)` 호출  

### TASK 4 — `GET /draws` 호환

- `limit` 만 전달 시: `total` = 전체 COUNT, `page`/`per_page`/`total_pages`/`limit` 포함  

### TASK 5 — 순위 드롭다운

- 대시보드에 **뇌별 가중치 순위** 토글, `/brain/ranks` 로 목록 채움  

### TASK 6 — 라벨

- 네비·섹션 제목: **특수부대**, **두뇌상태** 로 통일  

---

## 변경 파일

| 파일 | 변경 요약 |
|------|-----------|
| `app/lotto4/v13_routes.py` | `/collect-draws` 본문·헬퍼, `/draws` limit 응답 보강, `Request` import |
| `app/static/js/lotto4.js` | 카운트다운·차트·수집·순위 드롭다운·`loadDashboard`/`loadStats` 보강 |
| `app/static/index.html` | 대시보드·통계·데이터수집 마크업, 라벨, 캐시버전 |
| `app/static/css/lotto4.css` | 카운트다운·차트·드롭다운·수집 UI 스타일 |

*(줄 수·diff는 로컬 `git diff --stat` 미사용 환경이라 생략; 위 파일만 수정.)*

---

## 검증 결과

| # | 검증 | 결과 |
|---|------|------|
| 1 | `python -c "from app.main_v13 import app; print('app OK')"` | **통과** |
| 2 | TestClient: `GET /` | **200** |
|  | `GET /api/lotto4/v13/brain/status` | **200** |
|  | `GET /api/lotto4/v13/dashboard-summary` | **200** |
|  | `POST /api/lotto4/v13/collect-draws` `{"mode":"latest"}` | **200** |
|  | `POST /api/lotto4/v13/collect-draws` `{"mode":"all"}` | **200** |
|  | `GET .../stats/comprehensive-full` | **200** |
|  | `GET .../stats/cooccur-3` | **200** |
|  | `GET .../draws?page=1&per_page=50` | **200** |
|  | `GET .../draws?limit=10` (`total`=전체 행 수 확인) | **200** |
|  | `GET .../hall-of-fame` | **200** |
|  | `GET .../predictions/1222` | **200** |
| 3 | `My_Library` in `d:\3kweon\app\` | **0건** |
| 4 | `localhost:8123` in `d:\3kweon\app\` | **0건** |
| 5 | 브라우저 localhost:6124 UI | **미실행 (수동 확인 권장)** |

실패한 자동 검증 항목: 없음.  
브라우저에서 카운트다운·4개 차트·수집 3버튼·당첨이력 페이지네이션을 한 번씩 확인하면 됩니다.

---

## 비고

- `mode=all` 은 **DB `next_draw_no`부터 API 성공이 이어지는 동안만** 연속 수집합니다( 첫 실패에서 중단 ).  
- 동반출현 차트는 **cooccur-3 (3개 조합)** 데이터입니다.
