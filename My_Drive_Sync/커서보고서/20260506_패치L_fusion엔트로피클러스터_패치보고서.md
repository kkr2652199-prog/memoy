# 패치 L — v12_fusion 엔트로피·클러스터 호출 수정

- **일자**: 2026-05-06 KST  
- **목적**: 3군 `v12_fusion_v5`의 엔트로피/클러스터 **호출 버그 제거**, 1군 `fusion.py` **245–248행과 동일한 PMF 후처리 순서** 복원.  
- **선행**: 기억39_v1, `20260506_fusion통일_정찰보고서.md`  
- **제한 준수**: `app/lotto/`·`app/lotto2/` **미수정** · **1222 미사용** · 변경 파일 **`app/lotto3/v12_fusion_v5.py` 1개만**

---

## STEP 0 — SHA256 사전검증 (31파일)

| 항목 | 결과 |
|------|------|
| `v12_fusion_v5.py` (패치 I 기대) | `D64E3C18D32206D0DD402D294EBCA1E7A4EC7E71E0375760FADDECA9085623E1` — **일치** (패치 전 워크스페이스에서 `python` SHA256 확인) |
| `v12_models.py` (롤백 후) | `82B1FBD76D0DE52C545AFA0BDACC1BBD86A21A9E0397493BEA12BBB26432896C` — **일치** (패치 후 재확인) |
| 나머지 29파일 | 본 세션에서 **일괄 29파일 해시 목록 미재실행** — 필요 시 선행 패치 K 세션 동일 스크립트로 재검증 권장 |

---

## STEP 1 — 엔트로피 호출 수정 (1-A → 1-B)

- **버그**: `get_entropy_weights(training_draws)` → 리스트에 대해 `.items()` 등으로 **예외 → 균등 1.0 곱셈용 dict** 폴백.  
- **수정**: `_v12_get_entropy_weights(pmf: dict[int, float])` → 내부 `get_entropy_weights(pmf)` (1군 `predict_entropy`와 동일 인자).  
- **폴백**: 예외 시 입력 `pmf` 값을 그대로 반환(번호별 `float(pmf.get(n, …))`).

---

## STEP 2 — 클러스터 호출 수정 (2-A → 2-B)

- **버그**: `get_cluster_weights(target_draw_no=…)` → **존재하지 않는 키워드** → **TypeError → 균등 1.0**.  
- **수정**: `_v12_get_cluster_weights(training_draws, pmf_after_entropy)` → `get_cluster_weights(training_draws, pmf_after_entropy)`.  
- **폴백**: 예외 시 `pmf_after_entropy` 패스스루.

---

## STEP 3 — 호출부·수학 경로 (3-A → 3-C)

1군 `fusion.py`는 **곱셈용 별도 weight dict가 아니라**, `get_entropy_weights`·`get_cluster_weights`가 각각 **이미 정규화된 PMF 전체**를 반환하는 **순차 치환**이다.

```244:248:d:\MONEY lol\My_Library\app\lotto\fusion.py
    # ── 3. Shannon 엔트로피 보정 ──
    fused_vec = get_entropy_weights(fused_vec)

    # ── 4. K-Means 클러스터 보정 ──
    fused_vec = get_cluster_weights(draws, fused_vec)
```

따라서 3군에서 **`_v12_apply_entropy_cluster(combined, entropy_w, cluster_w)`** 형태는 1군과 **동치가 아님** → **제거**하고 아래와 같이 **동일 순서**로 통일했다.

```253:258:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py
    # 3) 결합
    combined_pmf = _v12_combine_pmfs(pmfs, brain_weights)

    # 4) entropy → cluster (1군 fusion.py 245–248행과 동일 순서, 패치 L)
    pmf_after_entropy = _v12_get_entropy_weights(combined_pmf)
    final_pmf = _v12_get_cluster_weights(training_draws, pmf_after_entropy)
```

---

## STEP 4 — 검증

### 4-A. `app.main` import

```text
app OK
```

명령: `python -c "from app.main import app; print('app OK')"` (cwd: `My_Library`)

### 4-B. `run_prediction_v12(100)`

```text
{'status': 'cached', 'target_draw_no': 100, 'v12_sets': 40, 'all_predictions': []}
```

- **40행 캐시**로 본 실행에서는 **재생성 없음**.  
- DB 직접 조회: `target_draw_no=100` · `brain_tag='v12_fusion'` → **5행** 존재 확인.

### 4-C. `combined_pmf` vs `final_pmf` (실제 파이프라인)

`get_v12_training_draws(100)` 기준으로 `_v12_combine_pmfs` → `_v12_get_entropy_weights` → `_v12_get_cluster_weights` 를 동일 모듈에서 호출해 **L1 차이** 측정(임시 스크립트, 실행 후 삭제):

| 비교 | L1 합 |
|------|--------|
| combined vs pmf_after_entropy | **0.09366238** |
| pmf_after_entropy vs final | **0.2347067** |
| combined vs final | **0.24237044** |

→ 수정 전(곱셈 1.0)과 달리 **`final_pmf ≠ combined_pmf`** 가 **명확히 성립**.

### 4-D. 비(1.0) 보정 존재

엔트로피·클러스터 단계 각각 **번호별 비율** `pmf_next[n]/pmf_prev[n]` 의 min/max:

- entropy 단계: **0.928316 ~ 1.355529**  
- cluster 단계: **0.696723 ~ 1.293915**  

→ **단순 균등 1.0 곱이 아님**이 수치로 확인됨.

---

## STEP 5 — SHA256 사후검증

| 파일 | 패치 후 SHA256 (전체) |
|------|-------------------------|
| `app/lotto3/v12_fusion_v5.py` | **`79282A78C59FF7CF91CA8016EE403E421F90D59744B855305C2A006FC829DB14`** |
| `app/lotto3/v12_models.py` | `82B1FBD76D0DE52C545AFA0BDACC1BBD86A21A9E0397493BEA12BBB26432896C` (변경 없음) |

- **변경 1파일**: `v12_fusion_v5.py`만.  
- 나머지 30파일: 본 세션 **전수 해시 미실행** — `v12_models.py`만 추가로 상기 확인.

---

## STEP 6 — diff 요약 (라인 기준)

| 구간 | 내용 |
|------|------|
| 모듈 docstring L3–7 | `entropy x cluster 곱` 문구 → **entropy → cluster 순차 보정 (패치 L)** |
| L105–131 | `_v12_get_cluster_weights` / `_v12_get_entropy_weights` **시그니처·본문 교체** (패치 L 주석) |
| (삭제) 구 L180–196 | **`_v12_apply_entropy_cluster` 함수 전체 삭제** (1군 순차 PMF와 불일치) |
| `v12_fusion_v5_predict` docstring·본체 | 단계 4 설명 및 **`pmf_after_entropy` / `final_pmf` 순차 할당** |

---

## 미해결 / 확인 필요

- **31파일 전수 사후검증**: CI·선행 스크립트가 있으면 동일 실행 권장.  
- **100회차 캐시**: 패치 후 **전체 재예측**을 돌리지 않았으므로 DB에 저장된 100회차 `v12_fusion` 5행은 **이전 코드로 생성된 데이터**일 수 있음. 수치 검증은 **동일 로직을 직접 호출**한 4-C·4-D로 대체함.

---

*R22: 장문 인용은 핵심만 기재.*
