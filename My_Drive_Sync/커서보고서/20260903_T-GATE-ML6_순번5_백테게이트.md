# 20260903 — 순번5 + 백테 게이트 · T-GATE 구행 분리

**작업일:** 2026-09-03  
**범위:** 1군 · 형 지시「진행해줘」  
**구행 삭제 없음.** 04월 1등 10건 유지 확인.

---

## 0. 왜 이 패치인가

전 회차 백테를 지금 돌리면 `run_prediction`이 기존 행을 반환하고 `best_match`가 04월 1등을 집는다.  
입력 SQL은 원래 `draw_no < t`로 정직하다. 막아야 할 것은 **구캐시 재사용**과 **lead1이 구 1등 이력을 학습 신호로 쓰는 2차 컨닝**.

---

## 1. 백테 게이트

- `lotto_predictions.formula_id` 컬럼 추가. 신규 INSERT = `T-GATE-ML6`. 구행은 빈 문자열.
- `FORCE_BACKTEST_REGENERATE=True` → 백테는 T-GATE 완비 시에만 재사용, 아니면 **구행은 두고** T-GATE만 생성.
- 백테 `best_match` = `formula_id=T-GATE-ML6` 만 (아카이브 1등 제외).
- lead1 풀·신뢰도 이력도 T-GATE 행만. 없으면 Laplace(동등 가중).
- 피드백 JSON도 T-GATE가 있으면 그것만.

검증: `archive_r1=10` 유지, `tgate_rows=0` (아직 전회차 백테 미실행).

---

## 2. F-D llm 태그

HOLD/미연결 통계대체 → `brain_tag=llm_fallback`.  
lead1 합의 풀은 HOLD 시 **stat/markov/lstm/fusion** 4뇌. 가짜 llm으로 k 부풀리지 않음.

---

## 3. F-I hyena 준비 판정

`army1_generation_tags()` = hyena OFF면 5태그. `_army1_predictions_ready`가 hyena 5세트를 요구하지 않음.

---

## 4. 검증

```
python -m app.lotto._test_tgate_ml6  → OK
llm_insert_tag(hold) → llm_fallback
army1_generation_tags → hyena 없음
tgate_complete는 formula 없는 fusion 1등을 완비로 안 봄
PRAGMA formula_id 존재, 아카이브 1등 10건
```

전 회차 백테는 **이 턴에 실행하지 않음.** LSTM 임시학습이 회차마다 붙는다. 남은 순번6(F-C/A/B, F-F) 후 형이 지시하면 그때 돌린다.

---

## 5. 변경 파일

- `models.py` formula_id 마이그레이션
- `honesty_flags.py` FORCE_BACKTEST_REGENERATE, army1_generation_tags
- `engine.py` force_regenerate · T-GATE 채점 · llm_insert_tag
- `predict_llm.py` llm_insert_tag
- `predict_brain7.py` T-GATE 풀 + HOLD 4뇌
- `data_service.py` F-I
- `feedback.py` T-GATE 우선
- `generation_timing.py` tgate_tags_complete
- `_test_tgate_ml6.py`

## 6. 미해결

순번6: F-C top-k · F-A confidence · F-B entropy · F-F LSTM 저번호.  
F-G / F-H pool_size·tier1.  
전 회차 백테는 아직 안 함.
