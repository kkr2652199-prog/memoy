# 4군 독립 뇌 재설계 Phase 1 보고서 (2026-05-09 KST)

## 목적·범위

- `d:\3kweon`만 수정. `lotto_cooccur_3`, `_4`, `bonus_stats`, `number_freq` 파이프라인 유지.
- 3군 복제 v13 구현체는 `app/lotto4/legacy/`로 이동(삭제 없음).
- 신규 엔진: `v13_engine_v2.py`, 가중치: `v13_weights_v2.py`, 뇌: `app/lotto4/brains/`.
- Phase 1 구현 뇌: **v13_trend**, **v13_bayesian** (각 5세트, 합계 10세트 저장).

## STEP 0 — 레거시 이동

`legacy/`에 다음이 보관됨: `predict_*.py`, `v13_engine.py`, `v13_models.py`, `v13_snake.py`, `v13_fusion.py`, `v13_fusion_v5.py` (내부 import는 `app.lotto4.legacy.*`로 정리).

## STEP 1~5 — 엔진·레지스트리

- `V13_V2_BRAIN_ORDER` 8개: trend, bayesian, graph, transformer, generator, rl_agent, ensemble, contrarian_v2.
- `run_prediction_v13`는 v2 엔진으로 연결; placeholder 뇌는 `NotImplementedError` → 빈 리스트.
- `v13_ensemble`만 `prior_results` dict로 앞선 뇌 출력을 전달(Phase 4에서 구현).
- DB 저장 시 **같은 회차의** `v13_trend`, `v13_bayesian` 행만 `DELETE` 후 재삽입(다른 `v13_*` 레거시 적중 이력은 유지).

## STEP 6 — 미니 백테스트 (1100~1221)

`run_v13_mini_backtest(1100, 1221, chunk=50)` 실행 결과(로컬 DB 기준):

| 뇌 | avg_mc | max_mc | n_predictions |
|----|--------|--------|---------------|
| v13_trend | 0.782 | 4 | 610 |
| v13_bayesian | 0.7541 | 4 | 610 |

- 3군 M-4 기준 avg 1.502는 **레거시 7+2뇌 체계** 기준이므로 수치 직접 비교는 참고용.
- 실행: `python run_v13.py --mini-backtest` 또는 `run_v13_mini_backtest(...)`.

## STEP 7 — 검증

| 항목 | 결과 |
|------|------|
| `run_v13.py` (uvicorn 6124) | 유지 (`--mini-backtest` 분기 추가) |
| `GET /api/lotto4/v13/brain/status` | 200, `v13_trend`·`v13_bayesian` 프로필 표시(`matched_count >= -1` 집계로 미채점 포함) |
| `POST /api/lotto4/v13/predict/1222` | 200, 10세트 |
| `My_Library` | `d:\3kweon` 기준 0건 |
| `legacy/` | 파일 보존 확인 |

## STEP 8 — 변경·추가·이동 파일·줄 수(대략)

### 이동 (`app/lotto4/legacy/`)

- `predict_cdm.py` 209, `predict_combo.py` 71, `predict_constraint.py` 338, `predict_contrarian.py` 255, `predict_cooccur.py` 240, `predict_dead_zone.py` 85, `predict_lstm.py` 53, `predict_markov.py` 14, `predict_offset.py` 162, `predict_run.py` 125, `predict_stat.py` 43, `predict_sumrange.py` 195
- `v13_engine.py` 459, `v13_fusion.py` 88, `v13_fusion_v5.py` 342, `v13_models.py` 354, `v13_snake.py` 166
- `__init__.py` 1

### 신규·수정

- `app/lotto4/brains/`: `trend.py` 92, `bayesian.py` 117, placeholder 각 ~3줄, `__init__.py` 3
- `app/lotto4/v13_engine_v2.py` 272, `v13_weights_v2.py` 65
- 수정: `main_v13.py`, `v13_routes.py`, `run_v13.py`, `app/static/js/lotto4.js`, `tools/init_4g_db.py`, `tools/verify_cooccur2_top3.py`

## 알고리즘 요약

- **trend**: 최근 10/30/50/100/전체 윈도우 정규화 빈도에 가중합(0.35/0.25/0.20/0.12/0.08) → 상위 15개 `C(15,6)` 중 합계 100~175·홀수 개수 2~4만 채택 → 세트 간 Jaccard\<0.5.
- **bayesian**: 매 회차 `Beta(α,β)` 갱신에 `0.998^(target - 관측회차)` 가중, `betavariate`로 샘플 후 상위 6개; `lotto_cooccur_3` 기반 log 보정.

## 비고

- 대시보드 `brain_power`는 `V13_V2_BRAIN_ORDER` 8슬롯 기준(미적중 태그는 0 표시).
- UI 탭 기본 뇌: `v13_trend`.
