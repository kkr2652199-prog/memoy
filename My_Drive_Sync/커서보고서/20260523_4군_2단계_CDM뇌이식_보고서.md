# 4군 2단계 — CDM 뇌 이식 보고서

- **날짜**: 2026-05-23
- **목표**: avg > 0.8813 (기저선 초과)
- **결과**: avg **0.8585** — **목표 미달 (악화 -0.0228)**
- **1~3군 간섭**: 0건

---

## STEP 0 — 확인

**RULES_FIXED.md**: `# 🛡️ RULES_FIXED.md (불변 룰)`  
**STATUS_LATEST.md**: `# STATUS_LATEST (2026-05-23 기억83)`  
**CURSOR_RULES.md**: `# CURSOR_RULES.md — 커서 행동 강제 규칙`

### SHA256

| 파일 | 변경 전 | 변경 후 |
|------|---------|---------|
| hyena_commander.py | `1a3d80ab...` | `2fac9ae5...` |
| cdm_brain.py | (기존 v13_cdm 다중윈도) | `726a2af4...` (신규 CDMBrain) |
| ensemble.py | `41eea023...` | (동일) |
| stat_generator.py | `b85fe41d...` | (동일) |

**기저선**: v13_ensemble avg = 0.8813, hit6 = 0

---

## STEP 1 — 3군 CDM 원본 분석

| 항목 | 내용 |
|------|------|
| `army3_cdm_prob_vector` | training_draws → `_accumulate_counts` → Dirichlet α=1+count → 정규화 PMF |
| `army3_cdm_predict` | PMF 비복원 6개 샘플링 + tier1_filter + win_avoid |
| Dirichlet prior | **α = 1.0** (`ALPHA_PRIOR`) |
| 조건부 | 직접적 조건부 아님 — **최근 50회 RECENCY_MULTIPLIER=2.0** 가중 |
| tier1_filter | **사용** (`app.lotto.filters.tier1_filter`) |
| DB | 3군은 training_draws 리스트 입력, DB 직접 접근 없음 |

---

## STEP 2 — cdm_brain.py (CDMBrain)

- `get_pmf()`: 최근 100회, α=1+count, 직전 회차 공동출현 boost(+0.15)
- `predict()`: PMF → Top15 → 15C6 → 구조필터 → 5세트
- RNG: `target_draw * 20260523` (fallback 샘플링)
- walk-forward: `load_draws_before(db, target_draw)` ✅

---

## STEP 3 — hyena_commander.py 수정

```python
# 210세트 = stat(200) + CDM(5) + cooccur(5)
cdm = CDMBrain()
for combo in cdm.predict(target_draw, db_path, CDM_SETS):
    candidates.append(combo)
```

stat/cooccur 로직 변경 없음. consensus → Top15 → 15C6 → 5세트 동일.

---

## STEP 4 — 단위 테스트 8개

| # | 테스트 | 결과 |
|---|--------|------|
| 1 | PMF 45키, 합≈1.0, ≥0 | PASS |
| 2 | PMF 2회 동일 | PASS |
| 3 | predict 5세트, 1~45, 정렬 | PASS |
| 4 | 구조필터 통과 | PASS |
| 5 | draw=100 PMF | PASS |
| 6 | hyena 결정론적 | PASS |
| 7 | ensemble == hyena | PASS |
| 8 | < 5초 (0.314s) | PASS |

---

## STEP 5 — 미니 백테스트 (1100~1222)

### 5-E: 전수 검증

| brain | avg | max | hit6 |
|-------|-----|-----|------|
| v13_struct | 0.8992 | 3 | 0 |
| **v13_ensemble** | **0.8585** | 5 | **0** |
| v13_diversity | 0.8585 | 4 | 0 |
| v13_evolution | 0.8472 | 5 | 0 |
| v13_ev | 0.7870 | 4 | 0 |
| v13_gap | 0.7675 | 4 | 0 |

**모든 뇌 hit6=0** ✅ | **avg > 1.0 없음** ✅

### 5-F: 비교표

| 방식 | avg | 4+% | max | 판정 |
|------|-----|-----|-----|------|
| Random | 0.800 | ~0.1% | — | 기준 |
| 깨끗한 기저선 (cooccur) | **0.8813** | 0.16% | 5 | 기저선 |
| **CDM 추가 후** | **0.8585** | 0.33% | 5 | ❌ -0.0228 |
| 3군 hyena | 2.076 | — | — | 최종 목표 |

---

## STEP 6 — CDM PMF vs stat PMF (draw 1200)

| 항목 | 값 |
|------|-----|
| CDM top10 | [13,3,7,37,19,6,33,38,16,30] |
| stat top10 | [13,3,7,6,16,19,30,37,33,38] |
| **Top-10 겹침** | **10/10** (정보 다양성 낮음) |
| CDM 고유 번호 | 없음 |
| Top-15 변경 (200+5 vs 200+5+5) | **0개** (draw 1200) |

### 악화 원인 분석
1. CDM PMF ≈ stat PMF (동일 100회 빈도 + Dirichlet ≈ 빈도 정규화)
2. CDM 5세트가 consensus Top-15를 바꾸지 못함 (draw 1200 기준)
3. 210세트 빈도 미세 변화 → 15C6 선택 조합 변경 → 123회차 누적 avg 하락
4. 4+% 0.16%→0.33% 상승은 표본 1건 증가 수준 (통계적 유의성 낮음)

---

## 판정

| 항목 | 결과 |
|------|------|
| 목표 avg > 0.8813 | **실패** (0.8585) |
| 커닝 재발 | **없음** (hit6=0) |
| CDM 정보 다양성 | **낮음** (top10 100% 겹침) |
| 권장 | CDM을 pool 추가가 아닌 **가중 PMF fusion** 또는 **sumrange** 이식 검토 |

---

## 현재 4군 상태

**파이프라인**: stat(200) + CDM(5) + cooccur(5) = 210 → freq → Top15 → 15C6 → 5세트

**활성**: ensemble, struct, diversity, ev, gap, evolution, cooccur, cdm(내부)  
**격리**: seq  
**Hidden**: v13_cdm(slot), cond_prob

## 다음 이식 대상: sumrange

## 1~3군 간섭: 0건
