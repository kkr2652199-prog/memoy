# 4군 예측 실행 버튼 번호 출력 경로 정찰 보고서

- **날짜**: 2026-05-29
- **이름**: 동생 → 커서
- **선행**: 정직앱 전환, 엔진3 구현 (기억88)
- **모드**: READ-ONLY (코드 수정 **0줄**)
- **1~3군 간섭**: **0건**

---

## 작업 시작 전 3파일 확인

```
✅ RULES_FIXED.md 확인 (R2 / R7 / R14)
✅ STATUS_LATEST.md 확인 (기억88)
✅ CURSOR_RULES.md 확인 (§2)
```

---

## 1. 프론트엔드 (버튼 → API)

### 1-A. 버튼 요소 (`index.html`)

```93:93:d:\3kweon\app\static\index.html
              <button type="button" class="btn btn-primary" id="btnPredict">두뇌 예측 실행</button>
```

- **id**: `btnPredict`
- **텍스트**: 「두뇌 예측 실행」
- **회차 입력**: `#drawSelect`, `#drawDirectInput` → `getDrawNoFromUi()`가 `#drawSelect` 값 사용

### 1-B. 클릭 핸들러 (`lotto4.js`)

```1573:1598:d:\3kweon\app\static\js\lotto4.js
  async function runPredict() {
    const st = document.getElementById('actionStatus');
    const d = getDrawNoFromUi();
    if (!d || d < 1) {
      setStatus(st, '회차를 선택하세요.', false);
      return;
    }
    setStatus(st, '예측 중…', true);
    try {
      const data = await fetchJsonHandled(API + '/predict/' + d, { method: 'POST' });
      // ... error/reason 처리 ...
      _lastDraw = d;
      await loadBrainMeta();
      const n = await refreshPredictionsFromServer(d);
      setStatus(st, d + '회 예측 완료 · ' + n + '행 표시', true);
    } catch (e) {
      setStatus(st, '실패: ' + (e && e.message), false);
    }
  }
```

**바인딩**:

```1851:1851:d:\3kweon\app\static\js\lotto4.js
    document.getElementById('btnPredict')?.addEventListener('click', runPredict);
```

### 1-C. API 호출 raw

| 항목 | 값 |
|------|-----|
| URL | `POST /api/lotto4/v13/predict/{d}` (`API = '/api/lotto4/v13'`) |
| body | **없음** (path param만) |
| POST 후 | `GET /api/lotto4/v13/predictions/draw/{d}` → `_lastRows` 갱신 |

```1538:1548:d:\3kweon\app\static\js\lotto4.js
  async function refreshPredictionsFromServer(drawNo) {
    const d = Number(drawNo);
    const data = await fetchJsonHandled(API + '/predictions/draw/' + d);
    const rows = data.predictions || (Array.isArray(data) ? data : []);
    // ...
    _lastRows = rows;
    _lastDraw = d;
    lotto4SwitchBrainTab(_currentBrain);
```

**1줄 해석**: 버튼 → POST predict → GET predictions/draw → 메모리 `_lastRows`에 **해당 회차 전체 행** 적재.

---

## 2. 백엔드 (API → 번호 생성)

### 2-A. 라우트

```41:45:d:\3kweon\app\lotto4\v13_routes.py
@router.post("/predict/{target_draw_no}")
async def api_predict_v13(target_draw_no: int):
    from app.lotto4.v13_engine_v2 import run_prediction_v13

    return run_prediction_v13(target_draw_no)
```

### 2-B. 엔진 `run_prediction_v13`

**캐시 분기** (해당 회차 DB 행 ≥ 35이면 **재생성 안 함**):

```111:146:d:\3kweon\app\lotto4\v13_engine_v2.py
def _is_cached(conn: Any, target_draw_no: int) -> bool:
    # ...
    return int(row["c"] or 0) >= V13_V2_TOTAL_ROWS

    if cached:
        _score_predictions(target_draw_no)
        return {
            "status": "cached",
            ...
        }
```

**7뇌 전부 실행** (Hidden 2개 제외):

```157:184:d:\3kweon\app\lotto4\v13_engine_v2.py
    for tag, mod_path in BRAIN_REGISTRY.items():
        if tag in V13_V2_HIDDEN_BRAINS:
            continue
        predict_fn = _load_predict(mod_path)
        raw = predict_fn(target_draw_no, db_path)
        # v13_seq 격리 시 placeholder 5세트
        for i, nums in enumerate(sets):
            normalized.append({ "nums": nums, "brain_tag": tag, ... })
```

**BRAIN_REGISTRY 9뇌** — predict 루프에서 **skip**: `v13_cdm`, `v13_cond_prob`  
**실행 7뇌**: struct, seq(격리 placeholder), gap, diversity, evolution, ev, ensemble

**저장 행 수**:

```78:79:d:\3kweon\app\lotto4\v13_weights_v2.py
SETS_PER_BRAIN_V2 = 5
V13_V2_TOTAL_ROWS = len(V13_V2_PREDICT_ORDER) * SETS_PER_BRAIN_V2  # 7×5=35
```

POST 시 기존 해당 회차 행 **DELETE 후 INSERT 35행** (`matched_count=-1` → 채점 시 갱신).

**1줄 해석**: 특정 뇌만이 아니라 **활성 7뇌 × 5세트 = 35행** DB 저장. ensemble은 `hyena_commander.predict` 경유.

---

## 3. 화면 표시 (사용자에게 보이는 번호)

### 3-A. 동시 표시 범위

- DB/API: **35행 전체** fetch → `_lastRows`
- **화면 `#setsHost`**: **현재 선택 탭 뇌 1개의 5세트만**

```503:505:d:\3kweon\app\static\js\lotto4.js
  function rowsForCurrentBrain() {
    return _lastRows.filter((r) => canonicalBrainTag(r.brain_tag) === _currentBrain);
  }
```

```598:655:d:\3kweon\app\static\js\lotto4.js
  function renderSetsForBrain() {
    const host = document.getElementById('setsHost');
    const list = rowsForCurrentBrain();
    // ...
    list.forEach((r, i) => { /* 5세트 볼 렌더 */ });
    host.innerHTML = html;
  }
```

### 3-B. 기본 탭 (최초 표시 뇌)

```59:59:d:\3kweon\app\static\js\lotto4.js
  let _currentBrain = 'v13_struct';
```

→ **최초 화면 = v13_struct 5세트** (ensemble 5세트 아님).

### 3-C. 탭 전환

- `BRAIN_ORDER`: struct, seq, diversity, evolution, gap, ev, ensemble (7탭)
- `lotto4SwitchBrainTab(tag)` → `renderSetsForBrain()` — 탭마다 **5세트**

### 3-D. 카톡 복사 (부가 출력)

```1724:1768:d:\3kweon\app\static\js\lotto4.js
  function getKakaoBrainOrder() {
    // 엘리트 OFF → BRAIN_ORDER 7뇌 전부
    if (!on) return BRAIN_ORDER.slice();
  }
  // buildKakaoText: 뇌별 5세트 × 7뇌 = 최대 35세트 텍스트
```

**1줄 해석**: 화면 = **1뇌 5세트**. 카톡 = **7뇌 35세트** (엘리트 필터 시 subset). **대표 1세트 로직 없음**.

---

## 4. 주 1회 제약

| 검색 | 결과 |
|------|------|
| `1주`, `weekly`, `주1`, `once per` in `app/lotto4/` | **매치 없음** |

**존재하는 제약**:

| 종류 | 위치 | 로직 |
|------|------|------|
| **회차별 DB 캐시** | `_is_cached()` | 해당 `target_draw_no`에 v13 행 **≥35** 이면 POST predict **재생성 skip**, 채점만 |
| **회차 UI 상한** | `rebuildDrawOptions(_nextDrawNo)` | `#drawSelect` max = **다음 추첨 회차** (주 1회 강제 아님) |

**결론**: **「1주 1회」를 강제하는 코드 없음.** 같은 회차에 버튼 재클릭 시 cached → **번호 변경 없음**. 다른 회차 선택 시 **매번 새 predict 가능**.

---

## 결론

| 질문 | 답 |
|------|-----|
| **「예측 버튼」이 주는 번호의 정체** | 선택 회차에 대해 **7뇌×5세트=35조합** DB 생성. 화면은 **현재 탭 1뇌 5세트**(기본 **v13_struct**). 카톡은 7뇌 35세트. |
| **교체 대상 (프론트)** | `runPredict()` — `POST /predict/{d}` 호출부 |
| **교체 대상 (백엔드)** | `api_predict_v13` → `run_prediction_v13()` (7뇌 루프 전체) |
| **교체 대상 (표시)** | `refreshPredictionsFromServer` + `renderSetsForBrain` (정직추첨 5세트로 `_lastRows`/렌더 단순화 가능) |
| **정직추첨 시 유지 가능** | `GET /predictions/draw/{n}` 스키마, `#setsHost` 렌더, 회차 UI — **predict 생성부만 교체** |

**작업 완료**
