# v12_snake 가중치 DB 연동 패치 보고서

- **저장 시각(파일명 기준)**: 2026-05-03 01:30  
- **작업 루트**: `D:/MONEY lol/My_Library`  
- **금지 준수**: `app/lotto/`, `app/lotto2/` **미수정** · 1군 `lotto_predictions` **미수정** · `target_draw_no=1222` **미사용** · 신규 테이블 **미생성** (`lotto_brain_weights_army3`만 읽기)

---

## STEP 0 — SHA-256 사전검증 (31파일, 패치 후 기준)

- **기준표**: 1430 보고서 `| app/... |` 29행 + `v12_models.py`(7C8E…) + `v12_fusion_v5.py`(493A…) 복합(기억28·0000 보고서와 동일).
- **패치 적용 직후 확인**: `app/lotto3/v12_snake.py`를 **제외**한 30경로는 모두 기대 해시와 **일치** (`excluding_snake_mismatch=0`).  
- **참고**: 전체 31경로를 `v12_snake` 포함해 검증하면 **의도적으로** `v12_snake.py`만 구 해시(`A4257596…`)와 달라짐(STEP 4 신규 해시).

---

## STEP 1 — `v12_snake.py` 수정 (파일 1개만 변경)

### 1-A) `_V11_SNAKE_WEIGHTS` 하드코딩 dict 삭제

- **삭제**: 기존 21–29행 부근 `_V11_SNAKE_WEIGHTS` dict 전체.

### 1-B) `_load_snake_weights()` 추가

- **위치**: 모듈 상단 `logger` 정의 직후(구 22행 부근).
- **구현**: `get_v12_brain_weights()` 호출 → 비어 있으면 `{}`, 아니면 그대로 반환.

### 1-C) consensus 누적부 변경

- `v12_snake_predict_sets`에서 **조기 return 이후 1회** `snake_weights = _load_snake_weights()` 호출.
- 루프 내 `w = float(snake_weights.get(tag, 1.0))` 로 변경.

### 1-D) 죽은 키·누락 태그

- **`v12_markov`**: 하드코딩 dict 제거로 **키 자체 소멸**.
- **`v12_run` / `v12_offset`**: `get_v12_brain_weights()`가 `V11_BRAINS`·시드 병합으로 **DB 행 포함** → snake 합의에 자동 반영.

### 기타

- 모듈 docstring에 **DB 연동** 한 줄 보강.
- 미사용 지역변수 `weights`(충원 루프) **삭제**(린터 unused 제거).

---

## STEP 2 — `get_v12_brain_weights` 존재 확인

- **`app/lotto3/v12_models.py` 126–147행**에 이미 정의됨.
- **추가 구현 없음** (요청한 `get_lotto_db` 기반 대체 코드 **불필요**).

---

## STEP 3 — 검증

### 3-A) 앱 import

```text
app OK
```

명령: `python -c "from app.main import app; print('app OK')"` (exit 0).

### 3-B) `_load_snake_weights()` → 7키 출력

```text
keys 7
v12_contrarian 12.464
v12_fusion 107.633
v12_hyena 257.382
v12_lstm 83.864
v12_offset 5.109
v12_run 5.261
v12_stat 8.042
```

형·동생 기대치(≈257, ≈108, ≈84, ≈12, ≈8, ≈5, ≈5)와 **정합**.

### 3-C) `run_prediction_v12(1000)`

- **결과**: `cached`, `v12_sets=40` — 해당 회차는 이미 `lotto_predictions_army3`에 40행 존재하여 **캐시 단락**.
- **snake 경로 직접 검증**: 동일 프로세스에서 `v12_snake_predict_sets`(가짜 30셋트·`target_draw_no=1000`) 호출 → **5셋 생성**, `brain_tag=v12_snake` 확인.

---

## STEP 4 — SHA-256 사후검증

- **30파일** (`v12_snake.py` 제외): 복합 기준과 **전부 일치**.
- **`app/lotto3/v12_snake.py`만** 해시 변경.
- **`v12_models.py`**: **변경 없음** → 기대 해시 `7C8ECDEF…` 유지.

| 파일 | 이전(1430 표) | 이후(패치 반영) |
|------|----------------|-----------------|
| `app/lotto3/v12_snake.py` | `A4257596916B6231B2A536E72E5CBCEC7492B1924FF78168E082EB3912CF6EB2` | **`367BE96CCA68EFAE643D9BCD4210118EE817611F59F634FD302D19D432A4D322`** |

※ unused `weights` 제거 1줄 반영 후 최종 해시.

---

## STEP 5 — 변경 요약

### 변경 파일

| 파일 | 변경 |
|------|------|
| `app/lotto3/v12_snake.py` | **단일 파일** 전체 구조 유지, 가중치 소스만 DB 연동 |

### 핵심 diff (요지)

**삭제 (하드코딩)**

```python
# 가중치 (1군 검증값 매핑: ...)
_V11_SNAKE_WEIGHTS: dict[str, float] = {
    "v12_stat":   5.0,
    "v12_markov": 5.0,
    ...
}
```

**추가**

```python
def _load_snake_weights() -> dict[str, float]:
    """Hedge 가중치 테이블에서 7뇌 current_weight를 읽어 snake consensus 가중치로 사용."""
    from app.lotto3.v12_models import get_v12_brain_weights

    weights = get_v12_brain_weights()
    if not weights:
        return {}
    return weights
```

**변경 (`v12_snake_predict_sets`)**

```python
# 추가 (조기 return 다음)
snake_weights = _load_snake_weights()

# 기존
w = float(_V11_SNAKE_WEIGHTS.get(tag, 1.0))
# 변경
w = float(snake_weights.get(tag, 1.0))
```

**삭제 (린트)**

```python
weights = [consensus[n] for n in pool]  # 미사용 → 제거
```

### 새 SHA-256 (`v12_snake.py` 단일)

`367BE96CCA68EFAE643D9BCD4210118EE817611F59F634FD302D19D432A4D322`

---

## 미해결 / 확인 필요

- 없음(본 패치 범위 내).

---

*검증 DB: `data/lotto.db` — 읽기 전용 SELECT 외 변경은 `run_prediction_v12` 캐시 경로만 사용(1222 미사용).*
