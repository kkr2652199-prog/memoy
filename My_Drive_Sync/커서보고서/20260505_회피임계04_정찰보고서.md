# 회피 임계 0.4 정책 — 코드 정찰 보고서

- **일자**: 2026-05-05 KST  
- **모드**: READ-ONLY (코드 수정 0줄)  
- **선행**: `동생기억/20260505_기억35_v1.md`, `커서보고서/20260505_패치H_하위합의제외_확정_보고서.md`  
- **대상 코드베이스**: `My_Library/app/lotto3/` (8뇌 = `v12_stat`, `v12_run`, `v12_offset`, `v12_contrarian`, `v12_lstm`, `v12_fusion`, `v12_hyena`, `v12_snake`)

---

## SHA256 사전검증 (31파일)

**실행**: `My_Library` 기준 상대경로 31개 파일 전체 SHA256 계산 → 사용자 지정 해시와 일치 시 `SHA_OK_31`.

**결과**: **일치 (SHA_OK_31)**

| 키 파일 (사용자 지시) | 기대 SHA256 (접두) | 비고 |
|----------------------|-------------------|------|
| `app/lotto3/v12_engine.py` | `E2C55B61…66BC7C` | 패치 H |
| `app/lotto3/v12_snake.py` | `692A7680…2CBF65` | 패치 H |
| `app/lotto3/v12_models.py` | `EBDE761B…` (전문 일치) | 패치 G |
| `app/lotto3/v12_fusion_v5.py` | `493AFBC9…` (전문 일치) | — |
| 나머지 27경로 | 31복합 중 동일 | — |

---

## Q1. 3군 8뇌 중 Jaccard 기반 회피가 있는 뇌

| 뇌 (`brain_tag`) | Jaccard 회피 | 임계값·비교 대상 | 근거 |
|------------------|-------------|------------------|------|
| **v12_snake** | **있음** | **0.4**, 1군 6뇌 `lotto_predictions` 30셋트 | 아래 인용 |
| **v12_fusion** | **있음** | **0.6**, 1군 `fusion` 태그만 | 아래 인용 |
| **v12_contrarian** | **있음** | **0.4**, 1군 6뇌 + 2군 `v10d_%`/`v11_%` 예측 | 아래 인용 |
| v12_stat, v12_run, v12_offset, v12_lstm, v12_hyena | **없음** (Jaccard 필터 없음) | — | `rg` + 엔진/개별 predict 확인 |

**v12_snake** — `v12_snake.py` `v12_snake_predict_sets` 내부, `threshold=0.4` 고정 호출:

```44:62:d:\MONEY lol\My_Library\app\lotto3\v12_snake.py
def _is_diff_from_army1(
    combo: list[int],
    army1_sets: list[set[int]],
    threshold: float = 0.4,
) -> bool:
    """1군 셋트 중 단 하나라도 Jaccard >= threshold 면 탈락.
    ...
    """
    if not army1_sets:
        return True
    s = set(combo)
    for a1 in army1_sets:
        inter = len(s & a1)
        uni = len(s | a1)
        jac = inter / uni if uni else 0.0
        if jac >= threshold:
            return False
    return True
```

```151:153:d:\MONEY lol\My_Library\app\lotto3\v12_snake.py
        # Jaccard < 0.4 검증
        if not _is_diff_from_army1(cand, army1_sets, threshold=0.4):
            continue
```

**v12_fusion (V5)** — `v12_fusion_v5.py` 상수 0.6 + 판정 함수:

```31:33:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py
# V12-D: 1군 fusion 회피 (Snake와 같은 패턴, 약한 회피)
_ARMY1_FUSION_TAG = "fusion"
_FUSION_JACCARD_THRESHOLD = 0.6   # 시뮬레이션 검증값 (0.5 너무 강함, 0.7 효과 동일)
```

```57:72:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py
def _v12_is_diff_from_army1_fusion(
    combo,
    army1_fusion_sets: list[set[int]],
    threshold: float = _FUSION_JACCARD_THRESHOLD,
) -> bool:
    """1군 fusion 셋트와 Jaccard >= threshold면 탈락."""
    ...
        if jac >= threshold:
            return False
    return True
```

**v12_contrarian** — `predict_contrarian.py`:

```183:193:d:\MONEY lol\My_Library\app\lotto3\predict_contrarian.py
def _is_diff_from_armies(combo: list[int], army_sets: list[set[int]], threshold: float = 0.4) -> bool:
    if not army_sets:
        return True
    s = set(combo)
    for a in army_sets:
        inter = len(s & a)
        uni = len(s | a)
        jac = inter / uni if uni else 0.0
        if jac >= threshold:
            return False
    return True
```

```266:267:d:\MONEY lol\My_Library\app\lotto3\predict_contrarian.py
        if not _is_diff_from_armies(cand, army_sets, 0.4):
            continue
```

**v12_hyena** — Jaccard 회피 없음: `tier1_filter`·중복 제거·합의 점수만 사용 (`_v12_build_sets_from_ranked`, 아래 Q3).

---

## Q2. `v12_snake.py` — Jaccard 0.4 실적용 여부

- **문서/모듈 독스트링**: `Jaccard 임계값: 0.4`, `threshold=0.4` 기본값 명시.
- **실행 경로**: 메인 루프와 충원 루프 모두 **`threshold=0.4`를 명시 인자로 전달** → **0.4가 적용됨** (다른 숫자로 덮어쓰지 않음).

위 Q1의 `151:153`행 및 `184:185`행(`if not _is_diff_from_army1(cand, army1_sets, threshold=0.4)`)과 동일 근거.

**`SUMMARY/RULES_FIXED.md`**: 검증 룰에 snake&lt;0.4 명시.

```87:87:d:\MONEY lol\My_Drive_Sync\SUMMARY\RULES_FIXED.md
2\. Jaccard 임계값: 2군 snake<0.4 / fusion<0.6 / 3군 0.5
```

*(참고: 동일 줄의 「3군 0.5」와 코드 내 0.4/0.6은 별도 정합 필요.)*

---

## Q3. `v12_hyena` (`v12_engine.py`) — 최종 선택 시 직전 당첨과 유사도 회피?

**결론: 없음.** 5005 전수 평가 후 `_v12_build_sets_from_ranked`에서 **`tier1_filter`**, **`used` 중복 방지**, 상위5 우회만 있으며 **당첨번호·Jaccard 비교 없음**.

```149:220:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
def _v12_build_sets_from_ranked(
    ranked: list[tuple[tuple[int, ...], float]],
    all_predictions: list[dict],
    n_sets: int,
) -> list[dict]:
    """1군 _build_sets_from_ranked: tier1 + 상위5 우회 + top50 가중 30회."""
    ...
    from app.lotto.filters import tier1_filter
    ...
    for cmb, t in ranked:
        nlist = list(cmb)
        if tier1_filter(nlist):
            chosen, t1 = cmb, t
            break
    ...
            if not tier1_filter(nlist):
                continue
```

`_v12_hyena_predict`는 합의→풀→5005→위 빌더만 호출:

```252:267:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
def _v12_hyena_predict(all_predictions: list[dict], n_sets: int = 5) -> list[dict]:
    """V12-A: 1군 hyena 5005 전수 파이프라인 미러링."""
    ...
        ranked = _v12_evaluate_combinations(pool_nums, consensus)
        if not ranked:
            return []
        return _v12_build_sets_from_ranked(ranked, all_predictions, n_sets)
```

---

## Q4. `v12_fusion_v5.py` — 1군 fusion 대비 회피 (Jaccard &lt; 0.6)

- **상수**: `_FUSION_JACCARD_THRESHOLD = 0.6` (위 Q1 인용).
- **비교 대상**: `_v12_load_army1_fusion_sets` → `lotto_predictions`에서 `brain_tag = 'fusion'`만 로드 (위 파일 `36:54`행).
- **적용 위치**: Top-K Greedy 1세트, 가중 random, bypass, fill 단계 전반에서 `_v12_is_diff_from_army1_fusion` 호출 (예: `269:311`행대).

```269:311:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py
    # V12-D: 1군 fusion 회피 비교 대상 로드
    army1_fusion_sets = _v12_load_army1_fusion_sets(target_draw_no)

    # 5-1) Top-K Greedy 1세트 (1군과 너무 비슷하면 변형)
    top_k = _v12_top_k_greedy(final_pmf, k=6)
    if not _v12_is_diff_from_army1_fusion(top_k, army1_fusion_sets):
        ...
    ...
            if not _v12_is_diff_from_army1_fusion(cand, army1_fusion_sets):
                continue
```

---

## Q5. 회피 임계 0.4를 「직전 N회 당첨」 기준으로 8뇌에 적용하려면 (수정 후보·READ-ONLY 목록)

**현재 코드의 Jaccard 회피는 모두 「타 군/타 뇌 예측 셋트」 대상**이며, **지시서의 「직전 N회 당첨번호」와는 축이 다름.**

| 우선순위 | 파일 | 함수·영역 (대략) | 예상 작업 |
|---------|------|------------------|-----------|
| 공통 | 신규 또는 `v12_models.py` 등 | `lotto_draws`에서 `draw_no < target` 조건으로 **최근 N회 당첨 6개 집합** 로드 + `_jaccard_ok(combo, winning_sets, 0.4)` | **신규 수십 줄** |
| v12_stat | `predict_stat.py` | 세트 생성 루프에 필터 1줄+재시도 | 소량 |
| v12_run | `predict_run.py` | 동일 | 소량 |
| v12_offset | `predict_offset.py` | 동일 | 소량 |
| v12_lstm | `predict_lstm.py` | 동일 | 소량 |
| v12_contrarian | `predict_contrarian.py` | 기존 `_is_diff_from_armies` **추가**로 당첨 집합 검사 (또는 통합 헬퍼) | 중간 |
| v12_fusion | `v12_fusion_v5.py` | `_v12_is_diff_from_army1_fusion`와 병행 또는 통합 (당첨 회피 단계) | 중간 |
| v12_hyena | `v12_engine.py` | `_v12_build_sets_from_ranked` 또는 그 직전에서 후보 제외/재추출 | 중간~다소 |
| v12_snake | `v12_snake.py` | `_is_diff_from_army1` 외 **당첨 N회** 검사 추가 | 소량~중간 |

**예상 총량**: 공통 헬퍼 ~40–80줄 + 뇌당 수 줄 ~ 수십 줄 (중복 제거 시 단축).

---

## Q6. DB에서 직전 당첨(학습/채점) 관련 함수

### `get_v12_training_draws`

- **위치**: `v12_models.py` `get_v12_training_draws`  
- **역할**: **직전 N회만이 아니라**, (1) 1군 미당첨 회차 전체 집합 + (2) **최근 50회차** `lotto_draws` 행을 합친 학습 리스트 (중복 제거 후 `draw_no` 정렬).

```52:105:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
def get_v12_training_draws(target_draw_no: int) -> list[dict]:
    """V11 학습 데이터: 1군 미당첨 회차 + 최근 50회차 (중복 제거).

    컷닝 0%: target_draw_no 미만만.
    """
    ...
        recent_rows = conn.execute(
            """
            SELECT * FROM lotto_draws
            WHERE draw_no < ?
            ORDER BY draw_no DESC
            LIMIT ?
            """,
            (target_draw_no, V11_RECENT_BOOST),
        ).fetchall()
    ...
        result.sort(key=lambda x: x["draw_no"])
        return result
```

**반환값**: `list[dict]` — `lotto_draws` Row를 dict로 변환한 항목들 (`num1`…`num6` 등 포함).

### 채점 시 단일 회차 당첨 조회 (참고)

- `_score_v12_predictions`에서 **해당 `target_draw_no`의 실제 당첨 한 줄**만 조회 (회피 정책용 「N회」 로더는 아님).

```404:407:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
        actual = conn.execute(
            "SELECT num1, num2, num3, num4, num5, num6, bonus FROM lotto_draws WHERE draw_no = ?",
            (target_draw_no,),
        ).fetchone()
```

**정리**: 「직전 N회 당첨만」 전용 공개 함수는 **현재 없음** — `get_v12_training_draws`의 recent 구간을 재사용하거나 `lotto_draws`에 `ORDER BY draw_no DESC LIMIT N` 쿼리를 **신규**로 두는 형태가 자연스러움.

---

## 정찰 결론

- **Jaccard 회피가 실제로 들어간 8뇌 중 3개**: **snake(0.4·1군 예측)**, **fusion V5(0.6·1군 fusion만)**, **contrarian(0.4·1·2군 예측)**. **hyena·나머지 기저뇌는 Jaccard 회피 없음.**  
- **직전 당첨번호와의 Jaccard 회피는 어디에도 미구현.**  
- **SHA 31파일** 사용자 지정값과 **전부 일치** 확인됨.

### 패치 설계 권고 (1줄)

**공통 `lotto_draws` N회 로더 + Jaccard≥0.4 필터를 두고, 예측 세트를 만드는 각 뇌 루프(및 hyena·snake·fusion 후보 선정)에 동일하게 끼워 넣되, 기존 「타군 예측」 회피와 논리적으로 합성할지(AND) 우선순위를 먼저 고정할 것.**
