# 20260726 — GitHub 진행 분석 + 1군 정밀분석 문제점

**작업일:** 2026-07-26  
**범위:** READ-ONLY (app/lotto 코드 수정 0건)  
**SSOT:** https://github.com/kkr2652199-prog/memoy · main · `c8cfefd`  
**선행:** 정직화 15항 `2713250` · FINDINGS F-A~F-H `1657e59`

---

## 0. R28 시작 확인

| 문서 | 결과 |
|------|------|
| BOOT.md (memoy HEAD) | 확인 — 로컬이 kweon으로 덮여 있었음 → HEAD 복원 |
| FINDINGS.md | F-A~F-H OPEN → 본 세션 코드 재검증 |
| RULES_FIXED.md | 확인 (본문 미수정) |
| STATUS_LATEST.md | 기억108 → 본 세션 갱신 |
| CURSOR_RULES.md | 확인 (본문 미수정) |

---

## 1. GitHub(memoy) 작업 진행 요약

| 시기 | SHA | 내용 | 성격 |
|------|-----|------|------|
| 07-26 | `c8cfefd` | BOOT 현재스레드 반영 | 운영 |
| 07-26 | `962b16a` | BOUNDARY.md + gitignore + 가드 | 운영 |
| 07-26 | `0c4763c` | check_finish memoy 경로만 | 운영 |
| 07-26 | `1657e59` | BOOT+FINDINGS+hooks 인프라 | 운영 |
| 07-26 | `2713250` | **1군 정직화 15항** (결정론 top-k) | **코드** |
| 07-18 | `1c56deb`~`14eda5a` | 정밀현황·postmortem·랜덤vs기대값 | READ-ONLY 분석 |
| 07-18 | `d8903f8` | R34 memoy SSOT 확정 | 운영 |
| 07-10 | `2b30af5`/`1d969ef` | STEP1 WF · STEP2 eta | READ-ONLY |

**판정:** 07-26 이후 1군 **코드 변경은 `2713250` 1건**. 이후는 인프라·경계 문서. 열린 결함은 FINDINGS에 등록만 되어 있고 **패치 미착수**.

**로컬 위험 (본 세션 발견·조치):**
- 작업트리 `BOOT.md`/`FINDINGS.md`/`STATUS_LATEST.md`가 **kweon(K-*) 내용으로 덮여 있었음** (R34 위반 위험)
- `git checkout HEAD --` 로 memoy 원본 복원 완료
- 미추적 `BOOT.txt`/`FINDINGS.txt`/`RESUME_HERE.*` 잔존 — 내용 확인 후 삭제·무시 필요

---

## 2. 1군 현재 구조 (코드 실측)

**활성:** stat · markov · llm(HOLD→stat대체) · lstm · fusion · lead1  
**OFF:** hyena · miss_analysis · snake · cluster · trap/hit · N+1 · postmortem훅  
**정직화 플래그:** `USE_DETERMINISTIC_*=True`, 나머지 ENABLE_*=False (`honesty_flags.py`)

### DB 실측 (20260726)

| 항목 | 값 |
|------|-----|
| lotto_draws MAX | **1234** (BOOT 구버전 1232와 불일치) |
| pred max target | 1235 |
| brain_weights | lstm 33.51 · hyena 25.41 · llm 7.51 · stat 5.10 · markov 3.43 (last=1234) |
| miss_analysis / snake 잔존행 | 각 **6130** |
| llm_fallback 잔존 | 4 |
| target=1235 conf | lstm avg 99.7 · lead1 avg **31.0** |

---

## 3. FINDINGS F-A~F-H 재검증

| ID | 판정 | 핵심 근거 |
|----|------|-----------|
| F-A | **CONFIRMED** | lstm conf≈80~99, lead1=score×10→avg 31 → top5 구조적 배제. brain7는 top5 이후 호출 |
| F-B | **CONFIRMED** | `predict_entropy.py` entropy_factor가 docstring과 반대 |
| F-C | **CONFIRMED** | `deterministic_sets` top-k → 세트 간 1번호 차이·union 붕괴 |
| F-D | **CONFIRMED** | `LOTTO_LLM_HOLD=True` → tag=`llm` INSERT → union k 부풀림 |
| F-E | **CONFIRMED** | eta=1.5 · SEED llm/lstm>stat/markov · DB 가중치 lstm 33.5≫stat 5.1 |
| F-F | **CONFIRMED** | uniform+번호순 tiebreak → pool=1~18 고정 |
| F-G | **CONFIRMED** | 벡터/예측 이중 로직·피드백 시점 분기 (현재 trap OFF라 잠재) |
| F-H | **CONFIRMED** (tier1 이중계산만 PARTIAL) | backtest가 운영가중치 변경 · 캐시 구버전 · miss/snake NOT IN·DB 잔존 |

활성 경로의 `random.choices`는 플래그로 우회됨. 레거시 분기는 코드에 잔존.

---

## 4. 신규 발견 (F-I~F-L 등록)

| ID | 심각도 | 요약 |
|----|--------|------|
| F-I | 높음 | hyena OFF인데 `_ARMY1_AUTO_SIX`에 hyena 필수 → readiness 영구 실패 가능 (`data_service.py:232`) |
| F-J | 중 | 캐시 path top5는 miss/snake 포함 가능, fresh는 `NOT IN` → 경로별 top5 불일치 |
| F-K | 중 | backtest best_match가 brain_tag 필터 없음 → 잔존 miss/snake가 성적 부풀림 가능 |
| F-L | 중 | BOOT/STATUS MAX=1232 vs DB MAX=1234 · 가중치 last=1234 문서 미반영 |

기타 (ID 미부여, 참고):
- fusion이 HOLD llm 벡터를 별도 뇌로 가중합
- `llm_fallback` 태그 쿼리 병행 잔존

---

## 5. 우선순위 제안 (형 결정용 · 패치 금지)

패치는 **「F-? VERIFYING」** ID 지시 후에만.

1. **F-E + F-D** — 가중치·합의 오염 (실력축 왜곡의 뿌리)
2. **F-A** — confidence 스케일 통일 (lead1 가시성)
3. **F-C / F-F** — 결정론 top-k 부작용 (다양성·lstm 저번호 고정)
4. **F-I / F-J / F-K** — 정직화 후유증 (hyena readiness·캐시·백테)
5. **F-B / F-G / F-H 잔여** — 보정·이중화·정리

---

## 6. 검증 명령

```text
git log --oneline --grep="1군" -10
python -c "import sqlite3; c=sqlite3.connect('My_Library/data/lotto.db'); print(c.execute('select max(draw_no) from lotto_draws').fetchone())"
# F-A~F-H: 코드 정적 대조 (본 보고서 §3) — app/lotto 미수정
```

---

## 7. 미해결

- F-A~F-L 전부 OPEN · 패치 대기
- 미추적 kweon형 `SUMMARY/*.txt` / `RESUME_HERE.*` 정리 여부 형 결정
- commit/push: 형 지시 대기
