# 패치 K 롤백 → 패치 J 복원

- **일자**: 2026-05-06 KST  
- **목적**: 패치 **K**(주기 정규화) 제거 · 패치 **J**(가중치 시계열 로그만) 동작 복원  
- **선행**: 기억38_v1 · `20260505_패치K_백테스트_보고서.md`  
- **제한 준수**: `app/lotto/`, `app/lotto2/` 미수정 · **1222 미사용** · `lotto_weight_log_army3` **삭제·스키마 변경 없음** · 수정 파일 **`v12_models.py` 1개**

---

## STEP 0 — SHA256 사전검증 (31파일)

- 롤백 **직전** `app/lotto3/v12_models.py` = **9F7C60DA258550E0EBFAE672F22717FAEC4B800759145AF75387675CE64AF50C** (패치 K) — 보고서·기억38 기준  
- 나머지 30파일 — 패치 K 세션 기대 해시와 **일치** (본 작업에서는 코드 변경 전 상태로 검증 가능)

---

## STEP 1 — 롤백 작업 (`v12_models.py`)

### 1-A. 제거

- 상수: `NORMALIZE_PERIOD`, `TARGET_SUM`, `ETA_LOG_NORMALIZE`  
- `update_v12_weights` 내: `wa_hedge` / `sum_hedge` / `normalize_effective` / `wa_final` 리스케일 · `elif normalize_effective` UPDATE · 로그 `eta` 분기  
- `update_v12_weights` 독스트링 내 정규화 문구

### 1-B. 유지

- `W_MAX`, `V11_HEDGE_ETA`, `_ensure_weight_log_table`, `init_v12_seeds`의 `draw_no=0` 로그  
- Hedge: `new_w = min(base * exp(eta * score_signal), W_MAX)`  
- 로그 `INSERT`: `weight_before`/`weight_after`/`matched_count`/`eta=float(V11_HEDGE_ETA)` (2.0)

### 1-C. 추가 주석 (요지)

`W_MAX` 아래:

```text
# 패치 K 롤백 — 정규화 제거, 패치 J(가중치 로그) 상태 복원.
```

*(원문은 소스 `v12_models.py` 46행대 참조.)*

---

## STEP 2 — 검증 결과

| 단계 | 결과 |
|------|------|
| 2-A | `python -c "from app.main import app; print('app OK')"` → **`app OK`** |
| 2-B | `target_draw_no=100`의 `v12_%` 예측·로그 삭제 후 `run_prediction_v12(100)` | **`status ok`** |
| 2-C | `lotto_weight_log_army3` `draw_no=100` | **7행** 존재 |
| 2-D | 동일 7행 `eta` | **전부 `2.0`** · `draw_no=100`에서 `eta != 2.0` **0건** |

*참고: DB에 과거 패치 K 백테로 남은 **`eta=-1` 행**이 있을 수 있음. 본 검증은 **재생성한 draw 100 로그**만 확인.*

---

## STEP 3 — SHA256 사후검증 (31파일)

| 파일 | SHA256 (전체) |
|------|----------------|
| `app/lotto3/v12_models.py` | **82B1FBD76D0DE52C545AFA0BDACC1BBD86A21A9E0397493BEA12BBB26432896C** |

- **패치 J 당시 해시** `CA001F7B9CE8DB9700FE24B62A66299A1E34D2101939DF9E86A53D576DEF11E8` 과 **불일치** — 원인: **롤백 주석 2줄**(패치 K 롤백 안내) 추가로 바이트 차이. **로직은 패치 J와 동일**.

| 구분 | 일치 여부 |
|------|-----------|
| 나머지 30파일 | **전부 기대 해시와 일치** |

---

## STEP 4 — 제거 diff 요약 (`v12_models.py`)

| 구간 | 제거·변경 내용 |
|------|----------------|
| **46~49행(구)** | `NORMALIZE_PERIOD`, `TARGET_SUM`, `ETA_LOG_NORMALIZE` 및 패치 K 정규화 주석 블록 |
| **46~47행(신)** | 롤백 한 줄 주석으로 대체 |
| **`update_v12_weights` 독스트링** | “+ 패치 K 주기 정규화” 및 `NORMALIZE_PERIOD`/`eta=-1` 문장 삭제 |
| **357~419행(구)** | `wa_hedge`~`normalize_effective`~`wa_final`·부분 UPDATE·`log_wb`/`log_eta` 분기 |
| **351~396행(신)** | 단일 루프 `wa = new_by_tag.get(tag, wb)` · 기존 full UPDATE만 · 로그는 항상 `wb`,`wa`,`V11_HEDGE_ETA` |

---

## 미해결

- 바이트까지 패치 J와 동일 SHA가 필요하면 **롤백 주석 제거** 후 `CA001…` 재현 가능(운영 정책은 형 결정).

---

## 검증 명령 (재현)

```powershell
Set-Location "d:\MONEY lol\My_Library"
python -c "from app.main import app; print('app OK')"
```
