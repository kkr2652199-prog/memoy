# 4군 뇌 튜닝 2단계 — v13_ensemble 보고서

**날짜**: 2026-05-13  
**대상**: `d:\3kweon`  
**제한**: My_Library 미접근

---

## TASK별 변경 요약

| TASK | 파일 | 내용 |
|------|------|------|
| 1 | `app/lotto4/brains/ensemble.py` | **전면 교체**: 7하위 뇌 `importlib`·`predict` 수집 → `lotto_brain_weights_army4` 가중 투표 → 상위 **15** 후보 번호 → `generate_sets_with_filters`로 5세트. **numpy 미사용**. |
| 2 | `app/lotto4/v13_engine_v2.py` | `BRAIN_REGISTRY` 순서: `v13_ensemble`을 **맨 마지막**(역발상 뒤). |
| 2 | `app/lotto4/v13_weights_v2.py` | `V13_V2_BRAIN_ORDER` 동일 순서로 정렬(표시·시드 일관). |
| 2 | `app/static/js/lotto4.js` | `BRAIN_SECONDARY`: 역발상 → 앙상블 순으로 UI 정렬. |
| 3 | `app/lotto4/brains/_utils.py` | **필수 수정**: (1) `_weighted_draw_without_replacement`는 **`v > 0`인 번호만** 표본추출. (2) `generate_sets_with_filters`에서 전 질량 0일 때만 균등 폴백; **전 번호에 1e-6 바르기 제거**(앙상블 15풀만 쓰도록). |
| 4 | `tools/mini_backtest_v13.py` | `v13_ensemble` 추가, 구간 **1200~1222**. |

### 코드 핵심 (`ensemble.py`)
- `SUB_BRAINS`: trend, bayesian, transformer, graph, generator, rl_agent, contrarian_v2 (**ensemble 제외 → 순환 없음**).
- `TAG_MAP`으로 모듈 경로 ↔ DB `brain_tag`.
- `_load_weights`: `v13_%` 행만, `max(w, MIN_WEIGHT)`, 기본 Missing 시 루프에서 `1.0`.
- 투표: 각 세트의 각 번호에 뇌 가중치 **가산**.
- 후보: `vote_score` 기준 내림차순·동점 시 번호 오름차순으로 15개.
- `generate_sets_with_filters(..., max_retry=80)` 호출.

---

## TASK 3 — `generate_sets_with_filters` / `score_dict` 모드

- **추가 API 없음**: 기존 첫 인자 `score_dict` 유지.
- **변경**: 명시적 0 가중은 **추출 후보에서 제외**; 질량 합 0이면 1~45 균등 폴백만 수행. → 앙상블 **Confidence Gating**(15개 풀)이 의도대로 동작.

---

## TASK 4 — 미니 백테스트 결과 원문 (`PYTHONIOENCODING=utf-8`)

```
=== v13_trend ===
  총 세트: 115
  평균 적중: 0.696
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 3
  4등+: 0, 5등+: 3

=== v13_bayesian ===
  총 세트: 115
  평균 적중: 0.930
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 6
  4등+: 0, 5등+: 6

=== v13_ensemble ===
  총 세트: 115
  평균 적중: 0.913
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 2
  4등+: 0, 5등+: 2
```

### trend / bayesian / ensemble 비교 (이번 구간만)

| 뇌 | 평균 적중 | 5등+(3개+) 건수(세트 기준) |
|----|-----------|---------------------------|
| trend | 0.696 | 3 |
| bayesian | **0.930** | **6** |
| ensemble | 0.913 | 2 |

- **이번 1200~1222 구간에서는 ensemble이 bayesian보다 높지 않음**(평균은 베이지안 > 앙상블 > 트렌드).
- 앙상블은 랜덤·플레이스홀더 5뇌가 동일 가중이면 노이즈가 섞이므로, **가중치/Hedge가 수렴하면** 투표 이득이 커질 여지가 있음.

---

## TASK 5 — 검증

| 항목 | 결과 |
|------|------|
| `from app.lotto4.brains.ensemble import predict` | OK |
| `predict(1224, db)` | `5` 세트, 각 `[6,6,6,6,6]` |
| `v13_ensemble in BRAIN_REGISTRY`, 마지막 키 | True, `v13_ensemble` |
| `POST /api/lotto4/v13/predict/1225` | 200, `all_predictions`에 ensemble 5세트 |
| `GET /api/lotto4/v13/brain/status` | 200, `brain_profiles`에 ensemble 존재 |
| `mini_backtest_v13.py` | 에러 없음 |
| `grep My_Library` in `app` | 0건 |

---

## 후속 제안 (3단계 뇌 구현 순서)

1. **플레이스홀더 5뇌** 실구현 또는 가중치 하한·엔트로피 페널티로 노이즈 억제.  
2. **앙상블**: (a) 가중 투표에 **신뢰도·세트 내 분산** 반영, (b) 후보 풀 15→동적, (c) 하위 뇌 캐시(동일 `draw_no`에서 엔진이 이미 돌린 결과 재사용)로 API 지연 감소.  
3. `lotto4.js` / `index.html` 명예의전당 등 **8뇌 순서** UI가 다른 곳에 하드코딩되어 있으면 `contrarian` → `ensemble` 순으로 맞추기.

*보고서 생성: Cursor 에이전트*
