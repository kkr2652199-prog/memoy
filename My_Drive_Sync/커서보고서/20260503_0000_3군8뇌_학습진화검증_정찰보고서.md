# 3군 8뇌 학습·진화 검증 정찰 보고서

- **저장 시각(파일명 기준)**: 2026-05-03 00:00  
- **DB**: `D:/MONEY lol/My_Library/data/lotto.db`  
- **제약**: **코드 변경 없음**. SQL은 **SELECT만** (STEP 1-A 등). 임시 스크립트 `_tmp_weight_sim.py`, `_tmp_w1222.py`는 결과 산출 후 **삭제 완료**.

---

## STEP 0 — SHA-256 사전검증 (31파일)

**복합 기준표**

- `20260502_1430_3군학습결함_벤치마킹_정찰보고서.md`의 `| app/... |` 표에서 **29파일** 해시를 읽음.
- 아래 **2파일**만 `20260502_2330_3군8뇌_회차별적중_정찰보고서.md`에 기록된 **패치 후 실측**으로 덮어씀(2330 표의 «실측» 열과 동일).
  - `app/lotto3/v12_models.py` → `7C8ECDEF6C71FF3EEC46B9A483CC805E13C5DF0783891C16D6580CFFED29A4F1`
  - `app/lotto3/v12_fusion_v5.py` → `493AFBC9A9114F63E98FF8F0B0479E1A3166E5A1B730F9CBEA4B8005F9E3B6B7`

**실행**: `My_Library` 루트에서 Python으로 표 파싱(`chr(96)` 백틱 제거) 후 `hashlib.sha256` 비교.

**결과**: `expected=31`, **`mismatch=0`** (31/31 일치).

---

## STEP 1 — `v12_snake` 가중치 실종 추적

### 1-A) `lotto_brain_weights_army3` 전체 덤프 (`SELECT *` … `ORDER BY brain_tag`)

```sql
SELECT * FROM lotto_brain_weights_army3 ORDER BY brain_tag;
```

**Raw (`|` 구분: brain_tag|current_weight|recent_avg_match|total_predictions|total_matches|last_updated_draw|updated_at)**

```
v12_contrarian|12.463711523942|0.8|245|196|1222|2026-05-02 19:58:52
v12_fusion|107.632797617341|1.93877551020408|245|475|1222|2026-05-02 19:58:52
v12_hyena|257.382201761691|2.34285714285714|245|574|1222|2026-05-02 19:58:52
v12_lstm|83.8644330059096|1.82857142857143|245|448|1222|2026-05-02 19:58:52
v12_offset|5.10908541202876|0.808163265306122|245|198|1222|2026-05-02 19:58:52
v12_run|5.2614579400874|0.828571428571429|245|203|1222|2026-05-02 19:58:52
v12_stat|8.04176656907797|0.836734693877551|245|205|1222|2026-05-02 19:58:52
```

→ **`v12_snake` 행 없음** (7행만 존재).

### 1-B) `init_v12_seeds` — INSERT 되는 `brain_tag` (코드)

`app/lotto3/v12_models.py`에서 `V11_SEED_WEIGHTS` 키만 `INSERT OR IGNORE` 한다.

- **시드 딕셔너리 정의**: ```30:38:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
V11_SEED_WEIGHTS: dict[str, float] = {
    "v12_stat": 1.5,
    "v12_run": 1.0,
    "v12_offset": 1.0,
    "v12_contrarian": 2.5,
    "v12_lstm": 2.0,
    "v12_fusion": 2.0,
    "v12_hyena": 2.0,
}
```
- **INSERT 루프**: ```161:165:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
        for tag, weight in V11_SEED_WEIGHTS.items():
            conn.execute(
                "INSERT OR IGNORE INTO lotto_brain_weights_army3 (brain_tag, current_weight) VALUES (?, ?)",
                (tag, weight),
            )
```

**INSERT되는 brain_tag (7개)**: `v12_stat`, `v12_run`, `v12_offset`, `v12_contrarian`, `v12_lstm`, `v12_fusion`, `v12_hyena` — **`v12_snake` 없음**.

### 1-C) `update_v12_weights` — UPDATE 대상 `brain_tag` 튜플

- **상수**: ```19:27:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
V11_BRAINS = (
    "v12_stat",
    "v12_run",
    "v12_offset",
    "v12_contrarian",
    "v12_lstm",
    "v12_fusion",
    "v12_hyena",
)
```
- **SELECT·바인딩**: ```182:191:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
        placeholders = ",".join("?" * len(V11_BRAINS))
        flat = conn.execute(
            f"""
            SELECT brain_tag, matched_count, bonus_matched
            FROM lotto_predictions_army3
            WHERE target_draw_no <= ? AND target_draw_no > ?
              AND matched_count >= 0
              AND brain_tag IN ({placeholders})
            """,
            (target_draw_no, target_draw_no - last_n, *V11_BRAINS),
        ).fetchall()
```
- **갱신 루프의 `WHERE brain_tag = ?`**: ```212:227:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
            conn.execute(
                """
                UPDATE lotto_brain_weights_army3
                SET current_weight = ?, recent_avg_match = ?,
                    total_predictions = ?, total_matches = ?,
                    last_updated_draw = ?, updated_at = datetime('now','localtime')
                WHERE brain_tag = ?
                """,
                (
                    new_w,
                    avg_m,
                    int(n),
                    int(sum_m_by[tag]),
                    int(target_draw_no),
                    tag,
                ),
            )
```
(`tag`는 위 `flat`에서 집계된 **`V11_BRAINS` 내 태그만** 순회.)

### 1-D) `V11_BRAINS` 상수

- **위치·내용**: ```19:27:d:\MONEY lol\My_Library\app\lotto3\v12_models.py``` (7뇌, **`v12_snake` 미포함**).

### 1-E) 결론 — 원인 특정

| 가설 | 판정 |
|------|------|
| 시드(`init_v12_seeds`)에 없어서 행이 안 생김 | **성립** — `V11_SEED_WEIGHTS`에 `v12_snake` 없음(1-B). |
| `update_v12_weights`에서 빠져 갱신되지 않음 | **성립** — `V11_BRAINS`에 없어 SELECT·UPDATE 경로 밖(1-C). |
| `DELETE`로 지워짐 | **해당 없음** — `init_v12_seeds`의 DELETE는 **`v12_combo`만** 대상(아래 코드 참조). |

```157:159:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
        conn.execute(
            "DELETE FROM lotto_brain_weights_army3 WHERE brain_tag = ?",
            ("v12_combo",),
        )
```

**종합**: `v12_snake`는 **설계상 Hedge 가중치 테이블의 시드/갱신 튜플에 포함되지 않아** 행이 존재하지 않는 것이 정상 동작에 가깝다. «실종»이 아니라 **미등록**에 해당.

---

## STEP 2 — 뇌별 학습 여부 판정

**2-A)** `SELECT 1 FROM lotto_brain_weights_army3 WHERE brain_tag=?` 존재 여부 (Y/N).  
**2-B)** `brain_tag in V11_BRAINS` 여부 (코드 19–27행 기준).  
**2-C)** `SELECT COUNT(*) FROM lotto_predictions_army3 WHERE brain_tag=? AND matched_count>=0` > 0.

**2-C Raw** (`brain_tag|cnt`):

```
v12_contrarian|6080
v12_fusion|6080
v12_hyena|6080
v12_lstm|6055
v12_offset|6080
v12_run|6080
v12_snake|6080
v12_stat|6080
```

| brain_tag | 가중치행 | 갱신대상 | 예측생성 | 판정 |
|-----------|----------|----------|----------|------|
| v12_stat | Y | Y | Y | 학습O (Hedge 시드·갱신 경로) |
| v12_run | Y | Y | Y | 학습O |
| v12_offset | Y | Y | Y | 학습O |
| v12_contrarian | Y | Y | Y | 학습O |
| v12_lstm | Y | Y | Y | 학습O |
| v12_fusion | Y | Y | Y | 학습O |
| v12_hyena | Y | Y | Y | 학습O |
| v12_snake | N | N | Y | 학습X — **원인**: `V11_SEED_WEIGHTS`·`V11_BRAINS` 미포함으로 DB 가중치·`update_v12_weights` 갱신 없음. 예측은 `v12_engine.run_prediction_v12`에서 별도 생성. |

---

## STEP 3 — 가중치 시계열 재현 (최근 50회차 창, `update_v12_weights` 동형)

### 코드 근거

- **η**: `V11_HEDGE_ETA = 2.0` — ```40:40:d:\MONEY lol\My_Library\app\lotto3\v12_models.py```
- **기본 가중치 `base`**: 매 갱신마다 `V11_SEED_WEIGHTS.get(tag, 1.0)` — ```206:211:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
        for tag, n in n_by.items():
            base = float(V11_SEED_WEIGHTS.get(tag, 1.0))
            avg_m = sum_m_by[tag] / float(n)
            avg_lottery_score = sum_ls_by[tag] / float(n)
            score_signal = avg_m + avg_lottery_score / 30.0
            new_w = base * math.exp(V11_HEDGE_ETA * score_signal)
```
- **윈도우**: `target_draw_no <= D` AND `target_draw_no > D - 50`, `last_n` 기본 50 — ```171:191:d:\MONEY lol\My_Library\app\lotto3\v12_models.py```

지시문의 «base=1.0»은 코드와 다름 — 본 재현은 **코드 그대로** 태그별 시드 + η=2.0.

`v12_snake`는 위 수식 경로에 없으므로 열은 **`-`**. **분산**은 Hedge **7뇌** 시뮬 weight에 대한 표본 분산.

| draw | v12_stat | v12_run | v12_offset | v12_contrarian | v12_lstm | v12_fusion | v12_hyena | v12_snake | 분산 |
|------|----------|---------|------------|----------------|----------|------------|-----------|-----------|------|
| 100 | 8.6769 | 4.0976 | 5.3527 | 10.4175 | 94.7031 | 150.1769 | 196.5429 | - | 5546.7626 |
| 200 | 7.5012 | 5.1634 | 3.5452 | 10.4259 | 82.4188 | 114.7796 | 209.7005 | - | 5327.9142 |
| 300 | 6.5806 | 4.4674 | 4.5503 | 12.1180 | 91.8430 | 148.3460 | 298.5728 | - | 10567.9587 |
| 400 | 7.4773 | 4.0747 | 4.3129 | 10.7076 | 111.6401 | 158.9115 | 276.3534 | - | 9615.8574 |
| 500 | 7.5978 | 4.9808 | 5.0788 | 13.3603 | 88.8320 | 91.4520 | 204.6185 | - | 4813.9046 |
| 600 | 7.2883 | 4.4531 | 6.8483 | 13.6958 | 100.6666 | 114.5656 | 269.2249 | - | 8271.7698 |
| 700 | 7.5333 | 5.0168 | 4.6274 | 11.9479 | 84.3758 | 137.4528 | 258.0484 | - | 7976.5255 |
| 800 | 7.9587 | 5.2035 | 4.4106 | 13.2186 | 89.4262 | 163.8165 | 241.2130 | - | 7698.4513 |
| 900 | 6.3007 | 5.0747 | 4.5467 | 11.5994 | 72.6520 | 100.9892 | 187.0820 | - | 4166.2941 |
| 1000 | 7.3155 | 4.7449 | 5.0249 | 11.9543 | 75.8391 | 105.2509 | 155.7648 | - | 3172.9504 |
| 1100 | 6.5212 | 3.9401 | 4.1704 | 12.0310 | 69.0442 | 124.4388 | 138.4829 | - | 3039.4752 |
| 1221 | 7.9016 | 5.2132 | 5.2719 | 12.9811 | 81.6532 | 106.0397 | 250.9902 | - | 7089.9532 |

**검증(부록)**: 현재 DB `last_updated_draw=1222`이므로 동일 수식으로 **draw=1222**를 계산하면 `v12_hyena=257.3822` 등 **DB `current_weight`와 소수점 일치**함(임시 스크립트로 확인 후 삭제). 지시 목록의 마지막 시점은 1221이라 표에는 1221을 유지.

**폭주 구간(분산 급증)**: **300·400** 부근에서 분산이 ~1만 전후로 최대에 가깝고, 600·700·800·1221에서도 상대적으로 크다. (절대 척도는 `v12_hyena` 등 대형 weight와 저시드 뇌의 격차 때문.)

---

## STEP 4 — 컷팅(컨닝) 검증

### 4-A) `run_prediction_v12` — 학습·캐시 쿼리

- **기존 예측 조회(캐시 판단)** — `target_draw_no`만 등치 조건, 미래 참조 없음: ```275:277:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
        existing = conn.execute(
            "SELECT brain_tag FROM lotto_predictions_army3 WHERE target_draw_no = ? AND brain_tag LIKE 'v12_%'",
            (target_draw_no,),
```
- **학습 데이터**: `get_v12_training_draws(target_draw_no)` 호출 — ```290:290:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
    training = get_v12_training_draws(target_draw_no)
```
- **`get_v12_training_draws`**: 1군 미당첨 서브쿼리·`lotto_draws` 모두 **`draw_no < ?`**, **`target_draw_no < ?`** — ```58:83:d:\MONEY lol\My_Library\app\lotto3\v12_models.py
            SELECT d.* FROM lotto_draws d
            WHERE d.draw_no < ?
              AND d.draw_no IN (
                SELECT target_draw_no FROM lotto_predictions
                WHERE brain_tag IN ('stat','markov','llm','lstm','fusion','hyena')
                  AND matched_count >= 0
                  AND target_draw_no < ?
...
            SELECT * FROM lotto_draws
            WHERE draw_no < ?
            ORDER BY draw_no DESC
            LIMIT ?
```

**판정(4-A)**: 예측에 쓰는 `training` 경로에서 **당첨 회차 `target_draw_no` 미만**만 사용. **미래 당첨 번호를 학습 입력으로 넣는 SELECT는 없음.**

### 4-B) `update_v12_weights` — draw 범위

- **조건**: `target_draw_no <= target_draw_no` 상한 및 `> target_draw_no - last_n` 하한 — **항상 과거(및 당회) 예측 행만** 포함. ```187:191:d:\MONEY lol\My_Library\app\lotto3\v12_models.py```

**판정(4-B)**: **현재 예측 대상보다 큰 `target_draw_no`의 채점 결과를 섞지 않음**(윈도우 상한이 호출 시점의 `target_draw_no`).

### 4-C) 예측 함수 입력 범위 (샘플 3뇌)

| 뇌 | 파일 | 근거 |
|----|------|------|
| v12_stat | `predict_stat.py` | `army3_stat_predict(miss_draws, ...)` — 호출자가 워크포워드로 `miss_draws`만 전달한다고 전제. ```5:8:d:\MONEY lol\My_Library\app\lotto3\predict_stat.py``` |
| v12_run | `predict_run.py` | docstring: «학습 구간은 호출자가 워크포워드로만 넘길 것». ```42:47:d:\MONEY lol\My_Library\app\lotto3\predict_run.py``` |
| v12_lstm | `predict_lstm.py` | `army3_lstm_predict(miss_draws, ...)` — 동일 패턴. ```17:20:d:\MONEY lol\My_Library\app\lotto3\predict_lstm.py``` |

실제 워크포워드는 `run_prediction_v12` → `get_v12_training_draws`가 보장(4-A).

**보충 — `v12_snake`**: `_load_army1_sets_for_target(target_draw_no)`로 **동일 회차** 1군 예측 번호만 읽음(당첨번호 아님). ```58:71:d:\MONEY lol\My_Library\app\lotto3\v12_snake.py```

### 4-D) 판정

**컨닝 없음** — 위 줄 근거 범위에서, **당회 `target_draw_no`의 당첨번호를 예측 생성 전에 주입하는 경로는 확인되지 않음.** (채점 `_score_v12_predictions`는 INSERT 이후이며 ```392:400:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py```.)

---

## STEP 5 — 종합 분석

1. **8뇌 중 실제로 Hedge 가중치 «학습(갱신)»에 들어가는 뇌**: **7개** (`v12_snake` 제외).
2. **`v12_snake` 실종(미등록)**: `V11_SEED_WEIGHTS`·`V11_BRAINS`에 없어 **INSERT/UPDATE 경로가 원천 부재**. `DELETE`로 지운 것이 아님. **복구 방안(설계)**: (코드 변경은 본 보고서 범위 밖) `v12_snake`에 대한 시드값을 정한 뒤 `V11_SEED_WEIGHTS`·`V11_BRAINS`에 추가하고, `init_v12_seeds`/`update_v12_weights`의 IN 리스트가 자동 확장되도록 튜플만 확장하면 됨. 단, Snake는 합성 메타 뇌라 **Hedge에 넣는 것이 의도인지** 제품 정책 확인 필요.
3. **가중치 «폭주» 시점**: STEP 3 분산 기준 **300·400** 전후가 최고봉에 가깝고, 이후에도 큰 값이 반복. 원인은 `new_w = base * exp(2 * score_signal)`에서 **고적중이 많은 최근 50창**이면 지수부가 커지기 때문(코드 구조상 **이전 weight를 누적 곱하지 않고 매번 시드×exp로 재계산**).
4. **컨닝**: STEP 4 기준 **컨닝 없음**.
5. **C구간(801–1221) 하락 vs 가중치 폭주**: 2330 보고서의 구간별 `avg_mc`와 본 STEP 3은 **서로 다른 집계**(구간 전체 vs 최근 50회 롤링). **SELECT만으로 인과(하락이 폭주 때문)**는 입증 불가. 다만 **동일 기간 백테가 누적되며 최근 50창 신호가 커졌을 때 weight가 커지는 구조**는 코드로 확인됨.

---

## STEP 6 — SHA-256 사후검증 (31파일)

- STEP 0과 **동일 스크립트·동일 복합 기준표** 재실행.
- **결과**: `mismatch=0`, **코드·규칙 파일 변경 없음**(본 MD 및 임시 스크립트만 생성·삭제).

---

## 변경·검증 요약 (보고 규칙)

| 항목 | 내용 |
|------|------|
| 변경 파일 | `My_Drive_Sync/커서보고서/20260503_0000_3군8뇌_학습진화검증_정찰보고서.md` (신규) |
| 앱 코드 | **변경 없음** |
| 임시 파일 | `_tmp_weight_sim.py`, `_tmp_w1222.py` — **삭제 완료** |
| 검증 명령 | `sqlite3 data/lotto.db "SELECT * FROM lotto_brain_weights_army3 ORDER BY brain_tag;"` / SHA Python one-liner(STEP 0과 동일) |
| 미해결 | 없음(정책적으로 Snake를 Hedge에 넣을지는 제품 결정 사항). |
