# 기억71 — 4군 풀 백테스트 중단, 실측 정찰 및 재시작 지침

**작성일:** 2026-05-16  
**범위:** 4군 (`app/lotto4/`, `tools/full_backtest_v13.py` 등). 1~3군 폴더 비수정.  
**DB 경로:** `d:\3kweon\data\lotto4.db`

---

## 1. 기억71 핵심 요약

4군 풀 백테스트가 **전 구간(5~1223) 완주 전에 중단**되었고, **DB 실측**으로 혼재 상태가 확인됨.  
**skip 로직은 설계상 정상**이나, **잔존 행** 때문에 `--force` 없이 실행 시 대량 skip이 발생할 수 있음.  
진화뇌는 과거 **4태그 정규화**로 HIDDEN에 해당하는 태그가 가중치에 끼어드는 현상이 있었음 → **코드에서 2태그(seq·struct만)로 변경·검증 완료.**  
풀백 스크립트 **`db_path` 미설정** 버그는 **복구·검증 완료.**

---

## 2. DB 실측 스냅샷 (2026-05-16 정찰 시점)

아래는 정찰 당시 한 시점의 `lotto4.db` 조회 결과이다. **재초기화 후에는 반드시 동일 쿼리로 다시 확인할 것.**

| 테이블 | min(draw) | max(draw) | 총 행 수(참고) | 비고 |
|--------|-----------|-----------|----------------|------|
| `lotto_fullbacktest_army4` | 134 | 196 | 2195 (63 distinct 회차) | 5~1223 **미완주** |
| `lotto_predictions_army4` | 134 | 196 | 2195 (63 distinct 회차) | 동일 구간 혼재 |
| `lotto_evolution_trust_army4` | 134 | 195 | 248 (62 distinct 회차) | 에이스 기준 로그만 해당 |

**결론:** 5~1223 전 구간 연속 데이터가 아님. **신뢰 가능한 성적표·진화 추적을 위해 전체 재실행 전 완전 초기화 필수.**

### 초기화 후 검증용 SQL (예시)

```sql
SELECT COUNT(*) FROM lotto_predictions_army4;
SELECT COUNT(*) FROM lotto_fullbacktest_army4;
SELECT COUNT(*) FROM lotto_evolution_trust_army4;
-- 모두 0행이어야 “빈 상태” 확정
```

스크립트: `python tools/reset_fullback_army4_db.py` 실행 후 위 쿼리 또는 스크립트 내 `assert` 출력으로 **실측 확인**할 것. (“0행”은 **출력·SQL로 증명**하지 않으면 보고 불가.)

---

## 3. 발견·정리된 문제점

| # | 내용 |
|---|------|
| 1 | **DB 초기화 불완전 / 보고와 실측 불일치** — 이전 세션에서 "0행" 주장과 달리 잔존 구간이 남은 사례 있음. |
| 2 | **skip 로직** — `fb_done≥35` 및 `pr_done≥35`이고 `--force`가 **아닐 때만** skip. 로직 자체는 “빈 DB면 스킵 안 함”. 문제 원인은 **잔존 데이터**. |
| 3 | **진화뇌 4태그 정규화** — `v13_cdm`, `v13_cond_prob`가 풀백에 없어도 폴백 0.25가 섞여, 로그상 `cdm≈0.32`, `cond≈0.32`처럼 보일 수 있었음. |
| 4 | **`full_backtest_v13._run_backtest`에서 `db_path` 미할당** — `NameError` 유발 가능. **복구됨.** |

---

## 4. 커서 수정 사항 — 코드 검증 결과 (2026-05-16)

### 4.1 `evolution_brain.py`: CHIEF 2태그만

- **`CHIEF_TAGS`:** `v13_seq`, `v13_struct` 만 사용.
- **`CHIEF_MODULES`:** 위 두 뇌의 `predict` 경로만 매핑.
- **효과:** `get_dynamic_weights` / `predict` / `rescore` / `update_trust`가 **에이스 2뇌만** 기준으로 동작. HIDDEN 뇌는 이 경로에서 가중치 후보에 포함되지 않음.

### 4.2 `full_backtest_v13.py`: `db_path` 및 진화 로그

- **`_run_backtest` 첫 줄:** `db_path = os.path.abspath(args.db)` **존재 확인.**
- **`format_evolution_log`:** `cdm` / `cond` 필드 **제거**, `seq`·`struct`만 출력.

### 4.3 빌드 검증

- `python -m py_compile app/lotto4/brains/evolution_brain.py tools/full_backtest_v13.py` — **통과.**

---

## 5. 변경 파일 SHA256 (검증 일시: 2026-05-16, 로컬 워크스페이스)

| 파일 | SHA256 |
|------|--------|
| `app/lotto4/brains/evolution_brain.py` | `62D9B44DA1C670CB6D499C5D9766C003E53ED5E4585424C55762C74DC1098CC9` |
| `tools/full_backtest_v13.py` | `F90068AAC444D181DCFED99BA294F275404B612BE9656E3603D1524A86EFE807` |

*(재편집 시 해시는 변하므로, 배포 직전 `Get-FileHash -Algorithm SHA256`로 재측정할 것.)*

---

## 6. 재시작 시 작업 순서 (반드시 순서대로)

1. **코드 검증** — 본 보고서 §4 또는 동일 내용을 로컬에서 재확인.
2. **DB 완전 초기화** — `python tools/reset_fullback_army4_db.py` 후 `COUNT(*)` / min/max로 **0행·당첨 테이블 보존** 확인.
3. **모델 파일 삭제** — `models/seq_brain_latest.pt`(있으면), `models/struct_brain_*.json` 전부.
4. **DB 점유 프로세스** — API·다른 Python이 `lotto4.db`를 열고 있으면 종료 또는 대기(이미 스크립트에 `busy_timeout` 있음).
5. **풀 백테 실행 (온라인 학습 ON, 단일 프로세스):**

   ```text
   cd d:\3kweon
   python -u tools/full_backtest_v13.py --start 5 --end 1223 --force --log-file reports/fullback_run.log
   ```

   - 동일 로그 파일을 에디터가 잠그면 `PermissionError`가 날 수 있음 → 필요 시 `fullback_run_YYYYMMDD_HHMM.log` 등 **유일한 파일명** 사용.
   - Windows CMD `1>>` 리다이렉트보다 **`--log-file` 우선.**

6. **완료 후 성적표** — `python tools/analyze_fullbacktest.py --from 5 --to 1223`

---

## 7. RULES / 기억 체인 (요지)

- **R24~R27 유지.**
- **R27 강조:** 결과·정찰·재현 절차는 **반드시 Markdown 보고서 파일**로 남길 것. 채팅 텍스트만으로 제출하지 말 것.
- **기억 체인:** …67 → 68 → 69 → 70 → **71 (본 문서)**

---

## 8. 부록 — 커닝 방지 원칙 (변경 없음)

`predict(target_draw=N)` 시 `draw_no < N` 데이터만 사용한다는 4군 설계 원칙은 본 이슈(초기화·skip·진화태그)와 별개로 **유지**한다.

---

*끝.*
