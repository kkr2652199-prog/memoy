🦅 동생 작성 지시문 기반 — 커서 자동 생성 (옵션 B)

# 1군 lottery_score 메커니즘 정밀 정찰 보고서 (READ-ONLY)

- 작성: 2026-05-02 15:00 KST  
- 선행: `20260502_1430_3군학습결함_벤치마킹_정찰보고서.md` STEP 1-D  
- 범위: `app/lotto/feedback.py` 중심 + `engine.py` 호출부 · `data/lotto.db` **SELECT만**  
- **R29**: 1·2군 READ-ONLY, **SHA256** `20260502_1430` STEP 0 baseline과 **31/31 일치**  
- **1222 미접근**: SQL 조건 `target_draw_no <= 1221`

---

## STEP 0 — SHA256 사전·사후 (31파일)

- **1430 baseline 대비**: PowerShell로 **31경로 전수 해시** 재계산 → **`STEP0_ALL31_MATCH_1430_BASELINE`** (터미널 raw).  
- **사후**: 동일 스크립트 **연속 2회** 해시 비교 → **전 파일 동일** (READ-ONLY, 변동 0).  
- 해시 표는 **1430 보고서 STEP 0 표와 동일** — 본 문서에 **중복 전재 생략**.

---

## STEP 1 — `lottery_score` 정의 위치 raw

### 1-A. `rg "lottery_score" app/lotto/` 매칭

| 파일 | 라인·내용 |
|------|-----------|
| `engine.py` | `6` 주석, `15` import `_calculate_lottery_score`, `611-648` 백테 요약에 `lottery_score_stats` |
| `feedback.py` | `6` 주석(/30 하이브리드), `258` `def _calculate_lottery_score`, `293-294` docstring 키 이름, `334` 호출, `372-374` 집계 키, `395-396` docstring, `423-425` `update_brain_weights` 본문, `460-467` 반환 dict |

(`routes.py.before_reset_*` 는 **백업 파일** — 런타임 경로 아님, 참고만.)

### 1-B. 계산 함수 정의

**`258:274:app/lotto/feedback.py`** — `def _calculate_lottery_score(matched_count: int, bonus_matched: int) -> int:`

### 1-C. 입력 변수

- **`matched_count`**: 적중 개수 (`int`)  
- **`bonus_matched`**: 보너스 일치 여부( truthy면 2등 분기에 사용) — `get_brain_tag_ranking` 루프에서 `row[2]` → `bm` (`332:333`)

### 1-D. 출력 범위

- **반환 타입 `int`**: **`100, 50, 30, 10, 3, 0`** 중 하나 (`258:274` docstring·분기 raw).

### 1-E. `/30` 정규화 의미

- **코드 근거**: `feedback.py` **상단 주석 `6`행** — *"avg_lottery_score/10 → avg_match + avg_lottery_score/30 하이브리드"* 로 **설계 변경 이력**만 서술.  
- **수학적 스케일**: `avg_match`는 대략 **0~6**, `avg_lottery_score`는 대략 **0~100**대이므로 **`/30`으로 lottery 쪽 기여를 압축**해 두 시그널을 **한 지수 인자** `eta * (avg_match + avg_lottery_score/30)`에 넣음 (`395`, `425`).  
- **“왜 30인가” 단일 이론식 근거**: 코드·주석에 **유도 과정 없음** → **역사적 튜닝 값**으로만 확정 가능, 그 이상은 **모른다**.

---

## STEP 2 — 계산식 raw 전체

### 2-A. 등수별 점수 매핑

`261:262:app/lotto/feedback.py` docstring raw:

- 1등(6적중)=**100**  
- 2등(5+보너스)=**50**  
- 3등(5적중)=**30**  
- 4등(4적중)=**10**  
- 5등(3적중)=**3**  
- 그 외=**0**

### 2-B. `matched_count` → 점수 변환

`264:274` 분기: `matched_count < 0` → `0`; `==6` → 100; `==5` → `50 if bonus_matched else 30`; `==4` → 10; `==3` → 3; else → 0.

### 2-C. 보너스(2등)

`268:269` — `matched_count == 5` 일 때 **`bonus_matched` truthy면 50**, 아니면 30.

### 2-D. 미당첨 (`matched_count < 3`)

`272:274` — 위 분기에 걸리지 않으면 **`return 0`**.

---

## STEP 3 — `analyze_prediction_feedback` 호출 흐름

### 3-A. `engine.py` 호출

`586:594:app/lotto/engine.py` — `run_backtest` 루프 내 `if has_feedback and best_match >= 0:` → `analyze_prediction_feedback(draw_no)` → `update_brain_weights(...)`.

### 3-B. 함수 입·출력

`31:149:app/lotto/feedback.py`:

- **입력**: `target_draw_no: int`  
- **출력**: `feedback` dict (`method_performance`, `brain_tag_performance`, …) — **`lottery_score` 키는 없음** (`120:128`).

### 3-C. DB 저장 위치

- **`lottery_score` 컬럼으로 `lotto_predictions`에 저장하지 않음**.  
- **`lotto_analysis`** 테이블에 `analysis_type='prediction_feedback'`, `data_json`에 **`json.dumps(feedback)`** (`136:139`) — JSON에도 **per-row lottery_score 필드 없음**(brain_stats는 `avg_match` 등만).

### 3-D. `update_brain_weights`가 읽는 경로

- **직접 SQL로 `lottery_score` 컬럼 SELECT 없음**.  
- `get_brain_tag_ranking(last_n)` (`401`) → 내부에서 `matched_count`, `bonus_matched`만 읽고 **`_calculate_lottery_score`로 행별 점수 합산** (`321:334`, `349`) → **`avg_lottery_score = sum_score/total`** (`372`).

---

## STEP 4 — `update_brain_weights` raw 요약

### 4-A~4-D. 핵심 본문

**시그널** (`422:426`):

```text
score_signal = raw_match + raw_score / 30.0
nw = base * math.exp(eta * score_signal)
```

**`SEED_WEIGHTS`**: `18:24`  
**`LAYER3_BRAIN_TAGS`**: `27` — `stat, markov, llm, lstm, hyena`  
**`EXCLUDED_UPDATE_TAGS`**: `28` — `fusion`, `legacy`, `llm_fallback`

### 4-E. fusion·snake 제외 이유

- **`update_brain_weights` 루프**: `for bt in LAYER3_BRAIN_TAGS` (`414`) + `EXCLUDED_UPDATE_TAGS` 스킵 (`415`).  
- **`fusion`·`snake`는 `LAYER3_BRAIN_TAGS`에 없음** — 따라서 **본 함수로 `lotto_brain_weights` 갱신 대상 아님**.  
- `fusion`은 추가로 **`EXCLUDED_UPDATE_TAGS`에 명시** (`28`).

---

## STEP 5 — 1군 vs 3군 시그널 정량 비교

### 5-A. `lotto_predictions` PRAGMA

`PRAGMA table_info(lotto_predictions);` 결과: 컬럼 인덱스 **0~14** — **`lottery_score` 컬럼 없음** (실측 raw: `matched_count`, `bonus_matched`만 존재).

### 5-B. `AVG(lottery_score)` SQL

- **컬럼 부재로 불가**.  
- **동등 의미**: 아래 **CASE**로 `_calculate_lottery_score`와 동형 평균(행별 점수 평균) 산출 — **`target_draw_no <= 1221`**, `matched_count >= 0`.

```sql
SELECT brain_tag,
  ROUND(AVG(matched_count), 4) AS avg_match,
  ROUND(AVG(CASE
    WHEN matched_count < 0 THEN 0
    WHEN matched_count = 6 THEN 100
    WHEN matched_count = 5 AND bonus_matched != 0 THEN 50
    WHEN matched_count = 5 THEN 30
    WHEN matched_count = 4 THEN 10
    WHEN matched_count = 3 THEN 3
    ELSE 0
  END), 4) AS avg_lottery_equiv
FROM lotto_predictions
WHERE target_draw_no <= 1221 AND matched_count >= 0
  AND brain_tag IN ('stat','markov','llm','lstm','hyena','fusion','snake')
GROUP BY brain_tag ORDER BY brain_tag;
```

**결과 raw** (sqlite3):

```text
fusion|1.5935|1.4081
hyena|2.2819|2.4149
llm|0.7992|0.0836
lstm|1.9034|1.3651
markov|0.7992|0.0885
snake|0.8523|0.0892
stat|0.7844|0.0685
```

### 5-C. 시그널 비율 `avg_match : avg_lottery_equiv/30` (동형 평균 기준)

비율 = `avg_match / (avg_lottery_equiv/30)` = `30 * avg_match / avg_lottery_equiv` (분모 0 방지: `avg_lottery_equiv>0`만)

| brain_tag | 30×avg_match / avg_lottery_equiv |
|-----------|----------------------------------|
| stat | ≈ 343.4 |
| markov | ≈ 270.8 |
| llm | ≈ 286.8 |
| lstm | ≈ 41.8 |
| fusion | ≈ 33.9 |
| hyena | ≈ 28.3 |
| snake | ≈ 286.6 |

(수치는 위 SQL 출력에서 **파생 계산**.)

### 5-D. 3군 도입 시(개념)

- **현 3군**: `update_v12_weights`는 **`avg_match`만** 지수에 사용 (`v12_models.py` 공식, 1430 보고서).  
- **하이브리드 도입 시**: 동일 η라면 **`exp(η*(avg_m + avg_lottery/30))` > `exp(η*avg_m/6)` 여부**는 뇌별·구간별로 다름 → **전 뇌 일괄 강화/약화 단정은 불가**(SQL 5-B·5-C가 **근거 자료**).

**3군 동형 SQL 결과** (`lotto_predictions_army3`, `brain_tag LIKE 'v12_%'`, `<=1221`):

```text
v12_contrarian|0.7817|0.0745
v12_fusion|1.685|1.6281
v12_hyena|2.1405|1.8482
v12_lstm|1.8155|1.1671
v12_offset|0.7931|0.0827
v12_run|0.789|0.0829
v12_snake|1.8125|0.9488
v12_stat|0.803|0.0773
```

(`v12_combo`: `SELECT COUNT(*) ... brain_tag='v12_combo' AND target_draw_no<=1221 AND matched_count>=0` → **0건** → GROUP BY 결과에 **미출현**.)

---

## STEP 6 — 3군 도입 가능성

### 6-A. `lotto_predictions_army3` PRAGMA

`lotto_predictions`과 **동일 스키마**(컬럼 **0~14**) — **`lottery_score` 컬럼 없음**.

### 6-B. 영속 컬럼이 필요한가

- **필수는 아님**: 1군도 **컬럼 없이** Python에서 계산.  
- **컬럼 추가 시**: `ALTER TABLE` + INSERT 경로 수정 — **DDL·쓰기 패치** (본 정찰에서는 **설계 옵션만 기록**).

### 6-C. `import` 가능성

- 문법·런타임: `from app.lotto.feedback import _calculate_lottery_score` **가능**(3군 코드가 이미 `app.lotto` DB 접근 사용).  
- **아키텍처**: 3군 학습 모듈이 1군 피드백 모듈에 **하드 결합** — R29 “벤치마킹 한정”과 충돌 여부는 **형 정책** 영역.

### 6-D. 학습 트리거

`v12_engine.py` **`360`** — `update_v12_weights(target_draw_no)` (1430 보고서 인용과 동일).

### 6-E. 패치 라인 수 추정

| 작업 | 추정 |
|------|------|
| `v12_models.update_v12_weights`에 동형 집계·공식 분기 | **중** (~40~90행, `get_brain_tag_ranking` 복제 또는 공유 헬퍼) |
| `army3` 전용 `get_v12_brain_tag_ranking` 신설 | **중** |
| DB `lottery_score` 컬럼 + INSERT 채움 | **대** (스키마·백필·마이그레이션) |

---

## STEP 7 — 패치 위험도

### 7-A. 본백테 호환

- **가중치 공식 변경 시** 과거와 **동일 수치 재현 불가** — DB `lotto_brain_weights_army3` 이미 갱신된 행과 **불일치** 가능.  
- **컬럼만 추가·NULL 허용**이고 공식 미변경이면 **예측 행 호환**은 유지되나 **이득 없음**.

### 7-B. 백테 재실행

- **공식·집계 로직 변경 시 `Y`** (가중치 궤적 비교 목적).  
- **문서화만**이면 `N`.

### 7-C. 1·2군 영향

- **3군 `v12_models.py`만 수정 + 1군 함수 import 호출** 시 1·2군 **바이트 변경 없음** 가능 — **R29 준수 조건부**.

### 7-D. `_tmp_archive_20260502` 4파일

- 본 패치와 **무관** (경로 미수정).

---

## STEP 8 — 종합 표

| 항목 | raw 근거 (파일:라인 or SQL) | 답 |
|------|-----------------------------|-----|
| lottery_score 정의 위치 | `feedback.py:258-274` | 함수 `_calculate_lottery_score` |
| 등수별 점수 매핑 | `261-262,264-274` | 100/50/30/10/3/0 |
| /30 정규화 의미 | `6` 주석, `425` | 하이브리드 스케일 맞춤; **유도식은 코드에 없음** |
| DB 컬럼 존재(1군) | `PRAGMA table_info(lotto_predictions)` | **없음** |
| DB 컬럼 존재(3군) | `PRAGMA table_info(lotto_predictions_army3)` | **없음** |
| 패치 규모 | STEP 6-E | **중**(필수), 컬럼 추가 시 **대** |
| 백테 재실행 | STEP 7-B | 공식 변경 시 **Y** |

---

## STEP 9 — 결론 4축

1. **한 줄 식(행 점수)**: `score = f(matched_count, bonus_matched)` ∈ {100,50,30,10,3,0} (`258:274`). **학습 시그널(뇌 평균)**: `exp(eta * (avg_match + avg_lottery_score/30))` where `avg_lottery_score = mean_row(score)` (`372`, `425-426`, `395`).  
2. **3군 도입 가능성**: **조건부 PASS** — 로직 복제는 **가능**, 다만 **1군 모듈 import 결합** 또는 **코드 복사** 선택 필요, **백테·가중치 궤적 재검증 필수**.  
3. **패치 규모**: **중**(집계+공식), DB 컬럼까지면 **대**.  
4. **1222 전/후**: **코드 패치 자체는 추첨과 독립** 가능하나, **학습 재현성** 목적이면 **백테 완료 후**가 안전(7-B).

---

## STEP 10 — 보고서 저장

- 본 파일: `D:/MONEY lol/My_Drive_Sync/커서보고서/20260502_1500_1군lottery_score_정찰보고서.md`

---

## STEP 11 — SHA256 사후

- **31/31** = 1430 baseline 일치 + 세션 내 연속 해시 동일.

---

## STEP 12 — 임시 파일

- `_tmp_lottery_*` **미생성** → 삭제 없음.  
- `_tmp_archive_20260502/` **유지**.

---

## 부록 A — `_calculate_lottery_score` 전체

```258:274:d:\MONEY lol\My_Library\app\lotto\feedback.py
def _calculate_lottery_score(matched_count: int, bonus_matched: int) -> int:
    """한국 로또 등수 기반 점수 계산.

    1등(6적중)=100, 2등(5적중+보너스)=50, 3등(5적중)=30,
    4등(4적중)=10, 5등(3적중)=3, 그 외=0.
    """
    if matched_count < 0:
        return 0
    if matched_count == 6:
        return 100
    if matched_count == 5:
        return 50 if bonus_matched else 30
    if matched_count == 4:
        return 10
    if matched_count == 3:
        return 3
    return 0
```

---

## 부록 B — `update_brain_weights` 핵심 블록

```388:439:d:\MONEY lol\My_Library\app\lotto\feedback.py
def update_brain_weights(
    target_draw_no: int,
    last_n: int = 50,
    eta: float = 1.5,
    min_scored_draws: int = 10,
) -> dict:
    """
    Hedge: new_weight[bt] = base_weight[bt] * exp(eta * (avg_match + avg_lottery_score/30)) — Layer 5-B 보정.
    ...
    """
    try:
        r = get_brain_tag_ranking(last_n)
        ...
            for bt in LAYER3_BRAIN_TAGS:
                if bt not in rankings or bt in EXCLUDED_UPDATE_TAGS:
                    continue
                rec = rankings[bt]
                avg_m = float(rec["avg_match"])
                ...
                raw_match = float(rec.get("avg_match", 0) or 0.0)
                raw_score = float(rec.get("avg_lottery_score", 0) or 0.0)
                # 하이브리드 시그널: avg_match 보존 + 등수 보너스 가산
                score_signal = raw_match + raw_score / 30.0
                nw = base * math.exp(eta * score_signal)
                conn.execute(
                    """
                    UPDATE lotto_brain_weights
                    SET current_weight = ?,
                        recent_avg_match = ?,
                        ...
```

---

## 부록 C — `get_brain_tag_ranking` SQL (점수 합산 전)

```321:327:d:\MONEY lol\My_Library\app\lotto\feedback.py
        rows = conn.execute(
            f"""SELECT brain_tag, matched_count, bonus_matched
                FROM lotto_predictions
                WHERE target_draw_no IN ({placeholders})
                  AND matched_count >= 0""",
            target_ids,
        ).fetchall()
```

---

## 부록 D — PRAGMA raw (`lotto_predictions` 컬럼명만)

`id`, `target_draw_no`, `method`, `num1`…`num6`, `confidence`, `reasoning`, `matched_count`, `bonus_matched`, `created_at`, `brain_tag` — **`lottery_score` 없음** (`PRAGMA table_info` 실측).
