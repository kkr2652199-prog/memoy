# 4군 분리 + 원본 복원 보고서

- **일자:** 2026-05-09 (KST)
- **목적:** `My_Library`에서 4군 흔적 제거·원상복구 후, 4군 코드만 `d:\3kweon`으로 이전.
- **제한:** `app/lotto/`, `app/lotto2/`, `app/lotto3/` 는 **수정·읽기 미실행**(에이전트). 보고서·기억 경로는 **Drive** 그대로 사용.

---

## PHASE A — My_Library 원본 복원

### A-0 복원 전 백업

- `Copy-Item -Recurse`: `app/lotto4` → `backups/lotto4_temp_20260509/lotto4\`
- (초기 `xcopy`는 인용부호 문제로 실패 → PowerShell로 대체.)

### A-1 `app/lotto4` 삭제

- `Remove-Item -Recurse -Force` 로 제거 완료.

### A-2 `app/main.py` 복원

- 삭제 항목:
  - `from app.lotto4.v13_routes import router as lotto4_v13_router`
  - `app.include_router(lotto4_v13_router)`
  - `startup` 내 `init_lotto4_db()` 블록
- **복원 후 SHA256 (SHA-256):**  
  `00D0220D01663D788D95A5093F5ABAAA6D52F50DB7516B4A0F54F830B9CECE5D` — 파일: `app/main.py`

### A-3 `app/static/index.html` 복원

- 제거: `lotto4.css` 링크, 네비 `data-tab="lotto-army4"`, `tab-lotto-army4` 섹션 전체, `lotto4TierWinsModal*`, `lotto4.js` 스크립트 태그.

### A-4 UI 파일 삭제

- `app/static/js/lotto4.js` — 삭제
- `app/static/css/lotto4.css` — 삭제

### A-5 `run_app_v13.py` 삭제

- 프로젝트 루트 `run_app_v13.py` — 삭제

### A-6 DB `army4` 테이블 삭제

- `Python sqlite3` 로 `D:\MONEY lol\My_Library\data\lotto.db` 에 대해 실행:
  - `DROP TABLE IF EXISTS lotto_predictions_army4;`
  - `DROP TABLE IF EXISTS lotto_brain_weights_army4;`
  - `DROP TABLE IF EXISTS lotto_analysis_army4;`
  - `DROP TABLE IF EXISTS lotto_weight_log_army4;`

### A-7 검증 결과

| 검증 | 결과 |
|------|------|
| `python -c "from app.main import app; print('app OK')"` | **app OK** |
| `GET /api/lotto3/v12/brain/status` (TestClient + lifespan) | **200** |
| `app/` 하위 파일에서 `lotto4` | **0건** |
| `app/` 하위 파일에서 `v13_` | **0건** |
| `app/` 하위 파일에서 `army4` | **0건** |
| `app/lotto3/` vs `20260509_step0_lotto3_sha256.txt` | **39파일·100% 일치** |
| 브라우저 1·2·3군 탭 | 에이전트 미실행(로컬 확인 권장). |

**A-7 통과 후 PHASE B 진행.**

---

## PHASE B — `d:\3kweon` 4군 이전

### B-0 백업에서 복사

- 원본: `backups\lotto4_temp_20260509\lotto4\`
- 대상: `d:\3kweon\app\lotto4\`

### B-1 독립 구조

| 경로 | 설명 |
|------|------|
| `d:\3kweon\app\__init__.py` | 빈 파일 |
| `d:\3kweon\app\main_v13.py` | 4군 전용 FastAPI (`lotto4` 라우터만, startup에서 `init_lotto4_db`) |
| `d:\3kweon\run_v13.py` | `PYTHONPATH`에 My_Library 우선, `app.lotto4`를 `d:\3kweon\app\lotto4`로 주입 후 `main_v13` 로드, **uvicorn 127.0.0.1:6124** |
| `d:\3kweon\requirements.txt` | My_Library `requirements.txt` 복사 |

**런타임 의존성:** `lotto4` 내부 모듈이 `app.lotto.*` 등을 import 하므로, `run_v13.py`는 **`D:\MONEY lol\My_Library`를 `sys.path` 맨 앞에 넣음.**  
코드 레벨에서는 계속 **`*_army4` / `v13_` 만** 사용(기존 복제본 기준).

### B-2 DB 공유

- `d:\3kweon\app\lotto4\models.py`, `v13_models.py` 의 `LOTTO_DB_PATH`를 다음으로 고정:  
  `D:\MONEY lol\My_Library\data\lotto.db`
- 4군은 **`*_army4` 테이블만** 사용. 3군 테이블은 4군 코드에서 참조하지 않음(문자열 grep으로 `army3`/`lotto3`/`v12_` 0건 확인).

### B-3 보고서·기억 경로

| 용도 | 경로 |
|------|------|
| 커서보고서 | `D:/MONEY lol/My_Drive_Sync/커서보고서/` |
| 동생기억 | `D:/MONEY lol/My_Drive_Sync/동생기억/` |
| SUMMARY | `D:/MONEY lol/My_Drive_Sync/SUMMARY/` |

### B-4 4군 독립 검증

| 검증 | 결과 |
|------|------|
| FastAPI `TestClient`로 `GET /api/lotto4/v13/brain/status` (`run_v13`와 동일 주입 방식) | **200** |
| `d:\3kweon\app\` 내 `*.py`에서 `lotto3` / `army3` / `v12_` | **각 0건** |
| `My_Library`에서 `app` 하위 `lotto4` 문자열 | **0건** |

### B-5 백업 폴더 정리

- `D:\MONEY lol\My_Library\backups\lotto4_temp_20260509` — **삭제 완료**  
  (이후 4군 소스는 `d:\3kweon\app\lotto4` 만 유지.)

---

## RULES_FIXED 제안 — R25 (4군 커서 접근 제한)

- **4군** Cursor 창(`d:\3kweon`): 다음에 대해 **읽기·쓰기 금지** —  
  `app/lotto/`, `app/lotto2/`, `app/lotto3/`, `app/static/js/lotto.js`, `lotto2.js`, `lotto3.js`, `app/main.py`(My_Library)
- **4군**이 허용하는 My_Library 경로 예: 공유 DB 파일, `My_Drive_Sync/`(보고서·기억·SUMMARY).
- **3군** Cursor 창(My_Library): `app/lotto4/` **접근 금지**(본 복원으로 디렉터리 없음).
- 위반 시 **작업 중단·롤백.**

---

## 최종 구조(요약)

```text
D:\MONEY lol\My_Library\     ← 1·2·3군 + 도서관 (4군 흔적 제거)
d:\3kweon\                   ← 4군 전용
├── app\lotto4\
├── app\main_v13.py
├── app\__init__.py
├── run_v13.py
└── requirements.txt
```

*(DB 파일 실체: `My_Library\data\lotto.db` — 4군은 `*_army4`만 사용.)*

---

*보고서 끝.*
