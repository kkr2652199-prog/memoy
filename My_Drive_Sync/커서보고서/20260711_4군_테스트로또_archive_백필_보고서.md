# 4군 테스트로또 archive 백필 보고서

📅 2026-07-11 KST · IMPLEMENT (데이터 백필 전용)

## 작업 범위

- **대상**: `app/testlotto/`, `data/lotto_testlotto.db`, `tools/run_testlotto_sync_archive.py`
- **미변경**: `app/lotto4/`, `app/hyodo/` 예측 로직

## STEP 0 — DB lock 원인 제거

| 항목 | 결과 |
|------|------|
| run_v13.py (6124) | 종료 확인 후 재가동 |
| walk-forward 백그라운드 | 없음 |
| 동시 쓰기 | 없음 |

## STEP 1 — archive 단독 재실행 (판매점 포함)

```bash
python tools/run_testlotto_sync_archive.py --start 134 --end 1231
```

- `--no-stores` **미사용** → 판매점 조회 시도 포함
- 소요: 약 53분 (API 타임아웃 구간 포함)
- 1차 결과: synced **1076**, failed **22** (동행복권 ConnectTimeout)

### gap 재시도

```bash
python tools/run_testlotto_sync_archive.py --start 639 --end 648
python tools/run_testlotto_sync_archive.py --start 1071 --end 1082
```

- 재시도: synced **22**, failed **0**

## STEP 2 — 판매점 실패 내성 보강

`app/testlotto/draw_archive.py` 변경:

- `fetch_and_save_draw_archive()` 전체 try/except — 1회 실패해도 루프 중단 없음
- 판매점 조회/저장 개별 try/except
- SPA/HTML 미제공 시 `store_fetch_status='pending'` (재시도 가능)
- `sync_draw_archive_range()` 회차별 예외 처리
- `models.get_lotto_db()` — `timeout=60.0` (lock 완화)

## STEP 3 — 검증 (READ-ONLY)

| 지표 | 건수 |
|------|------|
| **draw_detail** (archive) | **1231** / 1231 |
| **등수(prize_tiers)** | **1231** (5등 포함 1231) |
| **판매점(win_stores)** | **0** |
| store_status pending | 1098 |
| store_status skipped | 113 (1~133 기존) |
| **detail gap** | **0** |
| **tier gap** | **0** |

### lock 재발 여부

- 이번 세션: **`database is locked` 없음**
- 원인 회피: 서버·walk-forward 정지 후 archive 단독 실행

### 실패·pending

| 구분 | 회차 |
|------|------|
| lt645 실패 (최종) | 없음 (22건 재시도 완료) |
| 판매점 pending | 134~1231 전 구간 (SPA·HTML 미제공) |

판매점은 `store.do` SPA 전환으로 HTML 파싱 불가 — 등수·당첨금·raw JSON은 정상 저장.

## STEP 4 — 서버 재가동·API 확인

- `python run_v13.py` → 포트 **6124** 기동
- `/api/testlotto/detail/draw/500`: actual `[3,4,12,20,24,34]` bonus 41, tiers 5
- `/api/testlotto/detail/draw/1000`: actual `[2,8,19,22,32,42]` bonus 39, tiers 5

## 변경 파일 (소스)

- `app/testlotto/draw_archive.py`
- `app/testlotto/models.py`
- `tools/verify_archive_gaps.py` (검증 스크립트)

## 다음 작업

1. 판매점 SPA 대응 파서 (pending 1098회 재시도)
2. 보조 4뇌 UI (`aux_analysis_json`)
3. 구간별 흐름 등수 컬럼
