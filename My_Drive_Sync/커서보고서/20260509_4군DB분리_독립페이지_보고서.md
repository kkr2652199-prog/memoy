# 4군 DB 완전 분리 + 독립 페이지 보고서

- **일자:** 2026-05-09 (KST)
- **목적:** `d:\3kweon`만으로 4군 운영 — DB `lotto4.db` 독립, 웹 UI `6124`, **My_Library 경로·`sys.path` 참조 제거**.
- **제한 준수:** `My_Library`의 `app/lotto/`, `lotto2/`, `lotto3/`, `main.py` 는 **도구로 읽기·쓰기 없음.** (4군용 `app/lotto`·`app/lotto2`는 **셸 `robocopy`로만** 동기화.)
- **보고서 저장:** 본 파일 경로 그대로 Drive 동기화.

---

## STEP 1 — DB 완전 분리

### 1-A 디렉터리

- `d:\3kweon\data\` 생성.

### 1-B 경로

- `app/lotto4/models.py`, `app/lotto4/v13_models.py` 의 `LOTTO_DB_PATH` →  
  `Path(r"d:\3kweon\data\lotto4.db")`
- 벤더 `app/lotto/models.py`: `LOTTO_DB_PATH = Path(__file__).resolve().parent.parent.parent / "data" / "lotto4.db"`
- `app/lotto2/*.py`: `"lotto.db"` → `"lotto4.db"` 일괄 치환.
- `app/lotto2`·`app/lotto` 내 `.before_*` 백업 복사본 삭제.

### 1-C `lotto_draws` 복사

- 도구: `tools/import_lotto_draws.py` (인자로만 소스 DB 경로 전달 — **저장소 내 `My_Library` 문자열 없음**).
- 실행 예:  
  `python tools\import_lotto_draws.py "<소스_절대경로>\lotto.db" "d:\3kweon\data\lotto4.db"`
- **결과:** `SELECT COUNT(*) FROM lotto_draws` → **1222**

### 1-D 4군·1군·2군 스키마 및 시드

- `tools/init_4g_db.py` 로 일괄 호출:
  - `init_lotto_db()` — 1군 테이블 (`lotto_predictions` 등) 빈 구조(당첨표는 이미 존재).
  - `init_lotto2_db()`
  - `init_lotto4_db()`
  - `init_v13_seeds()` → **v13_ 가중치 7행**, `lotto_weight_log_army4` draw_no=0 로그 **7행** 확인.

### 1-E 검증

| 항목 | 결과 |
|------|------|
| `lotto_draws` 행 수 | **1222** |
| 주요 테이블 | `lotto_draws`, `lotto_predictions_army4`, `lotto_brain_weights_army4`, `lotto_analysis_army4`, `lotto_weight_log_army4` 및 1·2군 호환 테이블 |
| `grep My_Library` under `d:\3kweon\app\` | **0건** |

---

## STEP 2 — 독립 웹 페이지

- `app/static/css/lotto4.css`
- `app/static/js/lotto4.js` — API `/api/lotto4/v13/*`, 8뇌 표시 순서(지시서):  
  hyena → fusion → snake → lstm → stat → run → offset → contrarian  
- `app/static/index.html` — 타이틀 **「나의 지식 도서관 — 4군 AI 예측」**, 1·2·3군 탭 없음, **카톡 복사** 버튼.
- `app/main_v13.py`: `StaticFiles`, `GET /` → `index.html`.

---

## STEP 3 — My_Library 제거

- `run_v13.py`: `sys.path.insert(0, d:\3kweon)` 만 사용. `importlib` lotto4 주입 제거.
- 4군 전용 의존 코드: **`robocopy`로 `app/lotto`, `app/lotto2` 전체를 `d:\3kweon\app\`에 보관** 후 `lotto4.db`만 사용하도록 패치.
- 진입점 주석/1군 파일 주석 중 `My_Library` 문자열 제거( `predict_lstm.py` 주석 1곳 등).

### 3-C 검증

| grep 대상 | 결과 |
|-----------|------|
| `My_Library` / `MONEY lol` in `d:\3kweon\` | **0건** |
| `sys.path` in `d:\3kweon\app\` | **0건** |

---

## STEP 4 — 최종 검증

| 검증 | 결과 |
|------|------|
| `python run_v13.py` | **6124** (`uvicorn` 설정 동일) |
| `GET /` | **200** (HTML) |
| `GET /api/lotto4/v13/brain/status` | **200** (TestClient) |
| `lotto4.db` | 존재, `lotto_draws` 1222 |
| `My_Library\app\` 내 lotto4/v13/army4 문자열 | **0건** |
| `My_Library`에서 `python -c "from app.main import app; print('app OK')"` | **app OK** (본 작업에서 해당 파일 미수정) |
| 브라우저 1·2·3군 | 로컬에서 `8123` 등 기존 서버로 확인 권장 |

---

## STEP 5 — diff 요약 (줄 번호 기준 발췌)

> 전체 트리: `app/lotto`, `app/lotto2` 는 **이전 스냅샷 전체 복사 + 일괄 `lotto4.db` 경로 치환**이라 여기선 **핵심 신규·소스 패치**만 unified 형식으로 기록한다.

### `d:\3kweon\app\lotto\models.py` (헤더·경로)

```diff
--- lotto/models.py (이전: DATA_DIR/lotto.db)
+++ lotto/models.py
@@ -5,9 +5,8 @@
 import logging
 import sqlite3
+from pathlib import Path
 
-from app.config import DATA_DIR
-
 logger = logging.getLogger(__name__)
 
-LOTTO_DB_PATH = DATA_DIR / "lotto.db"
+LOTTO_DB_PATH = Path(__file__).resolve().parent.parent.parent / "data" / "lotto4.db"
```

### `d:\3kweon\app\lotto4\models.py`

```diff
@@ -5,5 +5,5 @@
 import sqlite3
 from pathlib import Path
 
-LOTTO_DB_PATH = Path(r"D:\MONEY lol\My_Library\data\lotto.db")
+LOTTO_DB_PATH = Path(r"d:\3kweon\data\lotto4.db")
```

### `d:\3kweon\app\lotto4\v13_models.py`

```diff
@@ -15,5 +15,5 @@
 import sqlite3
 from pathlib import Path
 
-LOTTO_DB_PATH = Path(r"D:\MONEY lol\My_Library\data\lotto.db")
+LOTTO_DB_PATH = Path(r"d:\3kweon\data\lotto4.db")
```

### `d:\3kweon\app\lotto\predict_lstm.py` (주석만)

```diff
@@ -37,5 +37,5 @@
 RETRAIN_INTERVAL = 50
-# 프로젝트 루트: .../My_Library (app/lotto/ 기준 3단계 상위)
+# 프로젝트 루트: app/lotto/ 기준 3단계 상위 (= 4군 루트 d:\3kweon)
 _PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent
```

### `d:\3kweon\run_v13.py` (전체 교체)

```1:20:d:\3kweon\run_v13.py
"""4군 서버 진입점: sys.path 에 d:\3kweon 만 추가 (외부 라이브러리 트리 없음)."""

from __future__ import annotations

import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent
sys.path.insert(0, str(ROOT))

if __name__ == "__main__":
    import uvicorn

    uvicorn.run(
        "app.main_v13:app",
        host="127.0.0.1",
        port=6124,
        reload=False,
        log_level="info",
    )
```

### `d:\3kweon\app\main_v13.py` (전체 교체)

```1:61:d:\3kweon\app\main_v13.py
"""4군 전용 FastAPI: lotto4 API + 독립 정적 페이지."""

from __future__ import annotations

import logging
from pathlib import Path

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import FileResponse
from fastapi.staticfiles import StaticFiles

from app.lotto4.v13_routes import router as lotto4_v13_router

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(name)s %(levelname)s %(message)s",
)

app = FastAPI(title="나의 지식 도서관 — 4군 AI 예측", version="0.1.0")

_ALLOW_ORIGIN_REGEX = (
    r"https?://("
    r"localhost|127\.0\.0\.1|"
    r"192\.168\.\d{1,3}\.\d{1,3}|"
    r"10\.\d{1,3}\.\d{1,3}\.\d{1,3}|"
    r"172\.(?:1[6-9]|2[0-9]|3[0-1])\.\d{1,3}\.\d{1,3}"
    r")(?::\d+)?$"
)

app.add_middleware(
    CORSMiddleware,
    allow_origin_regex=_ALLOW_ORIGIN_REGEX,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

_STATIC_DIR = Path(__file__).resolve().parent / "static"
app.mount("/static", StaticFiles(directory=str(_STATIC_DIR)), name="static")

app.include_router(lotto4_v13_router)


@app.get("/")
async def root():
    return FileResponse(str(_STATIC_DIR / "index.html"))


@app.on_event("startup")
async def startup():
    from app.lotto.models import init_lotto_db
    from app.lotto2.models import init_lotto2_db
    from app.lotto4.models import init_lotto4_db
    from app.lotto4.v13_models import init_v13_seeds

    init_lotto_db()
    init_lotto2_db()
    init_lotto4_db()
    init_v13_seeds()
```

### 신규 정적·도구 파일

- `app/static/index.html` — 전체 신규 (본 보고서에 전문 생략, 저장소 참조).
- `app/static/js/lotto4.js` — 전기 신규.
- `app/static/css/lotto4.css` — 전체 신규.
- `tools/import_lotto_draws.py` — 전체 신규.
- `tools/init_4g_db.py` — 전체 신규.

### `app/lotto2/*.py`

- 내용: 모든 ` "lotto.db" ` 문자열을 ` "lotto4.db" ` 로 치환(동일 상대 경로 `data/`).

---

## 최종 구조 (요약)

```text
d:\3kweon\
├── app\
│   ├── lotto\          ← 벤더(1군 로직, DB=lotto4.db)
│   ├── lotto2\         ← 벤더(2군 스키마·함수, DB=lotto4.db)
│   ├── lotto4\
│   ├── main_v13.py
│   ├── static\
│   │   ├── index.html
│   │   ├── css\lotto4.css
│   │   └── js\lotto4.js
│   └── __init__.py
├── data\lotto4.db
├── tools\import_lotto_draws.py
├── tools\init_4g_db.py
├── run_v13.py
└── requirements.txt
```

**My_Library** 는 본 4군 런타임·소스 grep 기준 **완전 분리** (당첨 `lotto_draws` 최초 이관은 **일회성 CLI 인자**로만 수행).

---

*보고서 끝.*
