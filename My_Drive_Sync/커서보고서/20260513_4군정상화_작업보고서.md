# 4군 정상화 작업 보고서

**날짜**: 2026-05-13  
**대상**: `d:\3kweon` (4군, `localhost:6124`)  
**제한**: My_Library 미접근, `d:\3kweon` 내부만 수정

---

## 지시문 검토 (간략)

| 항목 | 내용 |
|------|------|
| 이슈 | TASK 2는 엔진(`v13_engine_v2.py`)에 대해 `lotto` 관련 import를 `app.lotto4`만 허용한다고 명시함. `v13_routes.py`는 기존처럼 `app.lotto.routes.get_draws_for_backtest` 등 **1군 조회 헬퍼**를 사용함 → 엔진과 라우트의 import 범위가 다름. |
| 처리 | 엔진은 지시대로 `app.lotto4` + 표준 라이브러리만 사용. 라우트의 1군 헬퍼 의존은 중복 DB 조회 방지 목적로 **유지**함. |
| 기타 | DB 정리는 인라인 SQL/PowerShell 특수문자 이슈로 `tools/_normalize_db_20260513.py`로 1회 실행 후 스크립트는 삭제함. |

---

## TASK 1 — DB 정리

**실행**

- `army4_%` 가중치 행 삭제(있을 경우)
- 백업: `lotto_predictions_army4_backup_20260513`, `lotto_brain_weights_army4_backup_20260513`, `lotto_weight_log_army4_backup_20260513`
- `lotto_predictions_army4` / `lotto_weight_log_army4` / `lotto_brain_weights_army4` TRUNCATE
- 8뇌(`v13_*`) 가중치 1.0 시드

**검증**

- `COUNT(*) WHERE brain_tag LIKE 'army4_%'`: **0**
- 가중치 테이블: **8행**, 전부 `v13_*`, `current_weight=1.0`
- 예측 테이블(초기화 직후): **0건** (TASK 5 예측 호출 후 40건)

---

## TASK 2 — `v13_engine_v2.py` 재작성

**변경 요지**

- `BRAIN_REGISTRY` + `importlib`로 `app.lotto4.brains.*`의 `predict(draw_no, db_path)` 호출
- v11 / lotto2 / army2 import **제거**
- 뇌당 5세트(부족 시 패딩), `lotto_predictions_army4` INSERT, `_score_and_update_*` → `update_v13_v2_weights`

**파일**

- `app/lotto4/v13_engine_v2.py` — 전면 정리
- `app/lotto4/v13_weights_v2.py` — 8뇌 가중치·라벨·메서드·Hedge 갱신 로직 정렬
- `app/lotto4/models.py` — `init_lotto4_db`에서 `army4_*` 초기 가중치 INSERT **제거** (v13 시드만)
- `app/lotto4/brains/` — `trend`, `bayesian` 유지; `transformer`, `graph`, `generator`, `rl_agent`, `ensemble`, `contrarian_v2`는 지시 플레이스홀더 `predict` 추가 (`ensemble`은 `(draw_no, db_path)` 시그니처로 통일)

---

## TASK 3 — `v13_routes.py`

**변경 요지**

- `BRAIN_METHOD` 8뇌 한글 매핑
- `api_predict_v11` → `api_predict_v13`, `api_backtest_v11` → `api_backtest_v13`
- `brain/status`, `dashboard-summary`, `predictions` 엔드포인트가 **8뇌 고정 순서**·`V13_BRAIN_LABEL_UI` 반영
- 파일명·경로 문자열에서 `v11`/`lotto2` **제거** (공유 함수 import는 `app.lotto.routes` 유지 → 문자열 grep 대상 아님)
- 일부 docstring의 “V11” 문구를 4군 설명으로 정리

---

## TASK 4 — 프론트 (`lotto4.js`, `index.html`)

**변경 요지**

- `BRAIN_LABELS`, `BRAIN_PRIMARY` / `BRAIN_SECONDARY` 8뇌
- 두뇌예측 탭·명예의전당 8뇌 옵션
- 사이드바 “3군 v11 동일” 제거 → “4군 v13 독립 엔진”
- `lotto4.js` 캐시 버스터 `?v=20260513b`

---

## TASK 5 — 검증 결과

| 검증 | 결과 |
|------|------|
| `python -c "from app.main_v13 import app; print('app OK')"` | 통과 |
| `GET /api/lotto4/v13/brain/status` | 200, 프로필 8뇌 |
| `GET /api/lotto4/v13/brain/ranks` | 200, 8뇌, `army4_*` 없음 |
| `GET /api/lotto4/v13/dashboard-summary` | 200, `brain_power` 8뇌 |
| `POST /api/lotto4/v13/predict/1224` | 200, `v13_sets` 40 |
| `GET /api/lotto4/v13/predictions/1224` | 200, 40건 |
| SQLite `DISTINCT brain_tag` (1224) | 8종 `v13_*` |
| SQLite 가중치 8행 / `army4_%` 0 | 통과 |
| `grep v11\|lotto2\|army2` → `v13_engine_v2.py` | 0건 |
| `grep v11\|lotto2` → `v13_routes.py` | 0건 |
| `grep My_Library` → `app\` | 0건 |

**실패 항목**: 없음 (로컬 검증 기준).

**참고**: `app/lotto2/`, `app/lotto4/legacy/` 등에는 과거 v11·v13 명칭 코드가 남아 있을 수 있음. 지시서 grep 범위는 엔진·라우트 파일로 한정됨.

---

## 변경 파일 목록 (요약)

| 파일 | 요약 |
|------|------|
| `data/lotto4.db` | 백업 테이블 3개, army4 정리, v13 시드, TASK5 예측 샘플 |
| `app/lotto4/v13_engine_v2.py` | 브리지 제거, BRAIN_REGISTRY |
| `app/lotto4/v13_weights_v2.py` | 8뇌 일원화 |
| `app/lotto4/v13_routes.py` | API 이름·8뇌 응답·docstring |
| `app/lotto4/models.py` | DB 초기화 army4_* 제거 |
| `app/lotto4/brains/*.py` | 플레이스홀더/시그니처 |
| `app/static/js/lotto4.js` | 8뇌 UI |
| `app/static/index.html` | 탭·사이드바·버전 |

---

*보고서 생성: Cursor 에이전트 자동 정리*
