# 3군 8뇌 시스템 기능 버그 정밀 정찰 보고서 (READ-ONLY)

- 작성 시각: 2026-05-02 09:00 KST (정찰 실행: 동일일 세션)
- 범위: `My_Library/app/lotto3/` 코드 읽기 + `data/lotto.db` **SELECT만** (`target_draw_no = 1222` 미접근, `< 1222` 조건만 사용)
- **절대 룰 준수**: `app/lotto/`, `app/lotto2/` 디렉터리 **미접근**(파일 읽기·해시·rg 제외)
- DB: `D:\MONEY lol\My_Library\data\lotto.db`

---

## STEP 0 — SHA256 사전·사후 (v7 baseline 25파일)

**지시서와의 충돌**: STEP 0는 문서상 「1군 9 + 2군 6 + 보호 4 + 변경 6」25파일 전수 해시인데, **동일 지시서의 절대 룰**로 `app/lotto/`, `app/lotto2/` 접근이 금지되어 **1·2군 경로 파일 해시는 수행 불가**로 정찰을 중단하지 않고, **검증 가능한 범위**로 대체함.

### 0-A. `app/lotto3/*.py` 16개 파일 SHA256 (사전 = 사후 동일)

PowerShell `Get-FileHash -Algorithm SHA256`, 파일명 정렬 기준.

| 파일 | SHA256 (사전·사후 동일) |
|------|-------------------------|
| `__init__.py` | `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855` |
| `models.py` | `8524F0CDFF1214507A304936050B44FB90A66E911FF94AEFCDADB4FEDB8AB0B9` |
| `predict_combo.py` | `8F8B7B26FB799AE4F3F48C202486C2F7EED0BA722B4168BD3195B1DFD3025F20` |
| `predict_contrarian.py` | `4B5275CBFE32997139F7F16278B73AF9B468D9835F6D14B24B94AB1E5F8245F7` |
| `predict_dead_zone.py` | `431947332259A446A352A3F58E1DC60015BD462496799160DBCD7B35E7B67B50` |
| `predict_lstm.py` | `857D53AC84BED00634543418D29BFC080C91A6F5DDF34B197D87B70382A18359` |
| `predict_markov.py` | `9B5EAFD6D9E5817ACAE895886546FA2E4617C2E8F7018FE1270C1FB13D21F54` |
| `predict_offset.py` | `13D873CE8FDB67D4841F499639F51016020A00384330E0B62A8A7BF73D999B9C` |
| `predict_run.py` | `43219CD74D8F1B513A7D630AC8B9312C96ACF1523426A684C18A9BDEE7045161` |
| `predict_stat.py` | `FBB134F05BED94FD081197268976FE0DE5144720A08F796E3619724BF8363A3A` |
| `v12_engine.py` | `C107E021363ADAFDB5B559A0A18D1187A070A04C7BACDC203189FF898DBD5626` |
| `v12_fusion.py` | `E7D2160D116E7AEBCA9A8B04B29C7AC404E6FFB9EFFBEDC4E57FC2E7E0087B21` |
| `v12_fusion_v5.py` | `B5504A05E648614F0FE29348CF364FB6ED3B8E46EC3FC072256B0992E091913A` |
| `v12_models.py` | `F44F52EA56890D1C96E5D01BF2FF734A3A7E58BE7CF0E56C608A105633B7C007` |
| `v12_routes.py` | `5C7BADD7EED849DB7E4C20AD90E5CA007A203B343619A00242EE42CCB0988869` |
| `v12_snake.py` | `A4257596916B6231B2A536E72E5CBCEC7492B1924FF78168E082EB3912CF6EB2` |

**결론**: 문서 v7 **25/25**는 **미이행**(1·2군 금지). **lotto3 16/16** 해시 사전·사후 **PASS(변동 0)**.

---

## STEP 1 — 8뇌 predict 계열 raw (`predict_*.py`)

### 1-A. `rg "SELECT" app/lotto3/predict_*.py` (실제 매칭)

- **`predict_contrarian.py`만** `SELECT` 존재 (1·2군 예측 로드용).

### 1-B. `rg "WHERE" app/lotto3/predict_*.py`

- **`predict_contrarian.py`**: `WHERE target_draw_no = ?` (1군 `lotto_predictions`), `WHERE target_draw_no = ?` (2군 `lotto_predictions_army2`).

### 1-C. `draw_no` / `target_draw_no` 비교 (`app/lotto3/*.py` 확장 검색)

- `predict_contrarian.py`: `_filter_d_training` 에서 `dn >= target_draw_no` 제외 → **미래 회차 미포함**.
- `v12_models.py`, `models.py`, `v12_engine.py`, `v12_routes.py`, `v12_snake.py`, `v12_fusion_v5.py`: `draw_no < ?`, `target_draw_no < ?`, 동일 회차 `=` 조회 등 — **`draw_no > target` 형태의 미래 누수 패턴 없음**(본 정찰 범위 grep 기준).

### 1-D. `predict_stat.py` / `predict_run.py` / `predict_offset.py`

- **DB `SELECT` 없음**. 입력은 호출자가 넘기는 `training` / `miss_draws` 리스트.
- `predict_stat.py`는 **1군** `get_statistical_prob_vector` 등 호출(코드상 의존; **1군 모듈 본문은 금지로 미검**).

---

## STEP 2 — 1등 0건 6뇌 원인 (raw 요약)

DB (`target_draw_no < 1222`, `matched_count = 6` 집계):

| brain_tag | 1등(6개) 건수 |
|-------------|---------------|
| v12_stat | 0 |
| v12_run | 0 |
| v12_offset | 0 |
| v12_contrarian | 0 |
| v12_hyena | 0 |
| v12_snake | 0 |
| v12_lstm | 1 |
| v12_fusion | 7 |

**핵심 함수·무작위성**

| 뇌 | 생성 핵심 | PMF/스코어 | 무작위 |
|----|-------------|------------|--------|
| stat | `army3_stat_predict` → 1군 `_statistical_predict` | 1군 통계 PMF | 1군 쪽 |
| run | `army3_run_predict` | run 빈도 행렬 점수 | `np.random.default_rng(seed)` seed=최근10회차 해시 |
| offset | `army3_offset_predict` | 간격 분산·IQR·자기상관 등 | 동일 방식 RNG |
| contrarian | `army3_contrarian_predict` | D페어 PMF × gap10(1.5) | `random.Random(hash(...))` |
| hyena | `v12_engine._v12_hyena_predict` | 15개 풀 `combinations` **5005** 전수 합의 점수 | 상위50 가중 샘플 `random.choices` |
| snake | `v12_snake_predict_sets` | 1군 대비 Jaccard & tier1 | `random` 기반(파일 후반부) |

**hyena 5005**: `v12_engine.py` — `_v12_evaluate_combinations` 에서 `for combo in combinations(pool_nums, 6):` 명시.

**contrarian gap10 / 페어 부스트**

- gap10: `_calculate_gap10_weights` → 고정 집합 `_GAP10_NUMS` 에 대해 **가중 1.5**, 그 외 **1.0** (`predict_contrarian.py` 116–118행 근처).
- 페어: `_pair_boost_score` 에서 `_GAP_PAIR_BOOST` 에 속한 페어당 **`+2.0`** 누적. 지시서의 「페어 부스트 **30**」은 코드상 **곱 30**이 아니라 **특정 페어 집합 +2.0** 구조(숫자 30은 페어 `(30,34)` 등 데이터에 등장).

**snake Jaccard 0.4 / contrarian 50.0**

- Jaccard: `v12_snake.py` `_is_diff_from_army1` — `jac >= threshold`(기본 0.4)면 탈락.
- 가중치: `_V11_SNAKE_WEIGHTS` 에 `v12_contrarian: 50.0`, `v12_hyena: 30.0` (합의 가중에 반영; snake는 6뇌 셋트 메타 선택에 사용).

---

## STEP 3 — v12_lstm 6055 vs 6075(실측 6080) 행 누락

### SQL raw (`target_draw_no < 1222`)

```text
v12_stat|6080|6|1221
v12_fusion|6080|6|1221
v12_lstm|6055|11|1221
```

### 누락 회차 (6~26 구간에서 lstm 미존재)

```text
6,7,8,9,10
```

(5회차; 지시서「20행」과 수치 다를 수 있음 — **본 DB 기준 25행 차이** = 6080−6055.)

### `predict_lstm.py` 최소 데이터

- `army3_lstm_predict`: `len(miss_draws) < 10` 이면 **`return []`** (세트 미생성).
- `army3_lstm_prob_vector`: 동 조건 시 균일 PMF `{1/45}`.

**결론**: 초기 회차에서 **학습용 `miss_draws` 길이 < 10** 이면 DB에 lstm 행이 안 쌓이는 설계와 **정합**. 별도 버그라기보다 **초기 공백(설계)**.

---

## STEP 4 — Hedge η=2.0 및 가중치

### 함수명

- 지시서 `update_brain_weight` — **해당 식별자 없음**.
- 실제: `v12_models.update_v12_weights` (`update_v12_weights`).

### 공식 (코드 그대로)

- `new_w = base * math.exp(V11_HEDGE_ETA * avg_m / 6.0)` , `V11_HEDGE_ETA = 2.0`
- `base` = `V11_SEED_WEIGHTS` 시드 (예: contrarian 2.5, stat 1.5 등).
- `avg_m`: `lotto_predictions_army3` 에서 `target_draw_no <= ? AND target_draw_no > ?-last_n`, `last_n` 기본 **50**, `V11_BRAINS` 집합만 `GROUP BY brain_tag` 의 `AVG(matched_count)`.

### 정규화

- **뇌별 독립 지수 갱신**이며, `sum(weights)=1` 강제 **없음**.

### `lotto_brain_weights_army3` SELECT dump (전체 행)

```text
army3_combo|2.5|0.0|0|0|0
army3_fusion|2.0|0.0|0|0|0
army3_hyena|2.0|0.0|0|0|0
army3_lstm|2.0|0.0|0|0|0
army3_markov|1.0|0.0|0|0|0
army3_stat|1.5|0.0|0|0|0
v12_combo|3.1570058582345|0.7|20|14|10
v12_contrarian|3.35222419793119|0.88|25|22|10
v12_fusion|2.86665882912068|1.08|25|27|10
v12_hyena|3.18933951645663|1.4|25|35|10
v12_lstm|3.6393818551964|1.796|250|449|1221
v12_markov|1.27464367207303|0.728|250|182|1149
v12_offset|1.34088967917248|0.88|25|22|10
v12_run|1.30560517206495|0.8|25|20|10
v12_stat|2.09341863762913|1.0|25|25|10
```

(컬럼 순서: `brain_tag|current_weight|recent_avg_match|total_predictions|total_matches|last_updated_draw`)

---

## STEP 5 — v12_combo 잔존·영향

### 예측 테이블

```sql
SELECT COUNT(*), MIN(target_draw_no), MAX(target_draw_no)
FROM lotto_predictions_army3
WHERE brain_tag='v12_combo' AND target_draw_no < 1222;
```

**결과**: **0건** (MIN/MAX 없음).

### 코드 사용

- `rg "predict_combo" app/lotto3/` → `v12_fusion.py`, `v12_fusion_v5.py` 에서 `army3_combo_prob_vector` **import만**.
- `v12_routes.py` 에 `combo` 문자열 **매칭 없음**(API 전용 경로 없음).

### fusion 입력

- `v12_fusion_v5._v12_load_brain_pmfs`: 키 **`v12_stat`, `v12_markov`, `v12_combo`, `v12_lstm`** — **PMF로 fusion에만 사용**. DB `brain_tag='v12_combo'` 예측 행과는 별개.

**결론**: **예측 행 관점에서는 잔재·미사용(0행)**. **fusion PMF 관점에서는 사용 중**. DB 가중치에 `v12_combo` 행이 있으나 `V11_BRAINS` 튜플에는 combo 미포함 → **헤지 업데이트 대상 외**(코드상).

---

## STEP 6 — numpy RuntimeWarning·entropy·log

### `rg "entropy\(" app/lotto3/`

- `v12_fusion_v5.py`: 주석 + `_v12_get_entropy_weights` → **`from app.lotto.predict_entropy import get_entropy_weights`** (1군 모듈 — **본 정찰에서 본문 미검**).
- `v12_fusion.py`: `_entropy_clip` 에서 **`math.log(p + 1e-12)`** — log(0) 회피.

### 재현

- **본 세션에서 경고 재현 실행 안 함**(READ-ONLY + 시간 제약). 1군 `predict_entropy` 내부는 **접근 금지로 원인 확정 불가**.

---

## STEP 7 — DB legacy 예측 + `_tmp_*.py`

### SQL (`target_draw_no < 1222`)

```sql
SELECT COUNT(*) FROM lotto_predictions_army3
WHERE brain_tag NOT IN (
  'v12_stat','v12_run','v12_offset','v12_contrarian','v12_lstm',
  'v12_fusion','v12_hyena','v12_snake','v12_combo'
);
```

**결과: 0** — 지시서의 「legacy 6행」과 **불일치**(현 DB 스냅샷 기준).

### `_tmp_*.py`

- `app/lotto3/_tmp_*.py`: **0건**
- `My_Library/_tmp_*.py`: **4건**

| 파일 | 크기(byte) | 수정시각(로컬) | 헤더 요약 |
|------|------------|----------------|-----------|
| `_tmp_llm_diag.py` | 816 | 2026-04-25 15:24 | `logging`, `sys.path` 삽입 |
| `_tmp_q4.py` | 554 | 2026-04-25 15:24 | `app/lotto/predict_llm.py` 라인 스캔 스크립트 |
| `_tmp_special_backtest_step1.py` | 1655 | 2026-04-27 20:56 | sqlite3 백테 스텝 |
| `_tmp_v10v2_counts.py` | 833 | 2026-04-30 08:11 | `lotto_predictions_army2` COUNT |

---

## STEP 8 — 의존성 그래프 raw

- **`predict_*.py` → `lotto_predictions_army3` SELECT**: **없음**(contrarian만 1·2군 테이블 SELECT).
- **`v12_snake`**: 1군 `lotto_predictions` 동일 `target_draw_no` 6뇌 로드.
- **`v12_fusion_v5`**: 1군 fusion 세트 회피용 로드 + `get_cluster_weights(target_draw_no)` (구현은 1군/공용 모듈일 수 있음 — **lotto 폴더 미검**).
- **fusion 4뇌 PMF**: `stat/markov/combo/lstm` 하드코딩 유지 (`v12_fusion_v5.py` 상단 주석·`_v12_load_brain_pmfs` 일치).
- **hyena**: `v12_engine` 의 `_V11_HYENA_CONTRIBUTING` = **5기저뇌 + fusion** (`v12_stat, v12_run, v12_offset, v12_contrarian, v12_lstm, v12_fusion`) 가중 합의 후 15C6.
- **contrarian**: `_load_army_sets_for_target` — **동일 `target_draw_no`** 의 1·2군 세트로 Jaccard 회피.

---

## STEP 9 — 종합 표

| 항목 | 위치 | 심각도 | 1222 영향 | 권고 |
|------|------|--------|-----------|------|
| lstm 초기 누락 행 | `predict_lstm.py` + DB | 경미 | 없음(과거 회차만 공백) | 문서화 또는 placeholder 행 정책 결정 |
| combo 예측 0행 / PMF만 사용 | `v12_fusion_v5.py`, DB | 경미~중간 | 없음 | 가중치·문서 정합성 정리 |
| numpy warning 가능성 | `v12_fusion_v5`→1군 entropy | **확인 필요**(1군 미검) | 미확인 | 1군 허용 시 `predict_entropy` 점검 |
| `_tmp_*.py` 4개 | `My_Library/` | 경미 | 없음 | 백업 후 수동 정리 검토 |
| DB legacy 예측 비표준 태그 | 본 DB | **해당 없음(0건)** | 없음 | 다른 DB 덤프와 재대조 |
| 1등 0건 6뇌 | 각 predict + 필터 | 중간(모델 한계) | 당첨번호 무관 패턴 | 기대치 재설정·백테 지표 분리 |

---

## STEP 10 — 결론 4축

1. **1222 예측 안전성**: **조건부** — 본 정찰은 1222 미접근. 코드상 학습은 `draw_no < target` 위주이나, **동일 회차 타군 예측 읽기**(contrarian·snake·fusion v5)는 **미래 누수는 아님**, 다만 **타군 정보 의존**이 있음.
2. **1222 전 즉시 패치**: **0건**(READ-ONLY로 코드 변경 없음; 이슈는 대부분 설계·정합성).
3. **1222 후 패치**: **2건** 권고 — (a) 가중치 테이블 `army3_*` 잔재 행 정리方, (b) 1군 `predict_entropy` 경고 여부 점검.
4. **정리 가능(백업 제외)**: **3건** — `_tmp_*.py` 4개, 문서-DB 불일치 주석, `v12_combo` 가중치 행의 역할 명시.

---

## STEP 11 — 보고서 저장

- 본 파일: `D:/MONEY lol/My_Drive_Sync/커서보고서/20260502_0900_3군8뇌_시스템정찰_보고서.md`

---

## STEP 12 — 임시 파일

- `_tmp_sys_scout_*`: 워크스페이스 검색 **0건** → **삭제 없음**.
- 기존 `_tmp_*.py` 4개: **유지**(지시대로).

---

## 부록 — SQL / rg raw 추가

### MIN/MAX/COUNT (brain_tag별, `<1222`)

```text
v12_contrarian|6080|6|1221
v12_fusion|6080|6|1221
v12_hyena|6080|6|1221
v12_lstm|6055|11|1221
v12_offset|6080|6|1221
v12_run|6080|6|1221
v12_snake|6080|6|1221
v12_stat|6080|6|1221
```

### `rg "predict_combo" app/lotto3/` (요약)

- `v12_fusion_v5.py:138`
- `v12_fusion.py:15`

---

## 완료 멘트 (5줄)

1. 3군 8뇌 시스템 정찰 완료, 보고서 저장됨.
2. 문서 v7 **25/25** SHA256은 **1·2군 금지로 미이행**; **`app/lotto3` 16파일** 사전·사후 해시 **16/16 PASS**.
3. 발견 이슈 **6건**(심각 **0** / 중간 **2** / 경미 **4**) — 그중 2건은 「타군 의존·가중치 잔재」성격.
4. 1등 0건 6뇌 한 줄: **역발상·회피·필터·저확률 샘플링 구조**로 6/6 적중 기대가 극히 낮고, **fusion만 상대적으로 1등 다건**이 관측됨.
5. 1222 안전성: **조건부** — 미래 라벨 누수 징후는 없으나 **동일 회차 1·2군 예측 읽기**가 있으므로, 엄밀한 「군 독립」 관점에서는 운영 정책으로 보완 권고.
