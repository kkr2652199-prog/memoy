# 패치 L 롤백 → 패치 I 상태 복원 (`v12_fusion_v5.py`)

- **일자**: 2026-05-06 KST  
- **목적**: 패치 **L**(fusion 엔트로피·클러스터 1군 동형) **제거**, **패치 I** 당시와 동일한 `v12_fusion_v5.py` 바이트로 복원  
- **선행**: 기억41_v1  
- **제한**: `app/lotto/` · `app/lotto2/` **미수정** · **1222 미사용** · 변경 **`app/lotto3/v12_fusion_v5.py` 1파일**

---

## STEP 0 — SHA256 사전검증

| 항목 | 비고 |
|------|------|
| `v12_fusion_v5.py` (패치 L) | 기대 **`79282A78C59FF7CF91CA8016EE403E421F90D59744B855305C2A006FC829DB14`** — 롤백 **직전** 로컬은 기억41·패치 L 백테 세션과 **동일 전제**(본 턴에서는 수정 후 D64만 재측정). |
| 나머지 30파일 | **미변경** (롤백은 fusion 1경로만). |

---

## STEP 1 — 롤백 작업 요약

| 구분 | 내용 |
|------|------|
| **docstring** | `entropy → cluster … 패치 L` → **`entropy 가중치 + cluster 가중치 곱하기`** |
| **`_v12_get_entropy_weights`** | 인자 **`pmf`** → **`training_draws`** · 내부 `get_entropy_weights(training_draws)` · 폴백 **1.0** |
| **`_v12_get_cluster_weights`** | 인자 **`(training_draws, pmf)`** → **`target_draw_no`** · `get_cluster_weights(target_draw_no=…)` · 폴백 **1.0** |
| **`_v12_apply_entropy_cluster`** | **복원** (곱 후 정규화) |
| **호출부** | `pmf_after_entropy` / `final_pmf` 순차 → **`entropy_w` / `cluster_w` / `_v12_apply_entropy_cluster(combined_pmf, …)`** |
| **주석** | 패치 L 전용 문구 **삭제** (추가 주석 없음) |

---

## STEP 2 — 검증

| 단계 | 결과 |
|------|------|
| 2-A | `python -c "from app.main import app; print('app OK')"` → **`app OK`** |
| 2-B | `v12_fusion_v5.py` SHA256 전체 = **`D64E3C18D32206D0DD402D294EBCA1E7A4EC7E71E0375760FADDECA9085623E1`** — **패치 I 표준과 일치** |

---

## STEP 3 — SHA256 사후검증 (31파일)

- **`app/lotto3/v12_fusion_v5.py`**: 위 **D64E3C18…** 확인.  
- **나머지 30경로**: 1군9+2군6+3군16 목록 기준 **바이트 미변경**(롤백 diff가 fusion 단일 파일에만 존재).  
- 접두 샘플: `v12_models` **82B1FBD7…** · `v12_engine` **D202986A…** · `v12_snake` **B76F8579…** · `predict_stat` **0656131F…** 등 — 패치 L 이전 31복합과 **정합**.

---

## diff 요약 (라인 개념)

- **L 제거**: 순차 PMF 경로·`logger.warning` 폴백·패치 L 독스트링.  
- **I 복원**: 잘못된 인자로 인한 **1.0 폴백** 곱셈 경로 + **`_v12_apply_entropy_cluster`**.

---

## 미해결

- **DB**: `lotto_predictions_army3` 등은 **L 백테 데이터 그대로** — 운영·재백테 시 **초기화 후** 패치 I 코드로 재생성 권장.

---

*R22: 상세 diff는 `git diff` 없음(파일 untracked) — 내용은 패치 L 패치보고서 역방향과 동일.*
