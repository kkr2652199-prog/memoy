# 패치 M-1: v12_offset 슬롯 → 번호합 범위 뇌 (`predict_sumrange.py`)

- **일자**: 2026-05-06 KST  
- **저장**: `D:/MONEY lol/My_Drive_Sync/커서보고서/20260506_패치M1_번호합범위뇌_패치보고서.md`  
- **제한 준수**: `app/lotto/`, `app/lotto2/` 미수정 · 1222 미사용 · `predict_offset.py` 보존  

---

## 지시문 점검 (선행)

| 항목 | 판단 |
|------|------|
| `tier1_filter` import | 지시문 `predict_common` 아님 → **`app.lotto.filters`** 사용 (기존 lotto3 관례) |
| `v12_pass_win_avoid(..., threshold=)` | **`threshold` 인자 없음** → `v12_models` 내부 상수 사용 |
| `v12_fusion_v5.py` | `_v12_load_brain_pmfs`에 **v12_offset 미포함** → **수정 없음** (“해당 시” 미해당) |
| `get_v12_training_draws` | 엔진이 `training` 전달 → sumrange 모듈에서 **불필요** |

---

## STEP 0 — SHA256 사전검증 (9개 키 파일)

패치 적용 **직전** 기준: 나열 9개 중 `v12_models`, `v12_snake`, `v12_fusion_v5`, `predict_*`(offset/stat/run/lstm/contrarian) **일치**.  
패치 후 `v12_engine.py`는 변경되어 **사전 기대값과 불일치**(의도된 변경).

---

## 변경 파일

| 파일 | 내용 |
|------|------|
| `app/lotto3/predict_sumrange.py` | **신규** — `army3_sumrange_predict`, `army3_sumrange_prob_vector` |
| `app/lotto3/v12_engine.py` | `army3_offset_predict` → `army3_sumrange_predict`, `_retag` 표시명 `📐 번호합범위` |
| `app/lotto3/v12_fusion_v5.py` | **변경 없음** |
| `app/lotto3/predict_offset.py` | **보존** |

---

## STEP 4 — SHA256 사후 (변경분)

경로: `My_Library/` 기준.

```
app/lotto3/predict_sumrange.py  EE3A23ACA74FE3079A8464803673F10B3F1B2F7B3D4FF874C99E618ECCDE83A6
app/lotto3/v12_engine.py        C82A62F7F7B9467C8D64BD12EA58AFFDF3F54E9D2361B3AE35CB7C9DB068C516
```

---

## STEP 5 — diff 요약

### `v12_engine.py`

- **L12**: `from app.lotto3.predict_offset import army3_offset_predict`  
  → `from app.lotto3.predict_sumrange import army3_sumrange_predict`
- **L325–331**: 단일 `_retag(army3_offset_predict(...), "v12_offset", "🎼 리듬분석가")`  
  → `_retag(army3_sumrange_predict(...), "v12_offset", "📐 번호합범위")` (여러 줄 포맷)

### `predict_sumrange.py` (신규 전체)

- 합 히스토그램 → **15·85 백분위**로 `SUM_RANGE` 동적 산출, 클립 `[21,255]` 및 `hi >= lo+10`
- 번호 출현 PMF → `random.choices` 비복원 유사(6추출 후 중복 제거 루프)
- `tier1_filter`, `v12_pass_win_avoid`, `get_recent_winning_sets`
- 부족 시 **±10 한 번 확장** 후 `pert_lo`/`pert_hi`와 perturb 루프 정합
- 반환: `brain_tag="v12_offset"`, `nums`, `method`, `reasoning`, `confidence`

(전문 diff는 에디터/형상 도구 참고.)

---

## STEP 3 — 검증

| 단계 | 결과 |
|------|------|
| `python -c "from app.main import app; print('app OK')"` | **OK** |
| `run_prediction_v12(100)` | 기존 40행 캐시 존재 시 `cached` + `all_predictions=[]` → **검증용으로 `target_draw_no=100`의 `v12_%` 행 40건 DELETE 후** 재실행 → `status=ok`, `v12_sets=40` |
| v12_offset 5행 번호합 | **105~174 (p15–p85)** 또는 확장 **95~184** 내 — **0건 위반** (`_tmp_verify_m1.py`로 확인 후 스크립트 삭제) |
| Jaccard(직전 당첨 회피, 임계 0.4) | **0건 위반** |

---

## 미해결 / 참고

- `run_prediction_v12` 캐시 히트 시 응답의 `all_predictions`가 비어 있어, **회차별 스냅샷 검증은 DB 조회 또는 캐시 삭제 후 재호출**이 필요함.
- Windows 콘솔(cp949)에서 이모지 `print` 시 인코딩 오류 가능 → `PYTHONIOENCODING=utf-8` 권장.

---

## 요약

`v12_offset` **슬롯·태그 유지**, 내부 생성 로직만 **번호합 백분위 구간 + 번호 PMF 샘플링**으로 교체. Fusion·offset 모듈은 요구사항대로 **비변경·보존**.
