# 20260808 — 1~3군 구매 홀딩 · 1236 회차 숨김

**작업일:** 2026-08-08  
**범위:** 1~3군 API/UI · `honesty_flags.py` SSOT  
**배경:** 형 휴식 주간 — 이번 주 1~3군 구매 안 함 · 1236 예측 노출 시 FOMO·후회 방지

---

## 1. 형 결정

| 항목 | 결정 |
|------|------|
| 이번 주 구매 | **안 함** (1~3군) |
| 1236 예측 | **UI/API 비표시** (DB 삭제 아님) |
| N+1 자동 예측 | 1군 OFF 유지 · **2·3군 OFF 신규** |
| 1235까지 | 분석·복기용 **그대로 표시** |

---

## 2. DB 실측 (패치 전·후 동일 — 숨김만)

| 회차 | 1군 | 2군 | 3군 | draws |
|------|-----|-----|-----|-------|
| 1235 | 35 | 35 | 40 | ✅ 당첨 확정 |
| 1236 | 0 | 35 | 40 | ❌ 미추첨 |
| MAX(draw_no) | | | | **1235** |

1236 2·3군 35/40행은 **1235 당첨 확정 시 N+1 자동 생성**으로 이미 존재. 삭제하지 않고 API 필터.

---

## 3. 변경 파일

| 파일 | 변경 |
|------|------|
| `honesty_flags.py` | `PURCHASE_HOLD_*` · `ENABLE_ARMY2/3_AUTO_NEXT_PRED=False` · 헬퍼 3종 |
| `data_service.py` | 2·3군 N+1 자동 — 플래그 게이트 |
| `lotto/routes.py` | 1군 GET/POST 필터 · `GET /purchase-hold` |
| `lotto2/v11_routes.py` | 2군 GET/POST 필터 |
| `lotto3/v12_routes.py` | 3군 GET/POST 필터 |

---

## 4. 플래그 SSOT (`honesty_flags.py`)

```python
PURCHASE_HOLD_ACTIVE = True
PURCHASE_HOLD_HIDDEN_DRAWS = frozenset({1236})
ENABLE_ARMY2_AUTO_NEXT_PRED = False
ENABLE_ARMY3_AUTO_NEXT_PRED = False
```

**되돌리기:** `PURCHASE_HOLD_ACTIVE = False` 또는 1236을 `HIDDEN_DRAWS`에서 제거 · 2·3군 auto True 복원(원할 때) · **서버 재시작**

---

## 5. 검증

```text
python -c "from app.main import app; from app.lotto.honesty_flags import purchase_hold_status; print(purchase_hold_status())"
→ active=True hidden_draws=[1236] army1/2/3_auto_next=False

purchase_hold_blocks_draw(1236) → True
purchase_hold_blocks_draw(1235) → False
DB 1236 army2=35 army3=40 (행 유지, API 빈 배열)
```

---

## 6. 미해결

- 서버 재시작 후 UI에서 1236 드롭다운·번호 미노출 **형 수동 확인** 필요
- F-A~F-L 패치: 별도 ID 지시 대기
- 다음 주 구매 재개 시 홀딩 OFF 여부 형 결정
