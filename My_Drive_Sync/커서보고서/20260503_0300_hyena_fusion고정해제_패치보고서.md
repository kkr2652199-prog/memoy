🦅 동생 작성 지시문 기반 — 커서 자동 생성

# hyena 합의 `v12_fusion` 고정 해제 패치 보고서

- **작성**: 2026-05-03 03:00 KST  
- **선행 정찰**: `20260503_0230_v12_hyena_입력재설계_정찰보고서_파트1.md` §3-0 (fusion 2.0 고정 사실)  
- **금지 준수**: `app/lotto/` · `app/lotto2/` **미수정** · **`target_draw_no=1222` 미사용**

---

## 1. 요약

| 항목 | 내용 |
|------|------|
| **목적** | `_v12_merged_hyena_weights()`에서 **`v12_fusion`을 항상 2.0으로 덮어쓰던 동작 제거** → **`get_v12_brain_weights()`의 DB 값**을 그대로 hyena 합의에 사용 |
| **변경 파일** | `app/lotto3/v12_engine.py` **1곳** |
| **부가** | `SUMMARY/RULES_FIXED.md`에 **R22** 1줄 추가 (커서 보고서 파트 분할 규칙) |

---

## 2. RULES_FIXED.md — R22 추가

**추가 위치**: R21 블록 직후, `---` 구분선 앞.

**추가 문장(한 줄)**:

```text
R22 (NEW 제안): 커서 보고서는 파트당 최대 8000토큰. 초과 시 파트1/파트2로 분리 저장. 파일명 끝에 _파트1, _파트2 표기.
```

---

## 3. 코드 변경 (`v12_engine.py`)

### 3-A. 변경 위치

- **함수**: `_v12_merged_hyena_weights` (**구 54~64행 부근**)

### 3-B. 변경 전 / 후 (핵심)

**변경 전**

```python
def _v12_merged_hyena_weights() -> dict[str, float]:
    """DB 가중치 + v12_fusion은 FALLBACK 고정 (1군 _load_hyena_brain_weights 미러)."""
    out = dict(_V11_HYENA_FALLBACK_WEIGHTS)
    try:
        dbw = get_v12_brain_weights()
        for k, v in dbw.items():
            out[k] = float(v)
    except Exception:  # noqa: BLE001
        pass
    out["v12_fusion"] = float(_V11_HYENA_FALLBACK_WEIGHTS["v12_fusion"])
    return out
```

**변경 후**

```python
def _v12_merged_hyena_weights() -> dict[str, float]:
    """DB 가중치 전부 반영. v12_fusion 포함 (시드/예외 시 FALLBACK은 초기 dict)."""
    out = dict(_V11_HYENA_FALLBACK_WEIGHTS)
    try:
        dbw = get_v12_brain_weights()
        for k, v in dbw.items():
            out[k] = float(v)
    except Exception:  # noqa: BLE001
        pass
    return out
```

- **삭제**: `out["v12_fusion"] = float(_V11_HYENA_FALLBACK_WEIGHTS["v12_fusion"])`  
- **docstring**: 위와 같이 **DB 전량 반영**으로 정정.

---

## 4. 검증 raw

### 4-1) `app` import

```powershell
cd "d:\MONEY lol\My_Library"
python -c "from app.main import app; print('app OK')"
```

**출력**:

```text
app OK
```

### 4-2) `v12_fusion` = DB 값 일치

```powershell
python -c "from app.lotto3.v12_engine import _v12_merged_hyena_weights; from app.lotto3.v12_models import get_v12_brain_weights; mw=_v12_merged_hyena_weights(); db=get_v12_brain_weights(); print(mw.get('v12_fusion')); print(db.get('v12_fusion')); print(abs(mw.get('v12_fusion',0)-db.get('v12_fusion',0))<1e-6)"
```

**출력(로컬 DB 기준)**:

```text
125.30455081047687
125.30455081047687
True
```

- **≈125** 요구와 **일치**(소수 전체 동일).

### 4-3) SHA-256 (31파일, `0200` 보고서 STEP0 표준 해시와 비교)

- **`app/lotto3/v12_engine.py`**: **변경** — 사후 해시  
  **`2EA8BA18757AF3948FF5DD8C618FC3E018420419338AB555FFA8C2606FE277FF`**
- **나머지 30경로**: `20260503_0200_3군패치D후_백테스트_보고서.md` STEP0 표와 **바이트 단위 동일** (`mismatch 0` 스크립트 확인).

---

## 5. 영향·리스크 (정성)

| 항목 | 설명 |
|------|------|
| **합의 스케일** | `v12_fusion` 가중이 **2.0 → DB(예: 125)**로 커져 **번호별 합의 점수·5005 상대순위**가 변할 수 있음 → **hyena 5세트** 분포 변동 가능. |
| **1군 미러** | 1군 `predict_hyena`도 fusion을 **FALLBACK 고정**(`predict_hyena.py` 34~38행) — **3군만 DB fusion 반영**으로 **미러 불일치**가 남음(의도적 차별화인지 추후 형 판단). |
| **백테** | 본 패치는 **코드만** 갱신. **1221 이하 재백테**는 별도 지시 시 수행. |

---

## 6. 변경 파일 목록

| 경로 | 조작 |
|------|------|
| `D:/MONEY lol/My_Drive_Sync/SUMMARY/RULES_FIXED.md` | R22 1줄 추가 |
| `My_Library/app/lotto3/v12_engine.py` | `_v12_merged_hyena_weights` fusion 고정 제거 |

---

## 7. 미해결

- **전 구간 재백테**로 hyena·fusion 교차 영향 수치화 — **미실행**(본 패치 범위 외).  
- **1군 vs 3군** hyena fusion 정책 **통일 여부** — 정책 결정 대기.

---

*R22: 보고서 8000토큰 초과 시 `_파트1` / `_파트2` 분리 — 본 문서는 단일 파일로 작성.*
