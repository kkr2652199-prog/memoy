# 20260502_1545 — 1군 hyena 구조 정찰 (#6)

🦅 동생 작성 지시문 기반 — 커서 자동 생성 (압축 금지·간략 기입)

- 작성 시각: 2026-05-02 (KST)
- 선행: `20260502_1430_3군학습결함_벤치마킹_정찰보고서.md`(해시 표), `20260502_1500_1군lottery_score_정찰보고서.md`
- 제약 준수: 1·2군 소스 READ-ONLY, `target_draw_no=1222` 미사용, DB는 SELECT만(쓰기 0)

---

## STEP 0 — SHA256 사전 검증 (31/31)

`20260502_1430_3군학습결함_벤치마킹_정찰보고서.md` 표(경로 `My_Library/` 기준)와 실제 파일 해시를 PowerShell로 대조.

- 파싱 행 수: **31**
- 결과: **31/31 일치** (불일치 0건)

---

## STEP 1 — 1군 `app/lotto/` 하위 hyena 관련 파일

`rg` 패턴 `hyena` (경로 `My_Library/app/lotto`, 확장자 `.py`).

| 파일 | 대략 라인 수(물리 줄) | `^def ` 로 잡힌 함수 시그니처 |
|------|----------------------|------------------------------|
| `engine.py` | 668 | `_predictions_row_to_enriched`, `refresh_prediction_scores_for_target_draw`, `_lstm_predict_sets`, `_db_row_to_pred_dict`, `_delete_predictions_for_brain`, `_hyena_input_merged(`, `run_prediction`, `run_backtest`, `get_brain_status`, `get_hall_of_fame` |
| `predict_hyena.py` | 280 (UTF-8 `splitlines()` 기준) | `_load_hyena_brain_weights`, `_normalize_brain_tag`, `_weight_for_tag`, `_compute_consensus_score`, `_select_candidate_pool`, `_evaluate_combinations`, `_contributing_brain_count`, `_confidence_from_score`, `_build_sets_from_ranked`, `_hyena_predict_sets` |
| `predict_missanalysis.py` | 573 | (다수; 파일 내 `hyena`는 태그 튜플 등 문자열 수준) |
| `predict_snake.py` | 223 | `_load_draws_before_cutoff`, `_load_army1_sets_for_target`, `_is_diff_from_army1`, `_get_all_sub_brain_sets_for_cutoff`, `snake_predict_sets` |
| `feedback.py` | 475 | `analyze_prediction_feedback`, `get_feedback_summary`, `generate_all_feedback`, `_calculate_lottery_score`, `get_brain_tag_ranking`, `update_brain_weights`, `_load_brain_weights_from_db`, `_load_layer3_weights_5` |
| `models.py` | 93 | `get_lotto_db`, `init_lotto_db` |

핵심 구현 파일: `predict_hyena.py`, 입력·호출: `engine.py`.

---

## STEP 2 — 1군 hyena 입력 구성 (brain_tag)

**기저 태그 튜플(5뇌·fusion 포함 5키, 세트는 태그당 5세트로 통상 25행)**

```145:145:d:\MONEY lol\My_Library\app\lotto\engine.py
_BASE_HYENA_SOURCE_TAGS: tuple[str, ...] = ("stat", "markov", "llm", "lstm", "fusion")
```

**입력 병합: `fresh_by_tag` 우선, 없으면 DB `SELECT`**

```186:215:d:\MONEY lol\My_Library\app\lotto\engine.py
def _hyena_input_merged(
    conn,
    target_draw_no: int,
    fresh_by_tag: dict[str, list[dict]],
) -> list[dict]:
    """이번 실행에서 갱신한 태그는 fresh, 나머지는 DB에서 동일 회차 로드."""
    out: list[dict] = []
    for tag in _BASE_HYENA_SOURCE_TAGS:
        if tag in fresh_by_tag:
            out.extend(fresh_by_tag[tag])
            continue
        if tag == "llm":
            rows = conn.execute(
                """
                SELECT * FROM lotto_predictions
                WHERE target_draw_no = ? AND brain_tag IN ('llm', 'llm_fallback')
                """,
                (target_draw_no,),
            ).fetchall()
        else:
            rows = conn.execute(
                """
                SELECT * FROM lotto_predictions
                WHERE target_draw_no = ? AND brain_tag = ?
                """,
                (target_draw_no, tag),
            ).fetchall()
        for row in rows:
            out.append(_db_row_to_pred_dict(dict(row)))
    return out
```

**호출부(입력 길이·merge·`_hyena_predict_sets`)**

```338:359:d:\MONEY lol\My_Library\app\lotto\engine.py
    hyena_should_run = (not bf) or ("hyena" in bf)
    if hyena_should_run:
        if all(t in fresh_by_tag for t in _BASE_HYENA_SOURCE_TAGS):
            hyena_input: list[dict] = []
            for t in _BASE_HYENA_SOURCE_TAGS:
                hyena_input.extend(fresh_by_tag[t])
        else:
            hyena_input = _hyena_input_merged(conn, target_draw_no, fresh_by_tag)
        if len(hyena_input) < 10:
            conn.rollback()
            conn.close()
            return {
                "error": (
                    "하이에나 실행을 위해 stat~fusion 세트가 최소 10개 필요합니다 "
                    "(해당 회차의 기저 두뇌 예측이 DB에 있는지 확인하세요)."
                )
            }
        try:
            from app.lotto.predict_hyena import _hyena_predict_sets

            _delete_predictions_for_brain(conn, target_draw_no, "hyena")
            hyena_sets = _hyena_predict_sets(hyena_input, n_sets=SETS_PER_BRAIN)
```

**PMF**: `predict_hyena.py` 전역에서 `PMF`/`pmf` 문자열 **미존재**(로컬 `rg` 기준). 합의는 가중 번호 등장 합으로 정의됨(아래 STEP 4).

**3군 v12 hyena 입력(6뇌 + fusion, 동일 회차 `fresh` 리스트에 연장 후 hyena 호출)**

```299:307:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
    fresh: list[dict] = []
    fresh.extend(_retag(army3_stat_predict(training, SETS_PER_BRAIN_V12), "v12_stat", "V11통계두뇌"))
    fresh.extend(_retag(army3_run_predict(training, SETS_PER_BRAIN_V12), "v12_run", "🎯 사냥꾼"))
    fresh.extend(_retag(army3_offset_predict(training, SETS_PER_BRAIN_V12), "v12_offset", "🎼 리듬분석가"))
    fresh.extend(army3_contrarian_predict(training, target_draw_no, SETS_PER_BRAIN_V12))
    fresh.extend(_retag(army3_lstm_predict(training, SETS_PER_BRAIN_V12), "v12_lstm", "V11LSTM두뇌"))
    fresh.extend(v12_fusion_predict(training, target_draw_no, SETS_PER_BRAIN_V12))

    hyena_sets = _v12_hyena_predict(fresh, SETS_PER_BRAIN_V12)
```

| 항목 | 1군 (`engine.py`) | 3군 (`v12_engine.py`) |
|------|-------------------|------------------------|
| 입력 brain_tag 집합 | `stat`, `markov`, `llm`, `lstm`, `fusion` (145행) | `v12_stat`, `v12_run`, `v12_offset`, `v12_contrarian`(예측 dict 276행 등), `v12_lstm`, `v12_fusion` (299–305행) |
| 세트 수 통상 | 태그당 `SETS_PER_BRAIN`(엔진 상수; 본 보고서는 값 변경 없음) | `SETS_PER_BRAIN_V12 = 5` (26행) |
| DB merge SQL | `_hyena_input_merged` 내 `lotto_predictions` (197–212행) | 본 경로는 전부 `fresh` 메모리 입력 후 `_v12_hyena_predict` (307행); 별도 merge 함수 없음 |

---

## STEP 3 — 1군 hyena 후보 생성 로직

- **후보 풀 크기**: `top_n=15` 기본.

```83:91:d:\MONEY lol\My_Library\app\lotto\predict_hyena.py
def _select_candidate_pool(consensus: dict[int, float], top_n: int = 15) -> list[int]:
    try:
        if not consensus:
            return []
        ranked = sorted(consensus.items(), key=lambda x: (-x[1], x[0]))[:top_n]
        if len(ranked) < 6:
            logger.warning("하이에나: 합의 상위 pool 부족 (%d개)", len(ranked))
            return [n for n, _ in ranked]
        return [n for n, _ in ranked]
```

- **조합 수·샘플링**: `itertools.combinations(pool, 6)` → **5005** 전수; 세트 2~는 `random.choices`로 상위 50 중 가중 샘플.

```97:106:d:\MONEY lol\My_Library\app\lotto\predict_hyena.py
def _evaluate_combinations(
    pool: list[int], consensus: dict[int, float]
) -> list[tuple[tuple[int, ...], float]]:
    try:
        out: list[tuple[tuple[int, ...], float]] = []
        for combo in combinations(pool, 6):
            t = float(sum(consensus.get(n, 0.0) for n in combo))
            out.append((tuple(sorted(combo)), t))
        out.sort(key=lambda x: -x[1])
        return out
```

```150:207:d:\MONEY lol\My_Library\app\lotto\predict_hyena.py
    top50 = ranked[:50]
    ...
            pick = random.choices(
                idxs, weights=weights[: len(top50)], k=1
            )[0]
```

- **Top-K**: `ranked` 전체 정렬 후 `top50 = ranked[:50]`; 1세트는 `tier1_filter` 통과하는 첫 조합.

- **Jaccard(하이에나 경로)**: `predict_hyena.py` 내 **Jaccard 문자열 없음**(로컬 `rg` 기준).

---

## STEP 4 — 1군 가중치·스코어링

**가중치 로드(5뇌 DB + fusion 고정)**

```34:40:d:\MONEY lol\My_Library\app\lotto\predict_hyena.py
def _load_hyena_brain_weights() -> dict[str, float]:
    """5뇌(DB) + fusion(FALLBACK) = 6키. 예외 시 FALLBACK만."""
    try:
        w5 = _load_layer3_weights_5()
        return {**w5, "fusion": float(FALLBACK_BRAIN_WEIGHTS["fusion"])}
    except Exception:  # noqa: BLE001
        return dict(FALLBACK_BRAIN_WEIGHTS)
```

**DB 조회(5뇌 키)**

```503:525:d:\MONEY lol\My_Library\app\lotto\feedback.py
def _load_layer3_weights_5() -> dict[str, float]:
    """Layer3 Hedge: stat/markov/llm/lstm/hyena 5행 current_weight. 실패 시 SEED_WEIGHTS(5키) fallback."""
    seed = {k: float(SEED_WEIGHTS[k]) for k in LAYER3_BRAIN_TAGS}
    try:
        conn = get_lotto_db()
        try:
            rows = conn.execute(
                """
                SELECT brain_tag, current_weight FROM lotto_brain_weights
                WHERE brain_tag IN ('stat', 'markov', 'llm', 'lstm', 'hyena')
                """
            ).fetchall()
```

**합의 점수 식(문서 문자열 그대로)**

```59:76:d:\MONEY lol\My_Library\app\lotto\predict_hyena.py
def _compute_consensus_score(
    all_predictions: list[dict], weights: dict[str, float]
) -> dict[int, float]:
    """1~45 번호별 합의 점수: Σ(브레인가중 × 해당 세트에서 등장 1회당 1)."""
    try:
        if len(all_predictions) < 10:
            return {}
        scores: dict[int, float] = {n: 0.0 for n in range(1, 46)}
        for pred in all_predictions:
            w = _weight_for_tag(str(pred.get("brain_tag", "")), weights)
            if w <= 0.0:
                continue
            nums = pred.get("nums")
            if not isinstance(nums, (list, tuple)) or len(nums) < 1:
                continue
            for n in nums:
                if isinstance(n, int) and 1 <= n <= 45:
                    scores[n] += w
        return scores
```

**cluster / entropy**: `predict_hyena.py`에 해당 식별자 **없음**(로컬 `rg` 기준).

---

## STEP 5 — 3군 v12_hyena 동일 항목

**호출**

```307:308:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
    hyena_sets = _v12_hyena_predict(fresh, SETS_PER_BRAIN_V12)
    fresh.extend(hyena_sets)
```

**가중치·합의·풀·5005·정렬**

```54:88:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
def _v12_merged_hyena_weights() -> dict[str, float]:
    """DB 가중치 + v12_fusion은 FALLBACK 고정 (1군 _load_hyena_brain_weights 미러)."""
    out = dict(_V11_HYENA_FALLBACK_WEIGHTS)
    try:
        dbw = get_v12_brain_weights()
        for k, v in dbw.items():
            out[k] = float(v)
    except Exception:  # noqa: BLE001
        pass
    out["v12_fusion"] = float(_V11_HYENA_FALLBACK_WEIGHTS["v12_fusion"])
    return out


def _v12_compute_consensus_score(
    all_predictions: list[dict],
    weights: dict[str, float],
) -> dict[int, float]:
    """1~45 가중 합의 (1군 _compute_consensus_score 미러)."""
    consensus: dict[int, float] = {n: 0.0 for n in range(1, 46)}
    for p in all_predictions:
        tag = str(p.get("brain_tag", ""))
        w = float(weights.get(tag, 0.0))
        if w <= 0.0:
            continue
        nums = p.get("nums", [])
        if not isinstance(nums, (list, tuple)):
            continue
        for n in nums:
            try:
                ni = int(n)
            except (TypeError, ValueError):
                continue
            if 1 <= ni <= 45:
                consensus[ni] += w
    return consensus
```

```91:108:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
def _v12_select_candidate_pool(consensus: dict[int, float], pool_size: int = 15) -> list[int]:
    """상위 pool_size개 (점수 내림차순, 동점 시 번호 오름차순)."""
    ranked = sorted(consensus.items(), key=lambda x: (-x[1], x[0]))
    return [n for n, _s in ranked[:pool_size]]


def _v12_evaluate_combinations(
    pool_nums: list[int],
    consensus: dict[int, float],
) -> list[tuple[tuple[int, ...], float]]:
    """15C6=5005 전수, tier1 없음 (1군 _evaluate_combinations)."""
    from itertools import combinations

    out: list[tuple[tuple[int, ...], float]] = []
    for combo in combinations(pool_nums, 6):
        t = float(sum(consensus.get(n, 0.0) for n in combo))
        out.append((tuple(sorted(combo)), t))
    out.sort(key=lambda x: -x[1])
    return out
```

**Jaccard**: `_v12_hyena_predict` 본문에는 없음. 파일 내 Jaccard 언급은 **snake** 주석(310행).

**1 vs 3 대조(지시 5항목)**

| 항목 | 1군 raw | 3군 raw | 동일/차이 |
|------|---------|---------|-----------|
| 입력 | `_BASE_HYENA_SOURCE_TAGS` 145행 | `fresh.extend` 299–305행 | **태그·기저 알고리즘 집합 상이** |
| 샘플링 | `combinations`+`random.choices` 상기 | `_v12_evaluate_combinations`+`_v12_build_sets_from_ranked` 내 동일 패턴(97–208행 대역) | **동형** |
| 정렬 | `out.sort(key=lambda x: -x[1])` 105행 | `out.sort(key=lambda x: -x[1])` 108행 | **동일** |
| Jaccard | 하이에나 파일 내 없음 | 하이에나 경로 내 없음(310행은 snake) | **동일(부재)** |
| score 식 | 번호별 `Σ(w×등장)` 62행 주석+71–76행 | 번호별 `consensus[ni]+=w` 71–87행 | **합의 정의 동형**(태그·w 출처만 상이) |

---

## STEP 6 — top-1 0건 raw 원인 후보(코드·SQL 근거만)

| 질문 | 답 | 근거 |
|------|-----|------|
| 입력 차이로 인한 합의(가중 번호 합) 분포 변화 가능? | **Y** (입력 태그·예측 소스가 코드상 다름) | `engine.py` 145행 vs `v12_engine.py` 299–305행 |
| 정렬 키 차이? | **N** | `predict_hyena.py` 105행, `v12_engine.py` 108행 |
| Jaccard 회피 강도 차이? | **N**(하이에나 경로에 Jaccard 없음) | 상기 `rg` 결과; snake 주석만 `v12_engine.py` 310행 |
| Top-K(풀 15·상위50) 차이? | **N** | `predict_hyena.py` 83–91, 150행; `v12_engine.py` 91–94, 155행 |
| fusion 입력 의존 | **1군·3군 모두 fusion 계열 입력에 포함** | 1군 튜플에 `"fusion"`(145행); 3군 `v12_fusion_predict` (305행) |

**PMF를 통한 hyena 입력 로딩**: 본 경로 코드에서 **식별되지 않음** → 질문을 “PMF”에 한정하면 **해당 없음/모른다(PMF 미사용)** — `predict_hyena.py`에 pmf 문자열 없음.

---

## STEP 7 — DB raw (`target_draw_no <= 1221`)

실행 DB: `My_Library/data/lotto.db`  
도구: `sqlite3` (읽기 전용 SELECT)

**쿼리 1**

```sql
SELECT brain_tag, COUNT(*) AS cnt, AVG(matched_count), MAX(matched_count), SUM(CASE WHEN matched_count=6 THEN 1 ELSE 0 END) AS hits_6
FROM lotto_predictions
WHERE brain_tag='hyena' AND target_draw_no <= 1221;
```

**raw 출력**

```text
hyena|5955|2.28194794290512|6|2
```

**쿼리 2**

```sql
SELECT brain_tag, COUNT(*) AS cnt, AVG(matched_count), MAX(matched_count), SUM(CASE WHEN matched_count=6 THEN 1 ELSE 0 END) AS hits_6
FROM lotto_predictions_army3
WHERE brain_tag='v12_hyena' AND target_draw_no <= 1221;
```

**raw 출력**

```text
v12_hyena|6080|2.14046052631579|5|0
```

---

## STEP 8 — 종합 표(질문 / 1군 / 3군 / 차이 / 영향도)

| 질문 | 1군 raw | 3군 raw | 차이 | 영향도(코드상) |
|------|---------|---------|------|----------------|
| 입력 6뇌? | 5태그 `stat`…`fusion` (145행) | 6태그 `v12_*`+fusion (299–305행) | 기저 뇌 구성·태그명 전부 상이 | 합의 `consensus` 값 분포에 직접 반영(59–76 vs 67–87행) |
| 정렬 키 | 조합 점수 `t` 내림차순 (105행) | 동일 (108행) | 없음 | 후보 순위 로직 동일 |
| 회피 임계 | Jaccard 없음 | Jaccard 없음(hyena) | 없음 | — |
| Top-K | 풀 15, 상위 50 (83–91, 150행) | 풀 15, 상위 50 (91–94, 155행) | 없음 | — |
| score 식 | `Σ(브레인가중×등장)` (62행) | 가중 누적 (71–87행) | 태그·DB 테이블만 상이 | 동일 연산 구조 |

---

## STEP 9 — 4축 결론(추측 0)

1) **top-1(6적중) 0건 관련 raw 후보**  
   - DB: `v12_hyena`는 `MAX(matched_count)=5`, `hits_6=0`(STEP 7 raw).  
   - 코드: hyena 파이프라인은 1·3군 모두 **5005·풀15·정렬·tier1·top50 가중 샘플** 동형(`predict_hyena.py` 83–207, `v12_engine.py` 91–238).  
   - 코드: **입력 brain_tag 집합이 다름**(`engine.py` 145행 vs `v12_engine.py` 299–305행).

2) **가장 유력한 후보 1개(단정 금지, 코드·DB가 허용하는 범위)**  
   - “동일 파이프라인에서 **유일하게 확실한 차이**는 기저 입력 태그·예측 소스(145행 vs 299–305행)와 DB 집계(3군 `MAX=5`, 1군 `MAX=6`)” — **6적중 0건의 단일 원인을 코드 한 줄로 귀속하는 것은 이 자료만으로는 불가**(모른다: 인과는 미검증).

3) **패치 가능성**  
   - **Y**(가중치·입력 태그·기저 예측기 교체는 코드 변경으로 가능) — 구조상 `weights`·`fresh` 생성부가 분리되어 있음(`predict_hyena.py` 264–265, `v12_engine.py` 251–252).

4) **1222 영향**  
   - 본 작전에서 **1222 미접근**, 소스 해시 **변동 없음** → **1222에 대한 코드/DB 변경 없음**.

---

## STEP 10 — 보고서 저장

본 파일: `D:/MONEY lol/My_Drive_Sync/커서보고서/20260502_1545_1군hyena_구조_정찰보고서.md`

---

## STEP 11 — SHA256 사후 검증 (31/31)

STEP 0과 동일 스크립트 재실행 결과: **31/31 일치**, 불일치 0.

---

## STEP 12 — 임시 파일 정리

- 본 작전 중 **신규 생성 `_tmp_*` 없음**.  
- 워크스페이스에 기존 `_tmp_*` 일부 존재(예: `My_Library/_tmp_step1_c.txt` 등) — **삭제하지 않음**(작전 범위: 읽기·보고만).

---

## 검증 명령

```text
cd "d:\MONEY lol\My_Library"
python -c "from app.main import app; print('app OK')"
```

출력: `app OK`

---

## 미해결 사항

- `matched_count=6` 빈도 차이를 **입력 분포 차이로 인과 증명**하려면 회차별 `consensus`·`ranked` 덤프 또는 대조 실험이 필요 — **본 보고서 범위 밖(모른다)**.
