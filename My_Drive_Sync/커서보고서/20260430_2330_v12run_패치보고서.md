# v12_run 첫 뇌 패치 보고서

📅 2026-04-30 (KST)  
🎯 A안 확정: `v12_run` 가중치·CONTRIBUTING·`army3_run_predict` 통합 + 캐시 40행 + `nums` 스키마

---

## 1. PRE-BACKUP

- **위치**: [`backups/army3_pre_v12run_20260430_2315/`](../backups/army3_pre_v12run_20260430_2315/)
- **포함**: `app/lotto3/v12_engine.py`, `v12_models.py`, `predict_stat.py`, `data/lotto.db.before_v12run_20260430`, `CHECKSUMS.txt`, `ROLLBACK.ps1`

### CHECKSUMS.txt (백업 직전 원문)

```
8DDC2B478EB9943E7026E3F544B222A35708EF00C88F21FF275482D52B4245F3  app\lotto3\v12_engine.py
9B981967DB5FA330CBDE76BBE5B67BA8227255531F380420C6AA33419397062F  app\lotto3\v12_models.py
FBB134F05BED94FD081197268976FE0DE5144720A08F796E3619724BF8363A3A  app\lotto3\predict_stat.py
B82EB64C3F32C97F0B5C9F5A04466A924FECE8738E0FB0336496D104DAA9D66C  data\lotto.db
```

---

## 2. `predict_run.py` 전체 코드 (raw)

```python
"""3군 v12_run: 연속 출현(run) 통계 기반 예측 (워크포워드 전제, 호출자가 training만 전달).

출력은 v12_engine 파이프라인과 동일하게 `nums` 키 사용 (INSERT/합의 호환).
"""
from __future__ import annotations

import numpy as np

# run 가중·오프셋 보정 (지시서 α, β)
_ALPHA_RUN = 0.6
_BETA_OFFSET = 0.4
_NUM_COLS = 5  # run 길이 2~6 → 5열


def _draw_nums(d: dict) -> list[int] | None:
    try:
        return sorted(
            int(d[k])
            for k in ("num1", "num2", "num3", "num4", "num5", "num6")
        )
    except (KeyError, TypeError, ValueError):
        return None


def _maximal_run_segments(sorted_nums: list[int]) -> list[tuple[list[int], int]]:
    """한 회차 정렬 번호에서 길이>=2 인 최대 연속 구간만 반환."""
    if len(sorted_nums) < 2:
        return []
    runs: list[tuple[list[int], int]] = []
    i = 0
    n = len(sorted_nums)
    while i < n:
        j = i
        while j + 1 < n and sorted_nums[j + 1] - sorted_nums[j] == 1:
            j += 1
        seg = sorted_nums[i : j + 1]
        if len(seg) >= 2:
            runs.append((list(seg), len(seg)))
        i = j + 1
    return runs


def army3_run_predict(training: list[dict], n_sets: int = 5) -> list[dict]:
    """연속 출현(run) 빈도 + 미출현 경과 회차 가중으로 후보를 뽑아 n_sets개 세트 생성.

    예측 대상 회차는 인자로 받지 않음 — 학습 구간은 호출자가 워크포워드로만 넘길 것.
    """
    if not training or n_sets <= 0:
        return []

    # 결정성: 최근 최대 10회차 (draw_no, num1..6) 기반 시드
    tail = training[-10:]
    key_tuples: list[tuple[int, int, int, int, int, int, int]] = []
    for d in tail:
        nums = _draw_nums(d)
        if nums is None:
            continue
        try:
            dn = int(d["draw_no"])
        except (KeyError, TypeError, ValueError):
            dn = 0
        key_tuples.append((dn, *nums))
    seed = hash(tuple(key_tuples)) % (2**32)
    if seed < 0:
        seed += 2**32
    rng = np.random.default_rng(seed)

    draws: list[list[int]] = []
    for d in training:
        nums = _draw_nums(d)
        if nums is not None and len(nums) == 6:
            draws.append(nums)

    if not draws:
        return []

    # 번호별 run 참여 빈도 (45 × 5): 열 k = run 길이 (k+2)
    mat = np.zeros((46, _NUM_COLS), dtype=np.float64)
    last_idx: dict[int, int] = {}

    for idx, nums in enumerate(draws):
        for x in nums:
            last_idx[x] = idx
        for seg, leng in _maximal_run_segments(nums):
            col = min(leng - 2, _NUM_COLS - 1)
            for x in seg:
                if 1 <= x <= 45:
                    mat[x, col] += 1.0

    run_freq = mat[1:46].sum(axis=1)
    max_rf = float(run_freq.max()) if run_freq.size else 0.0
    last_draw_i = len(draws) - 1
    offsets = np.zeros(45, dtype=np.float64)
    for n in range(1, 46):
        if n in last_idx:
            offsets[n - 1] = float(last_draw_i - last_idx[n])
        else:
            offsets[n - 1] = float(len(draws))

    avg_off = float(offsets.mean()) if offsets.size else 1.0
    if avg_off <= 0:
        avg_off = 1.0

    scores = np.zeros(45, dtype=np.float64)
    for i in range(45):
        rfn = float(run_freq[i]) / max_rf if max_rf > 0 else 0.0
        ofn = float(offsets[i]) / avg_off
        scores[i] = _ALPHA_RUN * rfn + _BETA_OFFSET * ofn

    ranked_n = sorted(
        range(1, 46),
        key=lambda x: (-scores[x - 1], x),
    )
    pool15 = ranked_n[:15]

    out: list[dict] = []
    for _ in range(n_sets):
        pick = rng.choice(len(pool15), size=6, replace=False)
        nums6 = sorted(int(pool15[j]) for j in pick)
        conf = float(min(0.99, 0.35 + 0.1 * sum(scores[n - 1] for n in nums6)))
        out.append(
            {
                "nums": nums6,
                "brain_tag": "v12_run",
                "confidence": conf,
                "method": "V12런두뇌",
                "reasoning": "run_length_stats+offset",
            }
        )
    return out
```

---

## 3. `v12_engine.py` 변경 요약 (diff 개념)

- `from app.lotto3.predict_run import army3_run_predict` 추가 (predict_stat import 위).
- `_V11_HYENA_FALLBACK_WEIGHTS`에 `"v12_run": 1.0` 추가.
- `_V11_HYENA_CONTRIBUTING`에 `"v12_run"` 추가.
- 캐시: `len(existing) >= 35` → **`>= 40`** (기저 6×5 + hyena 5 + snake 5).
- `army3_stat_predict` extend 직후:  
  `fresh.extend(_retag(army3_run_predict(training, SETS_PER_BRAIN_V12), "v12_run", "V12런두뇌"))` 추가.

**하이에나** `_v12_hyena_predict`, `_v12_compute_consensus_score`, `_v12_merged_hyena_weights` **본문 미수정**.

---

## 4. `v12_models.py` 변경 요약

- `V11_BRAINS` 튜플: `"v12_stat"` 다음에 **`"v12_run"`** 삽입.
- `V11_SEED_WEIGHTS`: **`"v12_run": 1.0`** 추가.

---

## 5. 결정성 테스트 (raw)

```
3 import OK
determinism PASS 966 training rows
```

(`get_v12_training_draws(1100)` 동일 입력으로 `army3_run_predict` 두 번 호출 → 동일 리스트)

---

## 6. 워크포워드 검증 (raw)

- `rg "target_draw_no" app/lotto3/predict_run.py` → **0건** (문자열 미포함).

---

## 7. 미니 백테 50회차 (1100~1149)

- 호출: `run_prediction_v12(dn)` × 50 (로컬 Python).
- 각 회차 응답: **`ok`, `v12_sets`: 40** (8 brain_tag × 5세트).

**brain_tag별 (matched_count 평균, 50×5=250행):**

| brain_tag | count | avg_match |
|-----------|-------|-----------|
| v12_combo | 250 | 0.656 |
| v12_fusion | 250 | 1.696 |
| v12_hyena | 250 | 2.18 |
| v12_lstm | 250 | 1.864 |
| v12_markov | 250 | 0.872 |
| **v12_run** | **250** | **0.752** |
| v12_snake | 250 | 1.76 |
| v12_stat | 250 | 0.748 |

- **v12_run** 평균 매칭 **0.752** (목표 0.8 근접, 지시서대로 미달도 보고만).

---

## 8. SHA256 무손상 (1·2군 17 + 보호 3)

- **17/17**: [`20260430_2030_3군STEP1_패치보고서.md`](20260430_2030_3군STEP1_패치보고서.md) 표와 바이트 일치 (Python `hashlib` 검증, exit 0, `PASS`).
- **3/3 보호 파일** (`v12_fusion.py`, `v12_fusion_v5.py`, `predict_stat.py`) 동일 보고서 baseline과 일치.

---

## 9. 패치 후 신규·변경 파일 SHA256 (등록용)

| Hash (SHA256 대문자) | Path |
|----------------------|------|
| 56E328293D29D1A6AD870144F7EACCE0B40FC9A9E88882F8C9565F55E8705010 | app/lotto3/predict_run.py |
| 3880FEFFCC90E9787B48AC4253C8B3532A689D679B7F7CD0A59CB4FFACDEC55A | app/lotto3/v12_engine.py |
| 9CAC6CD1B6B616CD62CFDF3504EEAAB7C5DBDDD82E0296E016BA696200923C2D | app/lotto3/v12_models.py |

---

## 10. 임시 스크립트

- **삭제함**: `_tmp_v12run_minibt.py` (미니 백테 전용).
- **미삭제**: 저장소 내 기존 `_tmp_*.py` 타 패치 잔재 — 이번 작업에서 생성·삭제 대상 아님.

---

## 11. 종합 판정

| 항목 | 결과 |
|------|------|
| 1·2군 17파일 | **PASS** |
| 3군 보호 3파일 | **PASS** |
| import (`predict_run`, `run_prediction_v12`, `app`) | **PASS** |
| 결정성 | **PASS** |
| 워크포워드 (`predict_run`에 target_draw_no 문자열 0건) | **PASS** |
| 회차당 40세트 | **PASS** |
| `app OK` | **PASS** |

**종합: PASS**

---

## 참고

- DB `data/lotto.db`는 미니 백테로 **1100~1149 구간 army3 예측 행이 갱신**됨. 롤백 시 `ROLLBACK.ps1` + 백업 DB로 복원 가능.
