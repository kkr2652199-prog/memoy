# 4군 LLM 의존성 제거·점검 — 긴급 패치 보고서

**일자**: 2026-05-09  
**범위**: `d:\3kweon` — `app/lotto/`, `app/lotto2/`, `app/lotto3/` **수정 0건**

---

## 1. STEP 1 — 키워드 전수 조사 (`d:\3kweon`, `*.py`)

### 1.1 `localhost` / `127.0.0.1`

| 파일 | 라인 | 내용 |
|------|------|------|
| `app/main_v13.py` | 24 | CORS `allow_origin_regex`에 `localhost|127.0.0.1` 패턴 (HTTP 클라이언트 아님) |
| `run_v13.py` | 25 | `uvicorn` 바인딩 `host = "127.0.0.1"` (서버 수신 주소) |
| `app/lotto/predict_llm_client.py` | 8, 16 | `LOTTO_LLM_ENDPOINT = "http://localhost:1234"` — **1군 LLM(LM Studio)** |

### 1.2 `openai` / `ChatCompletion` / `lm_studio` / `lm-studio` / `/v1/chat` / `/v1/completions`

| 파일 | 라인 | 내용 |
|------|------|------|
| `app/lotto/predict_llm_client.py` | 24 | `url = f"{LOTTO_LLM_ENDPOINT}/v1/chat/completions"` |

*(대소문자 무관 `openai`/`ChatCompletion`/`lm_studio` 문자열은 다른 `.py`에서 추가 매칭 없음.)*

### 1.3 `requests.post` / `httpx` / `aiohttp`

| 파일 | 라인 | 내용 |
|------|------|------|
| `app/lotto/predict_llm_client.py` | 2, 36–37, 53, 56 | `import aiohttp`, LLM 호출·`/v1/models` 확인용 세션 |

*`requests.post` 매칭: 없음.*

**참고 (동행복권 HTTP, LLM 아님)**

| 파일 | 라인 | 내용 |
|------|------|------|
| `app/lotto/data_service.py` | 125, 167 | `requests.get` — 당첨번호 수집용 URL (`dhlottery` 등). **1군 전용**, 4군 `brains` 경로에서 미사용. |

### 1.4 `app/lotto4` (brains, ensemble, v13_engine_v2, tools)

- `https?://`, `aiohttp`, `httpx`, `openai`, `localhost`, `predict_llm`, `v1/chat` 등 **매칭 없음** (이번 검색 기준).
- LM Studio 호출 코드는 **1군 `predict_llm_client.py`에만** 존재.

---

## 2. STEP 2 — 제거·수정 내용

- **4군 뇌·ensemble·엔진**: 기존에 LLM API 호출이 없어 “삭제할 호출”은 없음.
- **회귀 방지·명시적 차단**
  - 신규: `app/lotto4/_llm_isolation.py` — `sys.modules`에 `app.lotto.predict_llm*` / `predict_llm_client*` 가 올라오면 `RuntimeError`.
  - `tools/full_backtest_v13.py`: 뇌 모듈 로드 직후 + 매 회차 처리 직후 가드 호출.
  - `app/lotto4/v13_engine_v2.py`: `run_prediction_v13` 성공 경로 종료 전 가드 호출.

---

## 3. STEP 3 — LM Studio 없이 백테스트

**명령**

```text
cd d:\3kweon
python tools/full_backtest_v13.py --start 1200 --end 1222 --no-online-update
```

**결과**

- **종료 코드**: 0 (에러 없음)
- **총 벽시계**: 약 **309.9 s (약 5.2분)** — 로그 기준
- **실행 대상**: 1200~1222 (23회차)
- **해석**: 외부 LLM 서버 없이 완료. 가드도 예외를 내지 않음.

---

## 4. SHA256 (변경·신규 파일)

| 파일 | SHA256 |
|------|--------|
| `app/lotto4/_llm_isolation.py` | `1C3A7643AA5B877855BA7ACA2DDDEBFC8D5789145DAA5FB55D1402530663B05F` |
| `tools/full_backtest_v13.py` | `96CD59008027A7CD6A07BCF32806358AA210EC3922E96046E95F77A56E7441F6` |
| `app/lotto4/v13_engine_v2.py` | `C4953232B1817F0969E9CE640B53BEFDBDEE6FF2579634994434029637FFA104` |

---

## 5. 1~3군 간섭

- `app/lotto/`, `app/lotto2/`, `app/lotto3/` **파일 수정 없음** (1군 LLM 클라이언트 등 기존 코드 유지).

---

## 6. 원인 정리 (참고)

백테스트 프로세스(`tools/full_backtest_v13.py`)만 보면 **1군 LLM 모듈을 import하지 않음**. LM Studio에 트래픽이 보였다면 다음 가능성이 큼.

- 동시에 **1군/통합 앱** 또는 **브라우저/UI**에서 LLM 예측 호출
- **LM Studio 자체**의 다른 클라이언트
- **같은 포트**를 쓰는 다른 도구

이번 패치로 4군 백테·`run_prediction_v13` 경로에 **1군 LLM 모듈이 끼어들면 즉시 실패**하도록 했다.

---

*보고서 경로: `d:\3kweon\reports\20260516_4군_LLM의존성제거_긴급패치_보고서.md`*
