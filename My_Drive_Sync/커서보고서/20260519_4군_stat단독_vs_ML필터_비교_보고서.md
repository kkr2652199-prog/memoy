# 4군 stat_generator 단독 vs ML 필터 비교 보고서

- **날짜**: 2026-05-19
- **목적**: ML 필터 없이 stat_generator 단독 성능 측정 → ML 필터 가치 판정
- **범위**: draw 1100~1222 (walk-forward, DB 읽기 전용)
- **코드 변경**: `tools/test_stat_generator_solo.py` 신규 1개만 (B안 코드 보존)
- **1~3군 간섭**: 0건

---

## STEP 0 – 파일 확인 (첫 3줄)

**RULES_FIXED.md**
```
# 🛡️ RULES_FIXED.md (불변 룰)
```

**STATUS_LATEST.md**
```
# STATUS_LATEST (2026-05-19 기억76)
## 3군: 패치 A~J+M 완료, avg 1.502, SHA 고정, 간섭 0
```

**CURSOR_RULES.md**
```
# CURSOR_RULES.md — 커서 행동 강제 규칙
# 최종 갱신: 2026-05-16
# 이 파일은 READ-ONLY. 형(사용자)만 수정 가능.
```

---

## 1. 테스트 스크립트 전문

`d:\3kweon\tools\test_stat_generator_solo.py`

```python
"""stat_generator 단독 성능 테스트 (1회성, DB 읽기 전용)."""

import json
import os
import sqlite3
import sys

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))
from app.lotto4.brains.stat_generator import generate_candidates

db_path = os.path.abspath("d:/3kweon/data/lotto4.db")
conn = sqlite3.connect(db_path)

START, END = 1100, 1222
results = []


def struct_score_simple(combo):
    nums = sorted(combo)
    total = sum(nums)
    odd = sum(1 for n in nums if n % 2 == 1)
    high = sum(1 for n in nums if n >= 23)
    score = 0.0
    score += 1.0 - abs(total - 137.5) / 75.0
    score += 1.0 - abs(odd - 3) / 3.0
    score += 1.0 - abs(high - 3) / 3.0
    return score


for draw_no in range(START, END + 1):
    row = conn.execute(
        "SELECT num1,num2,num3,num4,num5,num6 FROM lotto_draws WHERE draw_no=?",
        (draw_no,),
    ).fetchone()
    if not row:
        continue
    actual = set(row)

    candidates = generate_candidates(draw_no, db_path, 200)
    if not candidates:
        continue

    pick_first5 = candidates[:5]
    sorted_by_struct = sorted(candidates, key=struct_score_simple, reverse=True)
    pick_struct5 = sorted_by_struct[:5]
    all_hits = [len(actual & set(c)) for c in candidates]

    hits_first5 = [len(actual & set(c)) for c in pick_first5]
    hits_struct5 = [len(actual & set(c)) for c in pick_struct5]

    results.append({
        "draw": draw_no,
        "actual": sorted(actual),
        "first5_avg": sum(hits_first5) / len(hits_first5),
        "first5_max": max(hits_first5),
        "struct5_avg": sum(hits_struct5) / len(hits_struct5),
        "struct5_max": max(hits_struct5),
        "all200_avg": sum(all_hits) / len(all_hits),
        "all200_max": max(all_hits),
        "all200_hit4plus": sum(1 for h in all_hits if h >= 4),
    })

conn.close()
# ... (집계·출력·JSON 저장 — 스크립트 파일 참조)
```

---

## 2. 콘솔 출력 전체

```
=== stat_generator 단독 성능 (draw 1100-1222) ===

| 선택 방식 | avg | 4+적중 회차 | max |
|-----------|-----|------------|-----|
| 첫 5세트 (순서) | 0.8049 | 0 | 3 |
| 구조점수 상위 5 | 0.8585 | 0 | 3 |
| 200세트 전체 | 0.8364 | 48 | 5 |

비교 기준:
  랜덤 6/45: 0.800
  B안 ensemble: 0.797
  이전 에이스: 0.828
  3군: 1.502

=== 200세트 중 4+ 적중 발생 회차 ===
draw 1104: 4+적중 4건, max 4, actual [1, 7, 21, 30, 35, 38]
draw 1107: 4+적중 4건, max 5, actual [6, 14, 30, 31, 40, 41]
draw 1109: 4+적중 2건, max 4, actual [10, 12, 13, 19, 33, 40]
draw 1111: 4+적중 1건, max 4, actual [3, 13, 30, 33, 43, 45]
draw 1115: 4+적중 4건, max 4, actual [7, 12, 23, 32, 34, 36]
draw 1118: 4+적중 1건, max 4, actual [11, 13, 14, 15, 16, 45]
draw 1119: 4+적중 1건, max 4, actual [1, 9, 12, 13, 20, 45]
draw 1122: 4+적중 2건, max 4, actual [3, 6, 21, 30, 34, 35]
draw 1124: 4+적중 1건, max 4, actual [3, 8, 17, 30, 33, 34]
draw 1125: 4+적중 3건, max 4, actual [6, 14, 25, 33, 40, 44]
draw 1135: 4+적중 3건, max 4, actual [1, 6, 13, 19, 21, 33]
draw 1138: 4+적중 1건, max 4, actual [14, 16, 19, 20, 29, 34]
draw 1141: 4+적중 2건, max 4, actual [7, 11, 12, 21, 26, 35]
draw 1147: 4+적중 1건, max 4, actual [7, 11, 24, 26, 27, 37]
draw 1148: 4+적중 3건, max 4, actual [3, 6, 13, 15, 16, 22]
draw 1155: 4+적중 1건, max 4, actual [10, 16, 19, 27, 37, 38]
draw 1168: 4+적중 1건, max 4, actual [9, 21, 24, 30, 33, 37]
draw 1170: 4+적중 1건, max 4, actual [3, 13, 28, 34, 38, 42]
draw 1171: 4+적중 1건, max 4, actual [3, 6, 7, 11, 12, 17]
draw 1172: 4+적중 1건, max 4, actual [7, 9, 24, 40, 42, 44]
draw 1176: 4+적중 1건, max 4, actual [7, 9, 11, 21, 30, 35]
draw 1180: 4+적중 1건, max 4, actual [6, 12, 18, 37, 40, 41]
draw 1187: 4+적중 2건, max 4, actual [5, 13, 26, 29, 37, 40]
draw 1189: 4+적중 1건, max 4, actual [9, 19, 29, 35, 37, 38]
draw 1190: 4+적중 1건, max 4, actual [7, 9, 19, 23, 26, 45]
draw 1195: 4+적중 2건, max 4, actual [3, 15, 27, 33, 34, 36]
draw 1211: 4+적중 1건, max 4, actual [23, 26, 27, 35, 38, 40]
draw 1214: 4+적중 1건, max 4, actual [10, 15, 19, 27, 30, 33]

결과 저장: d:/3kweon/reports/stat_generator_solo_results.json
```

---

## 3. B안 SQL 결과 (lotto_fullbacktest_army4, 1100~1222)

| brain_tag | avg_mc | hit4plus | max_mc | total |
|-----------|--------|----------|--------|-------|
| v13_struct | 0.8992 | 0 | 3 | 615 |
| v13_evolution | 0.8927 | 1 | 4 | 615 |
| v13_seq | 0.8569 | 1 | 4 | 615 |
| v13_diversity | 0.8553 | 1 | 4 | 615 |
| **v13_ensemble** | **0.7967** | **0** | **3** | 615 |
| v13_ev | 0.7870 | 1 | 4 | 615 |
| v13_gap | 0.7675 | 1 | 4 | 615 |

---

## 4. 판정표

| 방식 | avg | 판정 |
|------|-----|------|
| 랜덤 기준 | 0.800 | 기준선 |
| B안 ensemble (ML필터) | 0.797 | **랜덤 미달, ML 필터 해악** |
| stat_generator 첫5세트 | 0.8049 | 랜덤 초과 (+0.005) |
| stat_generator 구조상위5 | 0.8585 | **ML 필터 대비 +0.061, 단순 통계 우수** |
| stat_generator 200세트 전체 | 0.8364 | **풀 품질 양호, ML 선택이 풀보다 열등** |
| 3군 | 1.502 | 목표 |

### 판정 로직 적용

| 조건 | 결과 |
|------|------|
| 200세트 avg(0.8364) > ensemble avg(0.797) | ✅ **ML 필터가 해악** — 풀에 좋은 조합 있으나 ML이 못 고름 |
| 구조상위5(0.8585) > ensemble avg(0.797) | ✅ **단순 통계 필터가 ML보다 우수** |
| 모든 stat 방식 avg > 0.800 | ✅ stat_generator 자체는 정상 (생성기 문제 아님) |

---

## 5. 고적중(4+) 회차 요약

- **200세트 풀**: 28회차에서 4+ 적중 조합 존재 (총 48건), 최대 **5적중** (draw 1107)
- **구조상위5 / 첫5세트**: 4+ 적중 회차 **0건** (max 3)
- **B안 ensemble**: 4+ 적중 **0건** (max 3)

→ stat_generator 200풀 안에 4~5적중 조합이 실제로 존재하나, **5세트 선택 로직**(ML·구조상위5·순서) 모두 이를 잡지 못함.

---

## 6. 결론: ML 필터 가치

**ML 필터는 현재 구성에서 가치 없음 — 오히려 해악.**

- stat_generator 200풀 평균 적중(0.8364)이 B안 ensemble(0.797)보다 **+0.039** 높음
- ML 5뇌 가중합이 풀에서 **열등한 5세트**를 고르고 있음
- 단순 구조점수(합계·홀짝·고저 중앙값) 상위 5세트(0.8585)가 ensemble보다 **+0.061** 우수
- 개별 ML 뇌(seq 0.857, struct 0.899)는 생성기로서는 양호하나, ensemble Commander의 ML 필터 조합이 성능을 **깎음**

---

## 7. 다음 방향 제안

1. **C안 — stat_generator + 단순 구조 필터**: ML 제거, `struct_score_simple` 상위 5세트 선택 (예상 avg ~0.86)
2. **풀 확대**: 200→500세트로 4+ 적중 조합 포착률 증가 후 선택 로직 개선
3. **선택 로직 교체**: ML score_batch 대신 풀 내 max-hit 탐색 또는 struct_brain 단독 필터
4. **ensemble 가중치 재검토**: 현재 5뇌 가중합이 seq/struct 개별 성능을 ensemble 이하로 끌어내림
5. **3군 벤치마킹**: 3군(1.502) 수준은 stat_generator+단순필터로도 도달 불가 — 3군 생성/필터 로직 벤치마크 필요

---

## 8. 1~3군 간섭

- `app/lotto/`, `app/lotto2/`, `app/lotto3/` 수정: **0건**
- B안 코드(stat_generator, seq, struct, ensemble 등): **보존, 미수정**

---

## 부록

- JSON: `d:\3kweon\reports\stat_generator_solo_results.json`
- 실행 시간: ~25s (123회차 × generate_candidates)
