# 4군 뇌 튜닝 3단계 — v13_contrarian_v2 (역발상 뇌) 보고서

**날짜**: 2026-05-13  
**작업 루트**: `d:\3kweon`  
**DB**: `d:\3kweon\data\lotto4.db`  
**제약**: `My_Library` — `d:\3kweon\app\` 기준 검색 결과 0건

---

## TASK별 변경 사항

### TASK 1: `app/lotto4/brains/contrarian_v2.py` 전면 교체

- **냉각 점수**: `gap[i] = draw_no - 마지막출현` (미출현 시 `draw_no`), `cooling = sigmoid((gap - MEDIAN_GAP) / SLOPE)`, `MEDIAN_GAP=7`, `SLOPE=3.0`.
- **과열 패널티**: 최근 `WINDOW=10`회 `freq_w`, `max(0, freq_w - 2) * 0.15`.
- **동반출현 역이용**: `load_cooccur3(db_path, COOCCUR_TOP_N=100)`의 각 트리플 구성 번호를 hot로 분류, hot에 `-0.03`, 그 외(보완 집합)에 `+0.02`.
- **최종 점수**: `max(cooling - overheat + cooccur_adj, 0.001)` → `generate_sets_with_filters` 입력.
- **필터**: 합계 100~175, 홀수 개수 2~4(2:4~4:2), Jaccard 0.5 미만, **5대역 중 최소 3대역** (`_band_filter`).
- **데이터 호환**: `load_draws_before`의 `[{draw_no, nums}]` 구조에 맞춤 (튜플 가정 제거).

### TASK 1 보조: `app/lotto4/brains/_utils.py`

기존 시그니처를 깨지 않도록 선택 인자 추가:

- `generate_sets_with_filters(..., odd_range: tuple[int,int] | None = None, extra_accept: Callable[[list[int]], bool] | None = None)`
- `_random_valid_combo`에도 동일 옵션 전달.

contrarian_v2 전용으로 `odd_range=(2,4)`, `extra_accept=_band_filter` 사용. 다른 뇌(trend/bayesian 등)는 기존과 동일(기본 홀짝 필터, `extra_accept` 없음).

### TASK 2: `tools/mini_backtest_v13.py`

- `v13_contrarian_v2` 및 `contrarian_predict` 추가.
- 구간 **1200~1222** 유지 (ensemble과 동일).

---

## 미니 백테스트 결과 (원문)

```
=== v13_trend ===
  총 세트: 115
  평균 적중: 0.696
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 3
  4등+: 0, 5등+: 3

=== v13_bayesian ===
  총 세트: 115
  평균 적중: 0.930
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 6
  4등+: 0, 5등+: 6

=== v13_ensemble ===
  총 세트: 115
  평균 적중: 0.835
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 4
  4등+: 0, 5등+: 4

=== v13_contrarian_v2 ===
  총 세트: 115
  평균 적중: 0.800
  최대 적중: 3
  6개(1등): 0, 5개(2·3등): 0, 4개(4등): 0, 3개(5등): 4
  4등+: 0, 5등+: 4
```

---

## 4뇌 비교표 (draw 1200~1222, 회차당 5세트)

| 뇌 | 평균 적중 | 최대 적중 | 5등+ 건수 | 비고 |
|----|-----------|-----------|-----------|------|
| v13_trend | 0.696 | 3 | 3 | 기준 트렌드 |
| v13_bayesian | 0.930 | 3 | 6 | 해당 구간 최고 평균 |
| v13_ensemble | 0.835 | 3 | 4 | 메타 |
| v13_contrarian_v2 | 0.800 | 3 | 4 | 역발상·대역 분산 |

**contrarian_v2**: 평균 적중 0.800, 5등+ 4건 (ensemble과 동일 건수, 평균은 베이지안보다 낮음, 트렌드보다 높음).

---

## 대역 분산 검증

- 명령: `predict(1224, lotto4.db)` 후 `all(_band_filter(s) for s in r)`.
- **결과: `True`** (5세트 모두 5대역 중 3개 이상 분포).

---

## 검증 결과 표

| 항목 | 명령/기준 | 결과 |
|------|-----------|------|
| import | `python -c "from app.lotto4.brains.contrarian_v2 import predict..."` | OK |
| predict 형태 | 5세트 × 6번호 | `5 [6,6,6,6,6]` |
| 대역 분산 | `_band_filter` 전세트 | `True` |
| API | `TestClient.post('/api/lotto4/v13/predict/1225')` | **200** (응답 `status=cached` 시 DB에도 `v13_contrarian_v2` **5행** 존재 확인) |
| 미니 백테스트 | `python tools/mini_backtest_v13.py` | **에러 없음**, 4뇌 출력 완료 |
| My_Library | `app\` 하위 `My_Library` | **0건** |

---

## 후속 제안

1. **구간 확장 백테스트**: 1200~1222는 샘플이 작음. 동일 스크립트로 구간을 넓혀 contrarian_v2 vs 베이지안·ensemble 안정성을 비교하는 것이 타당함.
2. **COOCCUR_TOP_N·PENALTY 민감도**: hot/cold 정의가 상위 100 트리플에 고정되어 있음. N 또는 패널티/가점 비율을 격자 탐색할 여지 있음.
3. **`generate_sets_with_filters` 재시도**: 극단적 가중치에서 5세트 미만이 나오면 보조 루프가 동작함. 필요 시 `max_retry`·RNG 시드 정책을 뇌별로 분리해 로그만 추가해도 운영 디버깅에 유리함.

---

## 기대 효과 (설계 관점)

- **앙상블 입력**: `ensemble.py`의 `SUB_BRAINS`에 이미 `contrarian_v2`가 포함되어 있어, 하위 7뇌 투표에 **역발상 시그널**이 반영됨.
- **포트폴리오 다양성**: 과열 회피·동반출현 hot 회피·**대역 분산**으로 trend/bayesian과 상관 낮은 조합이 나올 여지가 커짐.
- **3군 v12_contrarian 대비**: 본 구간에서는 평균 0.800·5등+ 4건으로 단기 지표는 확보됨. 장기 구간에서 v12 대비 우위는 별도 누적 검증이 필요함.

---

## 핵심 코드 위치

- 역발상 점수·세트 생성: `app/lotto4/brains/contrarian_v2.py` — `predict()`, `_band_filter`, `_calc_gap`, `_get_cooccur_hot_cold`.
- 공통 필터 확장: `app/lotto4/brains/_utils.py` — `generate_sets_with_filters`, `_random_valid_combo`.
