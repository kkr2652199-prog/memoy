# 패치 M-4: 제약만족 샘플링 뇌 — `v12_contrarian` 슬롯 교체

📅 **일자**: 2026-05-06 KST  
🔗 **선행**: 기억43_v1 · `20260506_패치M3_백테스트_보고서.md`  
⚠️ **제한 준수**: `app/lotto/` · `app/lotto2/` **미수정** (`tier1_filter` 등 **import만**)

---

## STEP 0 — SHA256 사전검증

로컬 실측 시 **`predict_cdm.py` ~ `predict_contrarian.py`** 등 지시문 나열 해시는 **본 패치 직전 기준과 일치** 확인함.

**참고**: 패치 적용 후 **`v12_engine.py`**는 호출부 변경으로 해시가 **`7FC26051…`(M-3 시점)**에서 갱신됨 → **STEP 4**에 패치 후 해시 기록.

---

## STEP 1 — 신규: `app/lotto3/predict_constraint.py`

| 항목 | 내용 |
|------|------|
| **역할** | `brain_tag` **`v12_contrarian`** 유지, 내부만 제약만족 샘플링으로 교체 |
| **PMF** | 학습 회차 번호별 출현 빈도 정규화 (`army3_constraint_prob_vector`) |
| **합 범위** | 학습 회차 당첨 6개 합의 **p15~p85** 선형 보간 (`SUM_PERCENTILE_LO/HI = 15/85`) |
| **엄격 제약** | 홀 개수 **2~4**, 저구간(1–22) 개수 **2~4**, 인접 연속쌍 **≤2**, `tier1_filter`, `v12_pass_win_avoid` |
| **완화** | 부족 시 합 ±**15**, 홀·저 **1~5**, 연속쌍 **≤3** 후 재시도; 여전히 부족 시 perturb 폴백 |
| **반환 시그니처** | `army3_constraint_predict(training_draws, n_sets=5, target_draw_no=None)` — CDM·공출현과 동일 순서 |

---

## STEP 2 — `v12_engine.py` 수정

| 위치 | 변경 |
|------|------|
| **10행** | `from app.lotto3.predict_contrarian import army3_contrarian_predict` → `from app.lotto3.predict_constraint import army3_constraint_predict` |
| **338~344행** | `army3_contrarian_predict(training, target_draw_no, SETS_PER_BRAIN_V12)` 제거 → `_retag(army3_constraint_predict(training, SETS_PER_BRAIN_V12, target_draw_no), "v12_contrarian", "🎯 제약만족샘플러")` |

*(기존 contrarian은 `(training, target_draw_no, n_sets)` 순서였음 → 새 함수는 `(training, n_sets, target_draw_no)`로 통일.)*

---

## STEP 3 — `v12_fusion_v5.py`

**변경 없음.** `_v12_load_brain_pmfs`는 **stat/markov/combo/lstm** 4뇌만 로드하며 **`v12_contrarian` PMF 미사용** 확인.

---

## STEP 4 — 검증

| 단계 | 결과 |
|------|------|
| **4-A** | `python -c "from app.main import app; print('app OK')"` → **`app OK`** |
| **4-B** | `get_v12_training_draws(500)` + `army3_constraint_predict(..., 5, 500)` → **5세트** 반환 |
| **4-C** | 동일 회차에서 **완화 구간** 기준(합 **±15**, 홀·저 **1~5**, 연속쌍 **≤3**) 검사 시 **5세트 모두 통과**. 동일 데이터에서 **엄격 제약**(합 p15–p85, 홀 2–4, 저 2–4, 연속≤2)으로 재검 시 **5세트 모두 strict_ok** |
| **4-D** | 엔진 전역 **Jaccard 위반 카운터 없음** — 기존 M-3 보고와 동일 서술 |

---

## STEP 5 — SHA256 사후검증 (패치 후)

```
predict_constraint.py (신규) = 7A2BF17B1F3E74AE7213B34799F18AF77DD66D322B74095DED4313ED72BDCBE8
v12_engine.py             = A23248FC9D94A96BAD254DC41B9C81D44F415FFDA61AC5B2699BDBEEF41C7684
```

- **`predict_contrarian.py`**: 보존, 해시 **`6AE36979…`** (STEP 0과 동일).

---

## STEP 6 — Diff 요약

### `v12_engine.py`

```diff
- from app.lotto3.predict_contrarian import army3_contrarian_predict
+ from app.lotto3.predict_constraint import army3_constraint_predict
...
-     fresh.extend(army3_contrarian_predict(training, target_draw_no, SETS_PER_BRAIN_V12))
+     fresh.extend(
+         _retag(
+             army3_constraint_predict(training, SETS_PER_BRAIN_V12, target_draw_no),
+             "v12_contrarian",
+             "🎯 제약만족샘플러",
+         )
+     )
```

### `predict_constraint.py`

- **신규 파일** (약 330줄 미만, 프로젝트 규칙 600줄 이하).

---

## 미해결 / 후속

- **`v12_routes.py`** API 한글 라벨은 여전히 **「역발상가」** 등 기존 표기 — UI 통일이 필요하면 별도 패치.
- Sonar: `_fill_sets` / `_perturb_fallback` **매개변수 개수** 경고 — 필요 시 설정 객체로 묶는 리팩터 검토.

---

*저장 경로: `D:/MONEY lol/My_Drive_Sync/커서보고서/20260506_패치M4_제약만족뇌_패치보고서.md`*
