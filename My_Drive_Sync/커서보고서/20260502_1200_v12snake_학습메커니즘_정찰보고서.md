🦅 동생 작성 지시문 기반 — 커서 자동 생성 (옵션 B)

# v12_snake 학습 메커니즘 정밀 정찰 보고서 (READ-ONLY)

- 작성: 2026-05-02 12:00 KST  
- 선행: `20260502_1100_3군8뇌_본질구조_정찰보고서.md` STEP 1  
- 제약: `app/lotto/`, `app/lotto2/` **미접근**, 코드·DB **쓰기 0**, `target_draw_no = 1222` **SQL 미사용** (`<= 1221` 등가)

---

## STEP 0 — SHA256 사전 (`app/lotto3` 16파일)

- 정리보고서(1000) STEP 0-B baseline과 **16/16 일치** → **PASS** (중단 없음).

---

## STEP 1 — `v12_snake.py` 전체 구조

### 1-A. 줄 수·`def` 목록

- **총 186줄**(파일 끝 `return sets[:n_sets]` 기준).
- **`def` 라인**:
  - `37`: `def _is_diff_from_army1(`
  - `58`: `def _load_army1_sets_for_target(`
  - `78`: `def v12_snake_predict_sets(`

### 1-B. 모듈 상단 `import` / `from` (라인 13–17)

```13:17:d:\MONEY lol\My_Library\app\lotto3\v12_snake.py
from __future__ import annotations

import logging
import random
from itertools import combinations
```

- **함수 내부 lazy import**(상단이 아님): `60`행 `from app.lotto.models import get_lotto_db`, `99`행 `from app.lotto.filters import tier1_filter`.

### 1-C. 가중치 dict (`_V11_SNAKE_WEIGHTS`)

```21:29:d:\MONEY lol\My_Library\app\lotto3\v12_snake.py
# 가중치 (1군 검증값 매핑: contrarian 50.0 / hyena 30.0 / 나머지 5.0)
_V11_SNAKE_WEIGHTS: dict[str, float] = {
    "v12_stat":   5.0,
    "v12_markov": 5.0,
    "v12_lstm":   5.0,
    "v12_contrarian": 50.0,   # 역발상가(구 miss_combo 자리)
    "v12_fusion": 5.0,
    "v12_hyena": 30.0,   # 1군 miss_weak 자리 (메타 두뇌)
}
```

### 1-D. `INSERT|UPDATE|DELETE|SELECT` (`v12_snake.py`만)

- `rg` 결과 **`SELECT` 1곳**: `65:72`행 `FROM lotto_predictions` … (1군 DB 경로 — **본 파일은 lotto3**, 호출은 런타임에 `app.lotto` 로딩).
- **`INSERT`/`UPDATE`/`DELETE`**: **본 파일 내 매칭 없음**.

### 1-E. `weight|learn|train|update|hedge|recent_avg` (대소문자 무시)

- `_V11_SNAKE_WEIGHTS` (`22`, `110`행 근처)
- `weights = [consensus[n] for n in pool]` (`158`행 — 합의 점수 배열, DB 헤지와 무관)

---

## STEP 2 — snake 예측 행 INSERT 주체

### 2-A. SQL (`brain_tag='v12_snake'`, `target_draw_no <= 1221`)

```text
COUNT=6080 | MIN(target)=6 | MAX(target)=1221 | MIN(created_at)=2026-05-01 14:51:10 | MAX(created_at)=2026-05-02 13:43:39
```

### 2-B. `rg "v12_snake" app/lotto3/`

- `v12_routes.py:275`, `370`
- `v12_snake.py:9,78,91,94,149,181`
- `v12_engine.py:16,312,315,316,319`

### 2-C. `v12_engine.py` snake 호출

```312:313:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
        snake_sets = v12_snake_predict_sets(fresh, target_draw_no, n_sets=SETS_PER_BRAIN_V12)
        fresh.extend(snake_sets)
```

### 2-D / 2-E — `lotto_predictions_army3` INSERT 위치·`brain_tag='v12_snake'`

- **INSERT 루프**: `v12_engine.py` `327:357`행 — `fresh` 내 각 `r`에 대해 `INSERT INTO lotto_predictions_army3` … `brain_tag`는 **`r.get("brain_tag", "v12_unknown")`** (`355`행).
- **snake 행의 `brain_tag` 문자열**: `v12_snake.py` `149`, `181`행 `"brain_tag": "v12_snake"`.

**주체(한 줄)**: **`run_prediction_v12` → `v12_snake_predict_sets` → `fresh` → 동일 함수 내 INSERT 루프** — raw: `v12_engine.py` 312–313행 + 337–356행 + `v12_snake.py` 149·181행.

---

## STEP 3 — `avg_match` 1.810 출처

### 3-A. SQL

```sql
SELECT AVG(matched_count), COUNT(*), SUM(matched_count)
FROM lotto_predictions_army3
WHERE brain_tag='v12_snake' AND target_draw_no<=1221 AND matched_count>=0;
```

**결과 raw**:

```text
1.8125|6080|11020
```

### 3-B. 1.810과 일치?

- **소수 둘째 자리 반올림 기준으로는 동일 계열**(1.8125 ≈ 1.81).  
- **`lotto_brain_weights_army3`의 `recent_avg_match`가 아님** — 아래 3-D.

### 3-C. snake 합의 가중치 출처

- **`_V11_SNAKE_WEIGHTS` 하드코딩**만 사용 — `108:114`행 `w = float(_V11_SNAKE_WEIGHTS.get(tag, 1.0))`.

### 3-D. `rg "lotto_brain_weights" app/lotto3/v12_snake.py`

- **매칭 없음**.

### 3-E. `rg "army2|army_2|v11_" app/lotto3/v12_snake.py`

- **매칭 없음**.

### 3-F. `lotto_brain_weights_army3`에 `v12_snake` 행?

```sql
SELECT COUNT(*) FROM lotto_brain_weights_army3 WHERE brain_tag='v12_snake';
```

→ **`0`**.

---

## STEP 4 — 1222 snake 가중치·캐시

### 4-A / 4-B. 코드상 가중치

- **합의 가중치**: `_V11_SNAKE_WEIGHTS` 고정 dict — **런타임 DB 갱신 없음**(본 파일 raw).
- **동적 “학습 가중치”**: `v12_snake.py` 내 **없음**.

### 4-C / 4-D — `target_draw_no = 1222` 조회

- **지시 절대 룰(1222 미접근)에 따라 SQL·파일 조회 미실행** → 해당 시점 `created_at`·“활성 가중치”는 **모름**.

---

## STEP 5 — fusion vs hyena 입력

### 5-A. `_v12_load_brain_pmfs` 4키

- `121:149:d:\MONEY lol\My_Library\app\lotto3\v12_fusion_v5.py` — 키 **`v12_stat`, `v12_markov`, `v12_combo`, `v12_lstm`** 만 PMF 로드.

### 5-B. `_V11_HYENA_CONTRIBUTING`

```38:40:d:\MONEY lol\My_Library\app\lotto3\v12_engine.py
_V11_HYENA_CONTRIBUTING: frozenset[str] = frozenset(
    {"v12_stat", "v12_run", "v12_offset", "v12_contrarian", "v12_lstm", "v12_fusion"}
)
```

### 5-C. 차이(한 줄)

- **fusion PMF**: 위 4키(마르코프·콤보는 **세트 예측이 아닌 PMF 입력**).  
- **hyena 합의**: `fresh`에 들어 있는 **6기저 태그**(`_V11_HYENA_CONTRIBUTING`)의 **이미 샘플링된 `nums`**에 가중 합산 — `v12_engine.py` `67:88`행 `_v12_compute_consensus_score` 로직(정찰 0900과 동일 구조).

### 5-D. contrarian은 fusion PMF에 없고 hyena 입력에는 있는 이유

- **fusion**: `_v12_load_brain_pmfs`에 **`v12_contrarian` 키 없음** — raw `v12_fusion_v5.py` 121–149행.  
- **hyena**: `fresh`에 `army3_contrarian_predict` 결과가 `v12_contrarian` 태그로 포함되고, `_V11_HYENA_CONTRIBUTING`에 **`v12_contrarian` 포함** — raw `v12_engine.py` 38–40행 + `303`행 `fresh.extend(army3_contrarian_predict(...))` 후 `307`행 `_v12_hyena_predict(fresh, ...)`.

---

## STEP 6 — 우회 경로 가설

| 가설 | raw |
|------|-----|
| 6-A 하드코딩만? | `_V11_SNAKE_WEIGHTS` 22–29행 + 110행 |
| 6-B army2 테이블 SELECT? | `v12_snake.py` 내 **매칭 없음**(3-E) |
| 6-C 매 회차 자체 weight 갱신? | **본 파일에 갱신 로직 없음** |
| 6-D 결론 | **Hedge 의미의 “학습”(DB 가중치 갱신)은 snake에 없음** — `update_v12_weights`는 `V11_BRAINS`만(`v12_models.py` 19–27, 156–166행). **당첨 채점으로 `matched_count`는 적재됨** — `_score_v12_predictions`는 `v12_engine.py` 359행 호출로 동일 트랜잭션 내 실행(INSERT 이후). |

---

## STEP 7 — 종합 raw 표

| 질문 | raw 근거 | 답 |
|------|----------|-----|
| snake 6080행 INSERT 주체 | `v12_engine.py` 329–356 INSERT 루프; `v12_snake.py` 149·181 `brain_tag` | **`run_prediction_v12` 내 공통 INSERT** |
| avg 1.810 계산 위치 | SQL `AVG(matched_count)=1.8125` (≤1221) | **`lotto_predictions_army3` 행 평균**(가중치 테이블 아님) |
| snake 가중치 출처 | `_V11_SNAKE_WEIGHTS` 22–29행 | **하드코딩 dict** |
| snake 학습 여부 | `lotto_brain_weights` snake **0행**; `update_v12_weights` `IN (V11_BRAINS)`에 snake 없음 | **Hedge 학습: 해당 없음(N)** / **채점 데이터는 적재(Y)** |
| 1222 snake 가중치 | 1222 SQL 금지 | **모름** |

---

## STEP 8 — 결론 4축

1. **snake 학습 여부**: **N**(Hedge·`lotto_brain_weights_army3` 갱신 경로 없음) — `V11_BRAINS` 19–27행, `update_v12_weights` 156–166행, `_V11_SNAKE_WEIGHTS` 22–29행.  
2. **1222 안전성**: **조건부 PASS** — 1222 행 **미조회**로 캐시·시각 검증 불가; 코드상 snake는 **고정 dict**만 사용.  
3. **헤지 편입 필요성**: **Y**(정책 문서 관점) — `update_v12_weights`가 snake를 **포함하지 않음** raw.  
4. **즉시 패치**: **N**(지시 READ-ONLY; 1222 영향은 **미검증**).

---

## STEP 9 — 본 보고서

- `D:/MONEY lol/My_Drive_Sync/커서보고서/20260502_1200_v12snake_학습메커니즘_정찰보고서.md`

---

## STEP 10 — SHA256 사후

- **16/16 PASS** (STEP 0과 동일 baseline).

---

## 부록 — `rg "^import|^from"` (`v12_snake.py` 상단만)

(STEP 1-B와 동일)
