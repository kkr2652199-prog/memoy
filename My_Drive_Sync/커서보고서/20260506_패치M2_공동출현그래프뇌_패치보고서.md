# 패치 M-2: v12_run 슬롯 → 공동출현 그래프 뇌 (`predict_cooccur.py`)

- **일자**: 2026-05-06 KST  
- **저장**: `D:/MONEY lol/My_Drive_Sync/커서보고서/20260506_패치M2_공동출현그래프뇌_패치보고서.md`  
- **제한 준수**: `app/lotto/` · `app/lotto2/` **미수정** · **1222 미사용** · `predict_run.py` **보존**

---

## 지시문 점검 (선행)

| 항목 | 판단 |
|------|------|
| `v12_pass_win_avoid(combo, target_draw_no)` | **오기** — 실제 시그니처는 `(combo, winning_sets, st)` · `get_recent_winning_sets` 사용 |
| 반환 키 `numbers` | 엔진·DB 호환을 위해 **`nums`** 유지 (M-1·`predict_run` 동일) |
| `is_diff_from_recent_wins` | `v12_pass_win_avoid` 내부에서 사용 → **직접 import 생략** |
| `collections` | 미사용 → **import 생략** (동작 불변) |
| `v12_fusion_v5.py` | `_v12_load_brain_pmfs`에 **v12_run 없음** (M-1과 동일) → **수정 없음** |

---

## STEP 0 — SHA256 (지정 10경로)

본 세션 구현 후 디스크 기준:

| 파일 | 사용자 기대 | 본 세션 결과 |
|------|-------------|----------------|
| `predict_sumrange.py` | EE3A… | **일치** (M-2에서 미변경) |
| `v12_engine.py` | C82A… | **불일치** (의도된 변경) |
| `v12_models.py` | 82B1… | **일치** |
| `v12_snake.py` | B76F… | **일치** |
| `v12_fusion_v5.py` | D64E… | **일치** |
| `predict_stat.py` | 0656… | **일치** |
| `predict_run.py` | 0509… | **일치** (보존) |
| `predict_offset.py` | B5D9… | **일치** |
| `predict_lstm.py` | 02D6… | **일치** |
| `predict_contrarian.py` | 6AE3… | **일치** |

- **신규** `predict_cooccur.py`: `1E72FFBCEE0160CA866C9118DE3AF8F5049EF84D3165FF6E51FE79C61FB7C183`  
- **변경** `v12_engine.py` (사후): `54E18F785FBDAFA0FDB5F9F0A30BBAC1DF0935E552C3680E908EDAB02E68B1ED`

---

## STEP 1 — 신규 `app/lotto3/predict_cooccur.py` 요약

- **45×45** 공동출현 행렬: 회차별 C(6,2) 쌍에 가중 누적, **최근 50회**에 `RECENCY_WEIGHT=2.0`.  
- **탐욕 모듈러리티 병합**: 45개 단일 노드 커뮤니티에서 **쌍 병합 ΔQ** 최대 선택, **6개**까지 반복 (`N_COMMUNITIES=6`).  
- **번호 추출**: 커뮤니티당 내부 연결 강도 가중으로 **1개**씩 → 6개(정렬).  
- **`tier1_filter`** + **`v12_pass_win_avoid`** · 메인 루프 최대 **3000**회 · 부족 시 **PMF fallback** + `v12_perturb_combo_one_swap` 보조.  
- 학습 **20회 미만**이거나 **총가중치≈0**이면 그래프 생략·fallback 위주.  
- `army3_cooccur_prob_vector`: 비대각 행합 정규화 PMF.

---

## STEP 2 — `v12_engine.py`

- **import**: `army3_run_predict` → `army3_cooccur_predict`  
- **호출**: `_retag(..., "v12_run", "🕸️ 공동출현그래프")`  
- **`brain_tag`**: `v12_run` 유지

---

## STEP 2-C — `v12_fusion_v5.py`

- **변경 없음** (v12_run PMF 미로드).

---

## STEP 3 — 검증

| 항목 | 결과 |
|------|------|
| `python -c "from app.main import app; print('app OK')"` | **app OK** |
| `army3_cooccur_predict(get_v12_training_draws(100), 5, 100)` | **5세트** · `brain_tag` 전부 `v12_run` |
| 직전 당첨 Jaccard(0.4) | **5/5 True** (`is_diff_from_recent_wins`) |
| 커뮤니티별 출처 로그 | `logger.debug`에 **커뮤니티 크기**만 기록(번호→커뮤니티 매핑은 필요 시 동일 로직으로 확장 가능) |

---

## STEP 4 — 사후 SHA256

위 표 참조 (`predict_cooccur` 신규 · `v12_engine` 변경).

---

## STEP 5 — diff 요약

### `v12_engine.py`

- **L13**: `from app.lotto3.predict_run import army3_run_predict` → `from app.lotto3.predict_cooccur import army3_cooccur_predict`  
- **L323–329** (부근): `army3_run_predict` + `"🎯 사냥꾼"` → `army3_cooccur_predict` + `"🕸️ 공동출현그래프"` (다줄 `_retag`)

### `predict_cooccur.py`

- **신규 전체** (약 280행) — 위 요약 참고.

---

## 미해결 / 확인 필요

- 모듈러리티 탐욕은 **휴리스틱**이며, 라이브러리 Louvain과 수치 동일성은 보장하지 않음.  
- `predict_run.py`는 **미참조 보존** — 롤백·비교용으로 유지.

---

*R22: 수치·해시는 본 세션 로컬 실행 기준.*
