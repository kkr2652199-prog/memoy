# 잼마 전용 뇌 (학습 로그)

> 인터넷·책·강의·잼마 대화에서 **새로 습득한 것**만 **짧게** 쌓는다.
> (긴 원문 복붙 금지 — **요약**과 **출처 URL**)

---

### 2026-04-25
- **출처:** Python Fundamentals for Beginners (W3Schools, GeeksforGeeks 등) — 파이썬 기본 문법 및 기초 개념 학습
- **한 줄 요약:** 파이썬은 읽기 쉬운 구문을 가진 고수준 언어로, 변수, 문자열, 숫자, 불리언과 같은 기본 자료형을 사용하여 프로그래밍을 시작한다.
- **우리 앱에 쓰면:** 데이터 분석이나 자동화의 기초를 다지고, 코딩 사고방식의 기반을 확립한다.
- **다음에 볼 키워드:** 변수(Variables), 자료형(Data Types), 기본 연산자, `print()` 함수

### 2026-04-24
- **출처:** LM Studio 로컬 API `POST /v1/chat/completions`, 모델 `google/gemma-4-e4b` — 잼마 멘토 **5강**\n- **한 줄 요약:** 리스트(`[]`)를 생성하고 `append`로 추가하며, `for` 루프로 순회하여 데이터를 처리하는 방법을 익혔다.\n- **우리 앱에 쓰면:** API에서 받는 JSON 배열을 읽고 반복문으로 자료를 효율적으로 분석하는 기초가 된다.\n- **다음에 볼 키워드:** `dict`, `items()`, `get()`, 리스트 컴프리헨션

### 2026-04-23
- **출처:** LM Studio 로컬 API `POST /v1/chat/completions`, 모델 `google/gemma-4-e4b` — 잼마 멘토 **4강**\n- **한 줄 요약:** `def`를 사용해 코드 블록을 묶고, `return`으로 계산 결과를 반환하는 함수 작성법을 익혔다.\n- **우리 앱에 쓰면:** API 라우트나 헬퍼 함수가 이름(인자)과 반환 값(결과)의 구조로 설계되는 원리를 이해한다.\n- **다음에 볼 키워드:** `리스트`, `append`, `import`, 기본 스코프 (지역/전역 개념)

### 2026-04-22
- **출처:** LM Studio 로컬 API `POST /v1/chat/completions`, 모델 `google/gemma-4-e4b` — 잼마 멘토 **3강**\n- **한 줄 요약:** `for`와 `while`을 사용하여 정해진 범위나 조건에 따라 반복 실행하는 방법을 배운다.\n- **우리 앱에 쓰면:** 데이터 목록이나 API 결과(`range`, 리스트)를 순회하며 처리하는 기초가 된다.\n- **다음에 볼 키워드:** `range`, `break`/`continue`, 리스트 컴프리헨션, `def`/`return`

### 2026-04-25 (Step 6 추가 기록)
- **출처:** 본 학습 세션 — 문자열 처리(split, join, f-string) 학습\n- **한 줄 요약:** 문자열을 분리하고(`split`), 리스트를 다시 합치고(`join`), 변수 값을 문자열 안에 삽입하는(`f-string`) 방법을 익혔다.\n- **우리 앱에 쓰면:** 외부에서 수집된 텍스트 데이터(예: 위키 항목)를 정제하거나, API 응답을 사용자 친화적인 형태로 포맷팅할 때 핵심적으로 사용됩니다.
- **다음에 볼 키워드:** 문자열 불변성, 정규표현식 기초 (다음 단계 준비)

### 2026-04-24 (Step 7 추가 기록)
- **출처:** 본 학습 세션 — 파일 입출력(open, read, write) 학습\n- **한 줄 요약:** 파일을 열고(`open`), 내용을 읽거나(`read`), 쓰거나(`write`) 저장하는 방법을 익혔다.\n- **우리 앱에 쓰면:** 설정 파일이나 로그 파일 등 시스템 상태를 영구적으로 기록하고 불러오는 데 사용됩니다. \n- **다음에 볼 키워드:** 파일 핸들링, 예외 처리(try/except)</replace>